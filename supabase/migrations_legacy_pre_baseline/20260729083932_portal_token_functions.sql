-- (أُلغيت — فشلت على `gen_random_bytes` غير المؤهَّل، تحلّ محلّها
--  20260729084021_portal_token_functions_qualify_pgcrypto.sql)
-- تُترك كسجلّ إصدار مطابق للإنتاج، بلا أثر عند إعادة البناء.
select 1;
