# AGENTS.md — دليل التطوير لـ ShipAudit Pro (لمحة)

## تحديث 2026-08-07 — إسناد مهام التحصيل بتفويض صريح
- توزيع مهام `collection_tasks` إجراء إداري مستقل يتطلب `collections.assign`؛ امتلاك عرض قائمة التحصيل أو تحديث مرحلتها لا يمنح الإسناد تلقائيًا.
- الإسناد الجماعي يقتصر على المهام المفتوحة (`todo` و`contacted` و`promised` و`snoozed`) وعلى موظف غير مدير يملك `collections.view` و`collections.update_stage` معًا. لا تمنح الصلاحية الجديدة لأي موظف قائم تلقائيًا.
- `assign_collection_tasks` يغيّر `assigned_to` فقط ويسجل دفعة الإسناد في `collection_task_assignment_batches`. ممنوع أن يغيّر الدين أو المرحلة أو الوعد أو أي رقم مالي.
- شاشة قائمة التحصيل تعرض المسؤول وتوفر تحديدًا جماعيًا وإسنادًا أو إلغاء إسناد واضحًا. فتح الصفحة أو تحديث بياناتها لا يشغّل الإسناد تلقائيًا.

## تحديث 2026-08-07 — قائمة رئيسية وقائمة سياقية للمركز
- التنقل على الحاسب يتكون من مستويين ظاهرين: **القائمة الرئيسية للمراكز** ثم **قائمة سياقية مستقلة لصفحات المركز المفتوح**. لا تعاد صفحات المركز كصفوف متداخلة داخل القائمة الرئيسية ولا كشريط تبويبات أفقي أعلى المحتوى.
- `ROUTE_ITEMS.subTabs` هو المصدر الوحيد لصفحات القائمة السياقية ومساراتها وصلاحياتها. المسارات التاريخية في `legacy` تبقى فعالة وتحدد الصفحة السياقية النشطة دون تكرار مدخلها في القائمة الرئيسية.
- مركز «الإعدادات» يجمع الذكاء الاصطناعي، البيانات والتكاملات، الفريق والصلاحيات، شركات الشحن، والعقود والأسعار. تبقى المسارات `/employees` و`/carriers` و`/contracts` مستقلة ومحميّة، لكنها تظهر للمستخدم داخل قائمة الإعدادات السياقية لا كمراكز رئيسية متساوية.
- على الجوال لا تُضغط قائمتان بجانب بعضهما كما تفعل بعض الأنظمة المرجعية؛ تختفي القائمة السياقية المكتبية وتتحول إلى اختيار واحد واضح باسم «داخل [المركز]» أعلى المحتوى. القائمة الرئيسية تبقى درجًا كاملًا قابلًا للتمرير.
- الصفحة ذات الأقسام الثانوية الدقيقة داخل الصفحة نفسها يمكنها الاحتفاظ بتحكم محلي صغير، لكن لا تعيد صفحات المركز أو عناوين القائمة السياقية كشريط أفقي.
- أي مركز جديد يملك أكثر من صفحة يجب أن يعرّفها في `subTabs` مع `tabId` ثابت ومسار `?tab=` أو `legacy`، ويجب أن يمر باختبار عقد التنقل في `tests/ui-layout-contract.test.mjs`.

## تحديث 2026-08-06 — مواعيد الناقلين الصريحة وحماية نوع الملف
- جدول الناقل لا يعتمد رقمًا غامضًا في `day_of_period`. الحقلان `schedule_basis` و`due_days` هما المصدر الصريح: `weekday` يحفظ يوم الأسبوع 0–6، و`month_days` يحفظ تواريخ الاستلام الفعلية داخل الشهر. يبقى `day_of_period` للتوافق مع السجلات القديمة فقط.
- نموذج الجدولة يعرض اختيارًا مسمى: يوم أسبوعي ثابت أو تواريخ شهرية محددة. الموعد غير المحدد لا يُحفظ ولا يتحول تلقائيًا إلى الأحد أو اليوم الأول.
- الناقل ذو `file_kind='audit_with_cod'` يملك جدول «فاتورة + تحصيل» واحدًا من نوع `invoice`؛ يمنع إنشاء جدول تحصيل منفصل له لأن اعتماد الملف نفسه يثبت الالتزامين. الناقل ذو `audit_and_cod_separate` يحتاج جدول فاتورة وجدول تحصيل مستقلين.
- ترحيل قاعدة البيانات يحوّل الجداول التاريخية إلى `due_days` دون تغيير معناها: 0–6 أيام أسبوع، والبداية الأسبوعية تتحول إلى تواريخ كل 7 أيام، ونصف الشهري إلى موعدَين.
- لوحة المهام تحسب الاستحقاق من المواعيد الصريحة لا من `last_completed_at + 7/30 يومًا`. الرفع المتأخر لا يرحّل موعد الشركة القادم؛ زر «تسجيل استلام» يحدّث التنبيه فقط ولا يثبت اكتمال دورة المحاسب أو يعوض سجل الملف.
- أعلى `/tasks` تظهر لوحة **اكتمال جداول الناقلين** لكل ناقل ذي عقد ساري. `audit_with_cod` و`audit_only` يحتاجان جدول فاتورة واحدًا، و`audit_and_cod_separate` يحتاج جدولَي فاتورة وCOD مستقلين، و`cod_only` يحتاج جدول COD. الناقل غير المصنف أو الجدول الناقص/غير الصالح يبقى ظاهرًا بإجراء مباشر ويمنع الإقفال.
- مرحلة مراجعات فواتير الناقلين لا تطلب فاتورة من `cod_only`؛ يظهر «غير مطلوب» فيها، لكنه يبقى مطلوبًا في مرحلة تحصيلات الناقلين حسب جدول COD. وجود جدول يدوي لناقل بلا `file_kind` لا يعوض تصنيفه ولا يسمح بإقفال الدورة.
- إذا كانت كل شركات الفترة `cod_only` أو لا تملك فاتورة مجدولة صراحةً، تصبح مرحلتا تصدير الأوزان وشحنات لمحة «غير مطلوبتين/مكتملتين» بدل طلب مراجعة وهمية؛ لا يؤثر ذلك على وجوب استلام دفعات COD أو رفع تحصيل لمحة.
- نموذج الجدولة يعرض من مهام الدورة ما يطابق `file_kind` فقط: الموحّد/الفاتورة فقط يعرضان الفاتورة، و`cod_only` يعرض التحصيل، والمنفصل يعرض الاثنين. كشف الحساب وتقرير الأوزان يظلان تذكيرين مستقلين لكل الأنواع، والناقل غير المصنف لا يقبل جدول فاتورة أو تحصيل يغطي خطأ التصنيف.
- اكتمال الجدول لا يُحسب بعدد الملفات المجرد. كل مراجعة فاتورة أو ملف تحصيل في جدول متعدد المواعيد يحمل `scheduleSlot` بتاريخ الموعد الذي يغطيه؛ الموعد نفسه يُحسب مرة واحدة ولو اختلف اسم الملف، والملف غير المنسوب يبقى ظاهرًا ولا يكمل الدورة. الجدول الشهري ذو الموعد الوحيد يُنسب تلقائيًا، وملف `audit_with_cod` يستخدم الإسناد نفسه لإثبات الفاتورة والتحصيل معًا.

## تحديث 2026-08-06 — اكتمال أرقام شحنات لمحة بالمطابقة لا بوجود ملف
- مرحلة «شحنات لمحة» لا تكتمل لمجرد وجود `lamha_shipment_imports`. تُنشأ مجموعة AWB المتوقعة من `audit_shipments` لكل مراجعة معتمدة وموثقة في الشهر (`weight_kg > 0` مع استبعاد صف رسم COD)، وتُقارن بكل AWB محفوظ في `lamha_shipments` للفترة نفسها.
- إذا اعتُمدت مراجعة ناقل جديدة بعد رفع ملف لمحة سابق، تعود المرحلة تلقائيًا إلى «تحتاج مراجعة» وتعرض عدد الشحنات المتبقية؛ لا يمكن إقفال الشهر حتى يصبح المتبقي صفرًا.
- زر أرقام الشحنات يصدّر **المتبقي فقط** بعد استبعاد أرقام الشحنات الموجودة في ملفات لمحة المرفوعة، مع إبقاء العدد الكلي والمستورد والمتبقي ظاهرًا للمحاسب. اختلاف ترتيب الرفع أو تعدد ملفات لمحة لا يغيّر نتيجة المطابقة.
- فشل قراءة `audit_shipments` أو `lamha_shipments` خطأ مصدر صريح يمنع الإقفال؛ ممنوع تحويله إلى اكتمال أو قائمة فارغة.

## تحديث 2026-08-06 — منع اختفاء الناقل المتعاقد من الإقفال
- كل ناقل يملك عقدًا يتقاطع مع الشهر المختار يدخل قائمة اكتمال الفواتير والتحصيل حتى لو لم يصل منه أي ملف؛ غياب جدول الاستلام يظهر «الجدول غير محدد» ويمنع الإقفال بدل إسقاط الناقل بصمت.
- الجدول الأسبوعي أو نصف الشهري أو الشهري يحتاج يومًا صالحًا. القيمة الفارغة لا تعني الأحد ولا «غير مطلوب»؛ تظهر كموعد غير مكتمل وتمنع الإقفال.
- العقود غير السارية في الشهر المختار لا تُنشئ متطلبًا لذلك الشهر، بينما وجود ملف فعلي أو جدول نشط يبقي الناقل ظاهرًا للتتبع.
- الناقل ذو `audit_with_cod` لا يظهر في اختيار رفع التحصيل اليدوي؛ نقص تحصيله يُعالج برفع واعتماد ملفه الموحّد من مرحلة الفواتير. الواجهة توجّه إلى صفحة الجداول عند وجود ناقل غير مصنف.

## تحديث 2026-08-06 — وضوح تسليم لمحة واستعادة ملفات الأوزان
- مرحلة الأوزان تعرض أيضًا زرًا مباشرًا لتنزيل قائمة أرقام الشحنات المطلوبة للبحث الجماعي في لمحة؛ يبقى الزر مكررًا في مرحلة استيراد شحنات لمحة حتى لا يعتمد المحاسب على معرفة موضع واحد.
- كل تصدير أوزان جديد يُحفظ في bucket خاص باسم `weight-billing` لتتيح خانة السجل إعادة تنزيل الملف نفسه بلا إنشاء تصدير جديد أو تغيير حالة المراجعات.
- التصدير التاريخي الذي سبق تفعيل المخزن يمكن استعادته من مراجعاته فقط إذا تطابق عدد الصفوف المعاد بناؤها تمامًا مع `weight_billing_exports.row_count`؛ عند الاختلاف يتوقف التنزيل برسالة صريحة بدل تقديم ملف متغير على أنه الأصل.

## تحديث 2026-08-06 — اعتماد J&T للدفع المختلط NLCard Cash
- تأكد إداريًا أن `NLCard Cash` يعني دفعًا مختلطًا، وأن `COD service charge` يساوي `posFeePct` مضروبًا في جزء البطاقة فقط. عقد J&T وحده يحمل `splitPosFeeConfirmed=true`؛ ممنوع تعميم الاستثناء على ناقل أو عقد آخر.
- عند وجود العلم، يستنتج المحرك مبلغ البطاقة من `COD service charge / posFeePct`، ويشترط أن يكون موجبًا ولا يتجاوز إجمالي COD. يُحفظ مبلغ البطاقة والنقد المستنتجان مع `posAmountSource='derived_from_confirmed_split_fee'` وإثبات `splitPaymentVerification` في تفاصيل الشحنة.
- إذا غاب العلم، أو كانت النسبة/القيمة غير صالحة، يبقى الدفع المختلط `unverifiable` ويمنع الاعتماد وفق حاجز التدقيق العقدي. لا يُصنَّف المبلغ كاملًا كاش أو شبكة.

## تحديث 2026-08-06 — جاهزية تدقيق النصف الثاني وملف أوزان لمحة
- الفترة التشغيلية تُستنتج من تواريخ شحنات ورقة التفاصيل، لا من تاريخ رفع الملف ولا من اسم الشهر الذي يختاره المستخدم. ملف يُرفع في يوليو ويحمل شحنات يونيو يبقى ضمن دورة يونيو.
- تصدير الأوزان يقبل فقط مراجعة معتمدة بإثبات عقدي حديث: `version >= 3` و`valid=true` واسم ملف المصدر وقائمة العقود المستخدمة. المراجعات التاريخية المستبعدة تظهر للمحاسب بسبب واضح ولا تُرحّل بصمت.
- ملف لمحة النهائي يتكون حصراً من عمودَي **رقم الشحنة** و**الوزن الجديد**. الوزن هو الوزن الكلي الذي قررته شركة الشحن؛ لمحة هي التي تقارنه بالوزن المصرح والمدفوع سابقاً حسب عقد المتجر.
- حالة ملف الوارد تُشتق من الإجراء الحقيقي (`audit_id` أو `processed_at`) لا من قيمة `status='processed'` التي كان الاستقبال يكتبها بمجرد حفظ المرفق.
- ملفات J&T الشهرية تتجه لمسار تدقيق الفاتورة، وملفات `WestBr`/`COD Client Receipt Statement` تتجه لمسار تحصيل COD؛ لا تعرض الواجهة المسار الخاطئ عندما يمكن تحديد النوع بأمان.
- تقرير الجاهزية المرجعي لحالة العقود والملفات عند بداية النصف الثاني موجود في `docs/carrier-audit-readiness-h2-2026.md`.
- قبل استيراد شحنات لمحة، يوفّر مركز دورة المحاسب ملفًا مستقلًا بعمود **رقم الشحنة** فقط من كل مراجعات الشهر المعتمدة والموثقة. يستخدمه المحاسب للبحث الجماعي في لمحة ثم يرفع `Admin Order Export` الناتج؛ تنزيل القائمة للقراءة ولا يغيّر حالة مراجعة أو تصدير أوزان.
- سجل مرحلة الأوزان يتيح **إعادة تنزيل الملف المخزّن نفسه** من `weight-billing` عبر سجل `weight_billing_exports`. إعادة التنزيل لا تنشئ سجل تصدير جديدًا ولا تعيد الشحنات إلى قائمة المعلّق.
- تعرض مرحلة الأوزان زرين مستقلين بوضوح: **ملف الأوزان** بعمودَي رقم الشحنة والوزن الجديد لرفعه إلى لمحة، و**ملف أرقام الشحنات** بعمود رقم الشحنة فقط للبحث الجماعي ثم جلب `Admin Order Export`. عند وجود تصدير سابق يظهر زر مباشر لإعادة تنزيل آخر ملف، إضافة إلى زر كل سجل تاريخي.

## تحديث 2026-08-05 — شفافية جمهور حملات التحصيل
- زر الحملة في `/customer-money` يعرض عدد نتائج الفلتر ويفتحها كلها في نافذة المراجعة؛ لا يسقط العميل بلا هاتف قبل النافذة.
- نافذة واتساب تعرض معادلة ثابتة: **نتيجة الفلتر = جاهز للإرسال + مستبعد تلقائياً**، مع أعداد أسباب الاستبعاد (هاتف ناقص/مكرر، محظور أو بلا واتساب، متابعة فريق هاتف، رقم ضعيف، حجز مالي أو حملة مستثناة).
- قوائم حماية الإرسال تُحمّل معاً قبل تفعيل زر الإرسال. أثناء الفحص أو عند فشله يبقى الإرسال معطلاً؛ ممنوع السماح المؤقت قبل وصول قوائم الحظر والجودة.
- «أُرسل له خلال 7 أيام» تحذير منفصل ولا يُعد استبعاداً تلقائياً حتى يضغط المستخدم «استبعادهم»؛ لا تخلطه مع عدد الحماية.

## تحديث 2026-08-05 — دورة تشغيل المحاسب الشهرية
- `/accounting-cycle` هو مركز العمل الموحد للمسار: مراجعات الناقلين ← تصدير الأوزان ← شحنات لمحة ← كشف الحساب ودليل المتاجر ← تحصيلات الناقلين ← تحصيل لمحة ← الإقفال. تبقى المسارات القديمة متاحة للتفاصيل ولا تُحذف.
- حالة كل مرحلة تُشتق من السجلات التشغيلية الفعلية، وليست checkbox أو `localStorage`: المراجعات من `audits`، الأوزان من حالة المراجعات و`weight_billing_exports`، مصادر لمحة من snapshots، والتحصيل من `cod_settlement`.
- تفاصيل كل مرحلة تعرض «سجل ملفات هذه المرحلة» للشهر المختار (الاسم، المصدر، عدد الصفوف، المبلغ، الحالة والوقت). لا يكفي عرض آخر ملف أو علامة مكتمل؛ الهدف أن يعرف المحاسب ما رُفع وما نُزّل دون الرجوع لصفحة أخرى.
- سجل الشهر يُقرأ كاملًا بصفحات بيانات متتابعة ولا يُقص إلى 12 أو 200 سجل. ملفات شحنات لمحة وكل أحداث الدورة وتصديرات الأوزان المرتبطة بالمراجعات تدخل التاريخ الكامل، مع منع تكرار سجل الحدث وصف المصدر نفسه.
- أي فشل في قراءة مصدر يظهر كتنبيه باسم المصدر، يحوّل مرحلته إلى «تحتاج مراجعة» ويمنع الإقفال؛ ممنوع تحويل خطأ Supabase إلى قائمة فارغة أو «لم يبدأ». محاولات الرفع/التصدير الفاشلة تُسجل بـ`status='error'` وتظهر في تاريخ المرحلة، وتبقى أحدث محاولة فاشلة بحاجة لمراجعة حتى نجاح محاولة لاحقة.
- فتح شهر جديد يحدد أول مرحلة تحتاج عملًا تلقائيًا، والنجاح ينتقل للإجراء التالي؛ التحديث اليدوي والمحاولة الفاشلة لا ينقلان المحاسب بعيدًا عن المرحلة التي يراجعها.
- استيراد شحنات لمحة يعتمد أسماء الأعمدة ومرادفاتها لا ترتيبها، يعيد حساب `!ref` لكل ورقة، يقرأ كل التبويبات، ويحفظ الصف الخام للتتبع. أي صف غير مفهوم يوقف الاستيراد برسالة واضحة؛ ممنوع إسقاط صفوف بصمت.
- ملف شحنات لمحة يُحفظ كـsnapshot مستقل في `lamha_shipment_imports` و`lamha_shipments`، مع بصمة SHA-256 تمنع رفع الملف نفسه مرتين وتسجيل المكرر داخل الملف والصفوف الواقعة خارج الشهر المختار.
- كشف حساب لمحة ودليل المتاجر يعتمدان `accounting_cycle_events.period` أولًا، لأن ملف يونيو قد يُرفع في أغسطس؛ `uploaded_at` التاريخي يبقى fallback للملفات السابقة على إنشاء المركز.
- تحصيل لمحة في المركز يقبل الملف المجمّع مرة واحدة ويستخدم `parseConsolidatedExpected`/`saveConsolidatedExpected` لتوزيع الصفوف على الناقلين حسب عمود شركة الشحن؛ لا يُجبر المحاسب على اختيار ناقل ورفع الملف نفسه عدة مرات. تحصيلات الناقلين ولمحة المتأخرة تعتمد حدث الدورة الناجح لتحديد الشهر، لا تاريخ الرفع وحده.
- تحصيلات شركات الشحن تُراجع لكل ناقل ظهر في مراجعات الشهر المعتمدة، ولا يكفي أول ملف لإكمال المرحلة. `audit_with_cod` مكتمل تلقائيًا، و`audit_only` لا يحتاج ملفًا، بينما `audit_and_cod_separate` و`cod_only` يتطلبان حدث رفع ناجح خاصًا بالناقل؛ النوع غير المحدد أو القارئ غير المهيأ يظهر كحالة إعداد ويمنع الإقفال.
- تصدير الأوزان من مركز الدورة يمرر الفترة صراحةً؛ لا يجوز أن يسحب مراجعات معلقة من شهر آخر. نقاط الاستدعاء القديمة التي لا تمرر فترة تحتفظ بسلوك «كل المعلّق» للتوافق.
- `audits.period` التاريخي مخزن بالعربية مثل `يونيو 2026` بينما محدد الدورة يستخدم `2026-06`؛ القراءة والتصدير يمران عبر `accountingPeriodAliases`/`auditPeriodMatches` ولا يقارنان النصين مباشرة.
- الشهر المختار في رأس الدورة يُمرّر إلى `UploadWizard` ويعيد تركيبه عند تغييره؛ ممنوع أن تعرض الدورة يونيو بينما نموذج المراجعة يعمل على أغسطس.
- تفاصيل المرحلة تُرسم مرة واحدة فقط حسب `matchMedia` (جوال أو حاسب). لا تكتفِ بإخفاء نسختين عبر CSS لأن ذلك يكرر حقول الرفع والأزرار والآثار الجانبية.
- إقفال الدورة يتطلب `system.period_close` واكتمال المراحل الست من بياناتها الحقيقية. هو إثبات اكتمال تشغيلي ولا ينشئ قيدًا محاسبيًا.

## تحديث 2026-08-05 — إثبات التدقيق العقدي لملفات الناقلين
- رفع فواتير شركات الشحن يمر من `UploadWizard` فقط؛ أُلغي مسار الرفع المختصر من دفتر الناقل لأنه كان يتجاوز اختيار ورقة التفاصيل والحقول الإلزامية وملخص الشركة وبصمة المصدر.
- الاعتماد والربط المالي يتطلبان `col_map.__control.version >= 3` مع `valid=true` واسم ملف وعقد محدد. المراجعات التاريخية بلا هذا الإثبات تبقى للقراءة فقط وتظهر «قديمة غير موثقة» ولا تدخل تصدير الأوزان أو الربط.
- أي رسم لا يملك معادلة عقدية قابلة للحساب (`inboundPassthrough` أو `codFeePassthrough` أو دفع بطاقة/نقد بلا مبلغ البطاقة أو وزن زائد بلا وزن) يصبح `unverifiable` ويوقف الاعتماد؛ لا تُساوِ قيمة Excel بالقيمة المتوقعة.
- أي عمود مالي غير معيّن ويحمل قيمة غير صفرية يوقف المراجعة. أعمدة القالب الصفرية والقياسات مثل `Chargeable weight` لا تُعامل كرسوم.
- فحص `audit_awb_ledger` للـAWB السابق يعمل بدفعات محدودة (180 رقمًا) لأن تمرير آلاف الأرقام في استعلام `.in()` واحد يتجاوز حد URL في PostgREST ويمنع تدقيق الملفات الشهرية الكبيرة.
- عدد شحنات ملخص الناقل يُقارن بعدد صفوف المصدر، لا بعدد الصفوف المسعّرة؛ الصفوف المستبعدة مثل مرتجعات J&T الصفرية تُعرض كعدد مستقل. إجماليات التفاصيل قبل الضريبة والضريبة والشامل تبقى مطابقة مستقلة مع ورقة الملخص.
- سجل تاريخ العقد يتتبع كل أعلام التسعير المؤثرة، ومنها `priceFromContract` و`pricingKey` وأعلام pass-through ورسوم POS والوزن الزائد، وليس الأسعار الأساسية فقط.

## تحديث 2026-08-03 — عقد كثافة الصفحات والجداول
- `src/workspace-layout.css` هو طبقة التخطيط النهائية المشتركة، ويُستورد بعد `design-v5.css` وقبل `mobile-scroll.css`. لا تُكرر قواعد التبويبات أو الجداول أو كثافة الهيدر داخل صفحة منفردة.
- على الجوال لا يتكرر عنوان المركز داخل `WorkspaceTabs` لأن شريط الصفحة يعرضه أصلًا؛ تبقى التبويبات مباشرة، ويُختصر `WorkspaceContext` إلى الغرض والنتيجة بسطرين دون الفقرة التفسيرية الطويلة.
- `PageHeader` يبقى تعريفًا موجزًا للمهمة؛ إجراءاته تستخدم شبكة واضحة على الجوال، ولا يجوز أن يدفع الهيدر أول بيانات الصفحة خارج أول شاشة بسبب أزرار متراصة عموديًا بلا داعٍ.
- إذا تجاوزت إجراءات `PageHeader` أربعة أفعال، تبقى الإجراءات اليومية ظاهرة وتُنقل الأفعال الثانوية إلى `page-action-menu` بمسميات نتيجة واضحة؛ لا تُعرض ثمانية أزرار متساوية الأهمية في رأس واحد.
- للجداول ثلاث استراتيجيات فقط على الجوال: `m-cards` لكل الحقول المهمة، `m-compact` لعمودَي القرار مع فتح التفاصيل، أو تمرير أفقي محصور داخل سطح الجدول. لا تُخفِ عمودًا بلا مسار واضح للوصول إلى قيمته.
- نهاية تمرير Safari تبقى ملك `mobile-scroll.css` حصراً؛ أي تحسين بصري جديد لا يضيف padding أو pseudo-element لنهاية الصفحة.

## تحديث 2026-08-03 — حاجز أمان إطلاق الموظفين
- `profiles.role` و`profiles.permissions` حقول تفويض محمية بخادم قاعدة البيانات؛ لا يكفي قيد الصف الذاتي، وأي موظف غير مدير يُمنع من تعديلهما عبر trigger حتى لو استدعى PostgREST مباشرة.
- المساعد المالي يتطلب JWT لمستخدم فعلي وصلاحية `system.ai_assistant`، ويشغّل تقارير خادم ثابتة فقط؛ ممنوع إعادة SQL حر أو نموذج يختاره المتصفح أو نتائج خدمة كاملة إلى مزود خارجي.
- ملفات Storage الخاصة تتبع صلاحية الصفحة والفعل (قراءة/رفع/حذف) لكل bucket؛ تسجيل الدخول وحده لا يمنح أي وصول للملفات.
- مفاتيح مزودي الذكاء الاصطناعي تبقى في أسرار الخادم ولا تُحفظ في `localStorage`. تسجيل الخروج يمسح مسودات المراجعة والاستيراد والإعدادات الحساسة من المتصفح.
- النسخة الاحتياطية من الإعدادات تتطلب `reports.export` بشكل مستقل عن `system.view_settings`.
- سياسات الجداول التشغيلية القديمة ذات `USING/WITH CHECK (true)` استُبدلت بصلاحيات فعلية للمراجعات وCRM والحملات والمهام والتجهيز والتصدير؛ لا تُضف سياسة كتابة عامة جديدة لمجرد أن المستخدم مسجل الدخول.
- التقارير وواجهات SQL العامة تعمل كـ`security_invoker` وتحترم RLS للموظف؛ يمنع إبقاء تقرير بيانات عملاء كـ`SECURITY DEFINER` بلا فحص صريح للصلاحية.
- الـviews المشتقة تملك حارس صلاحية مستقلًا حسب المجال؛ خصوصًا `customer_ar` و`customer_collectible_lines` فلا تكفي صلاحية CRM أو دليل المتاجر لرؤية الذمم المالية.
- `vite.config.js` يستخدم `base: '/'` لأن مسارات SPA تاريخية؛ إعادته إلى `./` تكسر فتح/تحديث `/employees` و`/settings/*` مباشرة وتحاول تحميل الحزم من مجلد المسار.
- الملف `.claude/settings.local.json` محلي وممنوع تتبعه أو نسخه إلى Git؛ تدوير أي مفتاح ظهر تاريخياً وحذف التاريخ العام خطوة تشغيلية إلزامية قبل اعتبار التسريب مغلقاً نهائياً.

## تحديث 2026-08-03 — الرئيسية التشغيلية حسب القرار
- ترتيب أعلى `/overview` ثابت: **يحتاج تدخلك اليوم ← المتاح الفعلي ← تفاصيل المصادر والبنوك**؛ لا تسبق الجداول أو بطاقات الأداء أولويات المدير.
- «المتاح الفعلي» يعيد استخدام معادلة البيانات القائمة فقط: أرصدة البنوك + ذمم العملاء − التزامات الناقلين − حجز الضريبة. هو ملخص إداري ولا ينشئ قيدًا محاسبيًا.
- تفاصيل البنوك والمطابقة مع زوهو تبقى داخل عناصر كشف قابلة للفتح؛ الملخص يعرض عدد الحسابات ومصدر الرصيد وآخر تحديث من دون إجبار المستخدم على المرور بصفوف طويلة.
- الجوال يعرض أولويتين في الملخص، ويخفي شريط الأوامر المكرر، ويثبت الوصول السريع إلى: السيولة · التحصيل · البنوك. لا يجوز إعادة التمرير الأفقي إلى هذا الشريط.
- عقد التصميم الدلالي موجود في `docs/overview-dashboard-contract.md`؛ الصور المعتمدة مراجع تركيبية فقط، وكل رقم ونص في المنتج يبقى عنصرًا برمجيًا مرتبطًا بالمصدر الحقيقي.

## تحديث 2026-08-02 — جانبية هرمية من ثلاثة مستويات
- بنية الجانبية أصبحت: **قسم رئيسي ← مجموعة عمل فرعية ← صفحة**. تعريف الأقسام والمجموعات وربط الصفحات بها موجود حصراً في `src/lib/navigation.js` عبر `NAV_SECTIONS` و`NAV_GROUPS` و`NAV_ITEM_IA`.
- مجموعات العمل عناوين تنظيمية ثابتة داخل القسم المفتوح، وليست أكورديونًا ثانيًا؛ لا نضيف نقرة إضافية للوصول للصفحة، ولا نكرر تبويبات الصفحة الداخلية في الجانبية.
- عند طي الجانبية للحاسب تختفي عناوين المجموعات وتبقى أيقونات الصفحات بالترتيب نفسه. المجموعات الفارغة بسبب صلاحيات الموظف لا تظهر، وأي عنصر بلا مجموعة معروفة يقع تحت «أخرى» بدل أن يختفي.
- إضافة صفحة ظاهرة جديدة تتطلب تحديد `section` و`group` و`order` في `NAV_ITEM_IA`. لا تكتب عنوان مجموعة أو ترتيبًا جديدًا داخل `App.jsx`.

## تحديث 2026-08-02 — فصل صفحات الإعدادات وتوحيد الوصول
- عناصر الإعدادات الأربعة تظهر مستقلة في الجانبية وبالترتيب: **الفريق والصلاحيات** (`/employees`، مدير فقط) · **شركات الشحن** (`/carriers`) · **العقود والأسعار** (`/contracts`) · **التكاملات والذكاء الاصطناعي** (`/settings/ai` و`/settings/data`).
- أُلغي غلاف `CenterWorkspace` الذي كان يكرر هذه الصفحات كتبويبات داخل «شركات الشحن»؛ القائمة الجانبية هي نقطة التنقل الوحيدة بينها، وكل مسار يرسم صفحته مباشرة ويحافظ على حارس الصلاحية القديم.
- لا تُدفن صفحة إدارية مستقلة كتبويب داخل كيان لا تنتمي إليه. أي إعداد جديد يأخذ مدخلًا واضحًا داخل قسم «الإعدادات» أو يندرج داخل صفحة «التكاملات والذكاء الاصطناعي» إذا كان إعداد مصدر/تكامل فعليًا.
- العدد الإداري الكامل أصبح **23 مدخلًا ظاهرًا** (يتناقص حسب صلاحيات الموظف)، بعد إضافة مداخل الإعدادات المستقلة وإزالة زر الإعدادات المكرر من تذييل الجانبية.

## تحديث 2026-08-02 — صلاحيات على مستوى الفعل لا الصفحة
- كتالوج الصلاحيات يفصل الآن بين عرض البيانات وبين الأفعال الحساسة: اتصال زوهو، استيراد البنك، تحويل المسودة، إرسال زاتكا، إعادة webhook، رفع الكشف، تعديل الملاحظة، المطابقة، الحذف والتصدير. لا تستخدم `zoho.configure` كإذن شامل لأي كتابة جديدة.
- `zoho-operations` يحدد الإذن المطلوب من `action` على الخادم؛ إخفاء الزر في الواجهة ليس حماية كافية.
- تبويبات `/money` تحمل صلاحيات مستقلة (`cod.view` / `payments.view` / `bank.view`) والمسار القديم لكل تبويب محمي بإذنه الخاص؛ امتلاك أحدها لا يكشف التبويبات الأخرى.
- تقارير التشغيل والمالية والبنوك منفصلة عن `reports.export`. العرض لا يمنح إنشاء ملف أو الوصول لسجل التنزيلات.
- RLS/Triggers للبنوك تمنع صاحب «تعديل ملاحظة» من تعديل مبلغ أو حذف حركة، وتمنع مطابقة حركة مع قيد دون `bank.reconcile` أو `payments.allocate`.
- إضافة مفاتيح جديدة للكتالوج لا تمنحها لأي موظف قائم تلقائياً؛ المدير يراجعها ويمنحها صراحةً من `/employees`.

## تحديث 2026-08-02 — نهاية تمرير موحّدة لكل صفحات الجوال
- كل `PageSlot` قابل للتمرير يرسم عنصر DOM حقيقيًا باسم `.page-slot-scroll-end` بعد محتوى الصفحة؛ هو مصدر المساحة الآمنة فوق `.bottom-nav` لكل الصفحات.
- على الجوال تعمل `.page-slot` بتدفق كتلي عادي `display:block` مع `overflow-y:auto`؛ لا تعِدها إلى flex لأن Safari قد يثبت `scrollHeight` قبل اكتمال الصفحات الطويلة الديناميكية ويقص آخر بطاقة.
- `src/mobile-scroll.css` مستورد بعد طبقات الثيم، ويلغي فاصل `::after` والحشو السفلي التاريخيين على الجوال. ممنوع إضافة فواصل أو حشو سفلي خاص بصفحة منفردة لمعالجة Safari.
- الفاصل الحقيقي جزء مباشر من حاوية التمرير المطلقة وفي التدفق العادي، لذلك يدخل في `scrollHeight` على Safari؛ قيمة الأمان تشمل شريط التنقل و`safe-area-inset-bottom`.
- غلاف محتوى الصفحة المباشر داخل `.page-slot` لا يأخذ تمريرًا عموديًا مستقلاً ولا ارتفاعًا يقص المحتوى؛ يبقى `PageSlot` مالك التمرير الوحيد حتى يصل فاصل النهاية بعد آخر عنصر فعلي.

## تحديث 2026-08-02 — فلتر الأرصدة الافتتاحية في التحصيل
- صفحة تحصيل العملاء تملك فلترًا مستقلاً باسم «رصيد افتتاحي» يعرض فقط العملاء الذين ما زال `opening_balance` لديهم أكبر من 0.50 ر.س.
- الفلتر يدخل في نفس قائمة النتائج المستخدمة للعدد، الإجمالي، التصدير وحملة واتساب؛ لا يجوز تنفيذ إجراء جماعي على عميل أخفاه الفلتر.

## تحديث 2026-08-02 — أعمار مديونيات العملاء
- أعمار الديون في `customer_money_dashboard()` تُحسب من `zoho_invoices.due_date` للفواتير المفتوحة ذات الرصيد فقط، مع الرجوع إلى تاريخ الفاتورة إن غاب تاريخ الاستحقاق.
- `customer_ar.opening_due` المتبقي يُعامل كفاتورة افتتاحية بتاريخ 2026-01-10؛ يدخل في «متأخر» وشريحة `+90` ما دام قائمًا، ويختفي فور سداد فاتورة Zoho الخاصة `customer_ob_invoice`.
- إجمالي المستحق يشمل الفواتير المفتوحة والرصيد الافتتاحي القائم فقط؛ لا تعرض رصيدًا افتتاحيًا مسددًا أو ذا `balance=0`.
- `zoho-sync` لا يثق بملخص قائمة جهات الاتصال وحده عند وجود `opening_due > 0.5`: يجلب `GET /contacts/{id}` ويصالح حقول الرصيد من بطاقة العميل التفصيلية. هذا يمنع بقاء رصيد افتتاحي مسدد كدين وهمي، مع إبقاء الأرصدة الافتتاحية الحقيقية.
- `zoho-webhook` يقرأ `invoice.contact.customer_balance` و`unused_customer_credits` من حدث الفاتورة ويحدّث `zoho_contacts` فورًا؛ حدث سداد الرصيد الافتتاحي لا ينتظر مصالحة الساعتين.

> **اقرأ هذا الملف بالكامل قبل أي تعديل في الكود.** المنظومة وصلت لحالة عمل مستقرة لعدة تدفّقات — أي تغيير يكسر أحد هذه التدفّقات غير مقبول.
>
> هذا ملف حيّ. كل ما تستقرّ ميزة جديدة، تضاف هنا. كل تعديل على هذا الملف يجب أن يُذكر في commit message.

## تحديث 2026-08-02 — وكيل زاتكا التلقائي
- وكيل زاتكا يعمل تلقائيًا يوميًا الساعة 11:45 م بتوقيت السعودية (`45 20 * * *` UTC) ولا ينتظر موافقة بشرية في التشغيل المجدول؛ الموافقة والصلاحية تخصان التشغيل اليدوي فقط.
- الأرصدة الافتتاحية مستبعدة داخليًا ولا تظهر في المعاينة أو عداد المستبعدات لأنها ليست فواتير تشغيلية قابلة للإرسال.
- المعاينة والتنفيذ يفحصان حالة الفاتورة حيًا من Zoho قبل اعتبارها جاهزة، ويحدثان المرآة المحلية عند اكتشاف `pushed` لمنع التكرار.

## تحديث 2026-08-01 — بدء تنفيذ هندسة التنقل المعتمدة (المرحلة الأولى)
- نقطة الحقيقة لهندسة القائمة الظاهرة أصبحت `src/lib/navigation.js`: ستة أقسام تشغيلية + الرئيسية و«مهام وقرارات اليوم»، و21 مدخلاً ظاهراً بدل 35. `App.jsx` يحتفظ بسجل المسارات والصلاحيات للحماية والتوافق فقط.
- إخفاء المدخل من القائمة **لا يحذف مساره ولا صلاحياته**؛ العناصر القديمة تحمل `navHidden` وتظل خلف `PATH_PERM` حتى تُدمج صفحاتها نهائياً في المراكز.
- الأقسام المعتمدة: الشحن والفوترة · العملاء والتحصيل · المبيعات والتواصل · المالية وزوهو · التقارير والرقابة · الإعدادات. لا تضف مسمى أو ترتيباً في `App.jsx`؛ أضفه في `NAV_ITEM_IA`.
- `/zoho-data` صار «زوهو والحسابات»: مجموعات واضحة للعميل/المورد/القيود/البنوك/المطابقة، وتنبيه أعلى اللوحة عند وجود بنوك غير مربوطة مع زر «أكمل ربط الحسابات».
- مسميات الأزرار في النطاق الجديد تصف النتيجة: «إعادة تفويض صلاحيات زوهو» · «تحديث أرصدة البنوك» · «ربط الحساب ببنك داخلي» · «حفظ تصنيف الحساب».
- مودال ربط حساب زوهو بالبنك يستخدم `<select>` من `financial_dashboard.internal_banks`، وليس كتابة يدوية أو `datalist`؛ الهدف منع اختلاف التهجئة وإظهار الخيارات الفعلية بوضوح على الجوال.
- صفوف حسابات زوهو تقرأ `financial.links` وتعرض حالة الربط ووجهته («مربوط مع …»)؛ الحساب المرتبط يظهر «تعديل الربط» لا زر «ربط» جديد. المقارنة بين المعرّفات تتم بـ`String()` لحماية اختلاف text/number.
- قائمة البنوك في مودال الربط تستبعد أي `internal_bank_name` مستخدم في ربط `link_kind='bank'` آخر، وتُبقي البنك الحالي عند تعديل الربط. الحماية النهائية في قاعدة البيانات عبر `zoho_financial_links_bank_unique_idx`؛ فلترة الواجهة للوضوح وليست بديلًا عن القيد.

## تحديث 2026-08-01 — دمج المراكز (المرحلة الثانية؛ فصل الإعدادات لاحقًا في 2026-08-02)
- `CenterWorkspace.jsx` هو الغلاف العام لدمج صفحات قائمة كاملة في مساحة عمل واحدة مع `WorkspaceTabs`، مع إبقاء المسار القديم لكل تبويب؛ لا تنشئ PageSlot مستقلاً لصفحة دُمجت في مركز.
- مركز `/reports` يجمع: التقارير · التقرير الشهري · مزامنة المصادر · سلامة البيانات · سجل النظام · الملفات المصدّرة. المسارات القديمة تبقى وتختار تبويبها.
- كان مركز `/carriers` يجمع شركات الشحن والعقود والفريق؛ أُلغي هذا الدمج بقرار 2026-08-02 وأصبحت المسارات الثلاثة مداخل جانبية مستقلة كما هو موثق أعلى الملف.
- كانت القائمة في هذه المرحلة 19 مدخلاً؛ العدد الحالي موثّق في تحديث 2026-08-02 أعلاه. سجل المسارات والصلاحيات لم يُحذف، و`navHidden` لا يعني إسقاط الحارس.
- في الصفحات المدمجة مُنع الزر العام «تحديث/فتح/تم»: يجب أن يذكر الهدف مثل «تحديث سجل النظام» و«عرض موضع المشكلة» و«إغلاق ملخص الدمج».

## تحديث 2026-08-01 — فواتير زوهو PDF على الجوال
- صفحة `/zoho-data` تتيح تحديد عدة فواتير عملاء وتحميلها مرتبة في **ملف PDF واحد**؛ الجلب يبقى عبر `zoho-sync/download_document` والدمج محلي داخل المتصفح عبر `pdf-lib`، بلا رفع المستندات لطرف ثالث.
- التحديد يخص أول 800 فاتورة ظاهرة ويحترم البحث والفلاتر والترتيب، ويُفرّغ ما خرج من العرض لمنع تنزيل مستندات مخفية بالخطأ.
- على الجوال جدول زوهو يتحول لبطاقات ويتدفق مع تمرير `PageSlot` الواحد؛ لا تعِد `overflow:hidden` إلى `.zoho-records-card` داخل media الجوال لأنه يعيد قص البطاقات في Safari.

---

## 1. الأشياء التي تعمل (ممنوع كسرها)

### 1.1 تدفّق DeliverNow End-to-End ✅
- إيميل من `@delivernow.net` يصل عبر InboxDone → webhook-intake v10
- يظهر في `/webhook` بشركة معرّفة + شارة XLS
- زر **"حفظ كمراجعة"** → ينقل لـ `/upload` → معالجة تلقائية → خطوة 3
- بانر أخضر "جاهزة للاعتماد بالهلله" → زر **"اعتماد المراجعة"**
- بعد الاعتماد:
  - قيد INV في `carrier_operations` (DR = 1,619.20 ر.س مع الضريبة)
  - 19 شحنة COD في `cod_settlement` direction='in' (مُستلَم)
  - `webhook_events.audit_id` يُحدَّث + `status='processed'`
- البطاقة في `/hub` تعكس الرصيد الجديد
- البروفايل في `/carrier?id=delivernow` يعرض كل شيء

**الثوابت الواجب احترامها:**
- `carriers.delivernow.file_signature.file_kind = 'audit_with_cod'`
- `carriers.delivernow.contracts[0]`: سعر ثابت 11 ر.س السعودية، ضريبة 15%
- `carriers.delivernow.file_signature.email_from = ['@delivernow.net']`
- النمط في `engine/audit.js` لـ `deliveryCharges` يلتقط `'Shipping Service / المجموع الصافي'`

### 1.2 بوابة الاعتماد (Penny-Perfect Gate) ✅
- `evaluateApprovalGate(audit)` في `src/lib/coreService.js`
- **يمنع** الاعتماد إذا:
  - `mismatch_count > 0` (أي شحنة بفرق فردي)
  - `|totalBilled - totalExpected| > 0.50` ر.س
- **يحذّر** (لا يمنع) إذا:
  - `|totalTax - totalBilled × 0.15| > 1.00` ر.س
- الثوابت المُصدَّرة:
  - `APPROVAL_DRIFT_TOLERANCE_PRE_TAX = 0.50`
  - `APPROVAL_DRIFT_TOLERANCE_TAX = 1.00`

### 1.3b ربط المراجعة بقيد RV الموجود بدل INV مكرر ✅ (2026-06-11)
- **الفخّ**: `approveAudit` كان ينشر INV دائماً. للناقلين ذوي الكشوف (أرامكس/سمسا) قيد RV يأتي من الكشف بنفس المبلغ → **ازدواج** (INV + RV = ضعف المبلغ). كشفه المستخدم عند «ربط مراجعة» (المودال يخفي المراجعة لأنها مربوطة بـINV الخاص بها).
- **الإصلاح**: قبل نشر INV، `approveAudit` يبحث عن قيد RV **غير مدقّق** بنفس الإجمالي (ضمن 0.5 ر.س) لنفس الناقل. إن وُجد **واحد بالضبط** → يربط المراجعة به (يضع `audit_id` + status='audited') بدل نشر INV. صفر أو أكثر من واحد → ينشر INV عادي (J&T/iMile/ديلفرناو بلا كشوف فلا RV → ينشرون INV دائماً).
- **القاعدة**: لا تفترض أن كل ناقل `audit_and_cod_separate` له كشف — J&T له INV شرعي (كشوف=0). المميِّز الصحيح = **وجود RV مطابق بالمبلغ** لا file_kind.

### 1.3 التسجيل التلقائي في الكشف المالي ✅
- `approveAudit` يكتب صف **INV** واحد في `carrier_operations` بإجمالي (الفاتورة + الضريبة)
- Idempotent عبر unique partial index `(audit_id) WHERE doc_type='INV'`
- `rejectAudit` / `reopenAudit` يحذفان القيد + يستدعيان `clearAuditCodOut`
- `saveSettlementUpload(direction='in')` يكتب صف **COD** (CR) إجمالي الدفعة، idempotent عبر `reference_no`
- `deleteSettlementUpload` يحذف القيد المقابل

### 1.4 استخراج COD التلقائي ✅
- **القاعدة الذهبية**: أي ملف من شركة شحن = `direction='in'` (مُستلَم). ممنوع `'out'` من أي audit.
- في `coreService.approveAudit`:
  - `file_kind === 'audit_with_cod'` → يستخرج 19 شحنة (أو أيّاً كان) كـ 'in'
  - أي قيمة أخرى → **لا يفعل شيئاً** في `cod_settlement`
- `syncAuditCodOut({ direction: 'in' })` في `codSettlementService.js`
- Source marker: `source_file = 'audit:<auditId>'`، upload_id = `audit_in_<auditId>`
- Idempotent: يمسح batches القديمة في كلا الاتجاهين قبل الإدراج

### 1.5 دورة حياة Webhook → Audit ✅
- `importToAudit` في `WebhookEvents.jsx`: يحمّل الـ blob من storage → base64 → `sessionStorage.setItem('webhookImport', payload)` → `navigate('/upload')`
- `UploadWizard` يستمع لـ `location.pathname` (ليس useEffect فارغ deps!) → يفحص sessionStorage عند كل وصول لـ `/upload`
- `CONSUMED_WEBHOOK_IMPORTS` Set على مستوى الـ module يمنع التكرار في StrictMode dev
- على نجاح الاعتماد: `markEventProcessed(eventId, auditId)` يربط الـ webhook event بالمراجعة
- الشارة في `/webhook`: **"✓ تمت مراجعتها"** + زر **"فتح المراجعة"**

### 1.5b دورة حياة Webhook → COD Remittance ✅ (J&T-style)
- للشركات تاجد `file_kind='audit_and_cod_separate'` أو `'cod_only'`، يظهر زر **"💰 حفظ كتحصيل"** بجانب أو بدل "حفظ كمراجعة"
- `importToCod` في `WebhookEvents.jsx`: stash payload في `sessionStorage.webhookCodImport` → `navigate('/cod-settlements')`
- `CodSettlements` يفحص `location.pathname === '/cod-settlements'` ويلتقط الـ payload (مع `CONSUMED_COD_IMPORTS` set)
- يفتح `UploadModal` تلقائياً بـ `direction='in'` + `preloadedFile` + `sourceEventId`
- `UploadModal` يستدعي `handleFiles([preloadedFile])` على mount → preview جاهز
- بعد save بنجاح: `markEventProcessed(eventId, auditId=null, userId)` — `processed_at` تُحدَّث
- الشارة في `/webhook`: **"تحصيل مُستلَم"** (gold) + زر **"تم استيراده كتحصيل"** → ينقل لـ `/cod-settlements`
- إشارة الفعل: `e.processed_at != null && !e.audit_id` → COD imported

### 1.6 حذف Webhook Events ✅
- RLS policy `webhook_events_delete` موجودة (FOR DELETE TO authenticated USING true)
- `deleteWebhookEvent` / `deleteWebhookEvents` يستخدمان `.select('id')` بعد الحذف
  → يرفعان خطأ إذا 0 صف انحذف (RLS silent-fail protection)

### 1.7 مفاتيح Storage ASCII-آمنة ✅
- `webhook-intake` v10 — دالة `sanitizeStorageName` تحوّل أي حرف غير `[A-Za-z0-9._-]` إلى `_`
- الاسم الأصلي (مع العربية) يبقى في `webhook_events.file_name`
- المفتاح المنظّف في `webhook_events.file_path`
- لا حذف لـ blob القديم بمفتاح عربي — Supabase Storage يرفض الـ key

### 1.8 توسعة الحجم ✅
- جدول `audit_shipments` يحفظ كل شحنة بـ row مستقل (إلى 500K+ صف لكل مراجعة)
- `saveAuditToDB` يكتب الشحنات بدفعات 1000 صف
- `audits.results` JSONB يحمل فقط الـ issues (مع حد أقصى 2000)
- `loadAuditByIdFromDB` يحمّل issues فقط افتراضياً
- `loadAuditShipments(id, { from, limit, status })` للـ pagination

### 1.9 دليل المتاجر + التنبيهات + حملة التحصيل ✅ (Phase 1–5)
- `/merchants` يستقبل رفع `stores.xlsx` من المنصّة الداخلية (1,491 متجر حالياً)
- snapshot model: كل رفع يولّد `snapshot_id` جديد؛ `loadLatestMerchants` يرجع الأحدث
- `customer_merchant_links` يربط `customer_name` (من الـ receivables) بـ `store_id`:
  - زر "ربط تلقائي" في `/merchants` يستخدم Levenshtein + تطبيع عربي (alefs/ya/ta marbouta + diacritics)
  - عتبة auto-fuzzy = 0.78. تطابق دقيق على segment بعد `splitReceivableName` → confidence 1.0
  - `match_method='manual'` يُحفظ ضد إعادة كتابة auto-link لاحقة
- `customerReceivablesService.loadLatestReceivables` يدمج بيانات المتاجر تلقائياً عبر:
  `c.merchant = { storeId, storeName, phone, billingType, platformStatus, integrationType, shipmentCount, lastShipmentAt, walletBalance }`
  والفشل صامت (try/catch + console.info) — صفحة المديونيات تشتغل بدون merchants snapshot
- في `/receivables` تبويب **🚨 تنبيهات** (red accent) يصنّف كل عميل لواحد من:
  - `prepaid_with_debt` 🚨 — دفع مسبق + عليه دين = خلل تقني
  - `postpaid_overdue` ⏰ — دفع لاحق + +60 يوم = مرشّح للإيقاف
  - `inactive_with_debt` 😴 — موقوف في المنصّة + عليه دين = تحصيل قبل الإغلاق
- زر **📞 ملف حملة تحصيل** يصدّر 13 عمود (هاتف/نوع/حالة/aging/anomaly) للعرض الحالي بعد الفلاتر
- `/merchants` يعرض: top-20 بالشحنات + top-20 churned (مُعطَّل لكن شحن سابقاً، مرتّب بأحدث آخر شحنة) + walletPilesUp (محافظ راكدة)

**الثوابت:**
- table `merchants` (snapshot rows) + `customer_merchant_links` (customer_name → store_id)
- `computeMerchantInsights(merchants)` يرجع: `total/active/prepaid/postpaid/newLast30/neverShipped/dormantActive/churned/walletPilesUp/walletPilesAmount/walletTotal/topByVolume/churnedTop/walletPilesTop`
- `findMerchantForCustomer(name, merchants)` يرجع `null` تحت 0.78 (لا تخفّض هذه القيمة دون اختبار)
- الهواتف: `toPhoneString` يحوّل number → string بـ `Math.round` لحماية 12 رقم من scientific notation
- XLSX read: `raw:true` مطلوب لقراءة الهواتف الكبيرة بدقّة

### 1.10 نظام الأدوار والصلاحيات ✅ (Phase 6)
- **دوران فقط**: `admin` (مدير، يفعل كل شيء) + `accountant` (محاسب، صلاحياته في JSONB)
- العمود `profiles.permissions JSONB` يحمل المفاتيح الممنوحة `{ "audits.approve": true, ... }`
- مفاتيح الصلاحيات معرّفة في `src/lib/permissions.js` كـ `PERMISSION_CATALOG` — ~٧٠ مفتاح موزَّعة على ١٤ قسم (overview/uploads/webhook/audits/carriers/cod/receivables/collections/merchants/money/ledger/internal_exports/reconciliation/system)
- في كل مكوّن: `const { can } = useAuth(); if (!can('audits.approve')) return null;`
- في `AuthProvider`: يصدّر `can(key)`, `canAny(keys)`, `canAll(keys)`, `isAdmin`
- إدارة الصلاحيات من `/employees` — كل محاسب له زر "صلاحيات" يفتح modal مع checkboxes مجمَّعة + presets (`بدون / قراءة فقط / محاسب قراءة وكتابة / صلاحيات كاملة`)
- إجراء "حسّاس" (`sensitive: true`): يُستبعَد من preset "قراءة وكتابة" — مثل `audits.approve`, `audits.delete`, `bank.set_balance`, `system.period_close`
- **`admin` لا يخضع للـ JSONB** — يمرّ من `can()` بـ `true` دائماً
- صفحة `/employees` متاحة فقط للـ `admin` بغض النظر عن الصلاحيات
- FKs المعدَّلة لـ ON DELETE SET NULL (تسمح بحذف موظف): `audits.created_by`, `task_actions.user_id`, `tasks.assigned_to`, `tasks.created_by` — يحتفظ بالسجلات بدون اسم منشئ
- Edge function `manage-users` v2 — يقبل `permissions` في create، يستخدم `auth.admin.deleteUser` الذي يكاسكد عبر الـ profiles cascade الآن
- Nav gating في `App.jsx` — كل `NAV_ITEM` له `permKey` يفلتر العنصر للـ accountant

**ملفات نقطة الحقيقة:**
- `src/lib/permissions.js` — الكتالوج + `can()` + presets
- `src/lib/auth.jsx` — يعرض `can` في الـ context
- `src/lib/employeeService.js` — `loadEmployees`, `updateEmployeePermissions`
- `src/pages/EmployeeManager.jsx` — UI الإدارة الكامل
- `supabase/functions/manage-users` v2 — CRUD للموظفين

**القاعدة الذهبية:** أي زر يقوم بعملية مالية/حذف/اعتماد يجب أن يكون ملفوفاً بـ `can('key')`. UI الـ gate وحده ليس أمناً — RLS / edge function policies هي الحماية الفعلية.

### 1.11 تبويبات الـ hubs مرئية في القائمة الجانبية ✅ (UX 2026-05-29)
- مشكلة سابقة: شاشات الـ hubs (CustomerHub `/customer-360`، MoneyHub `/money`، CarriersWorkspace KPI) كانت تبويبات مخفية بلا مدخل في القائمة الجانبية → معلومات مدفونة.
- الحل في `App.jsx`:
  - `NAV_ITEMS` لـ `money` و `customer-hub` تحمل الآن `subTabs: [{ tabId, label, icon, legacy }]`
  - تُرسَم كصفوف فرعية (`NavSubBtn`) تحت العنصر الأب، تنقل لـ `${path}?tab=${tabId}`
  - `أداء الناقلين` (`/carrier-kpi`) أُضيف كعنصر مسطّح مستقل في قسم carriers (بدل أن يكون تبويباً مخفياً في `/hub`)
  - `subTabOf(item)` يحدّد التبويب النشط (canonical `?tab=` أو legacy path أو افتراضي = أول تبويب)
  - `activeFor` يُخفي تمييز الأب عندما يملك subTabs نشطاً (لا تمييز مزدوج)
- **القاعدة (مُعدَّلة v4 — 2026-07-02):** أي hub جديد بتبويبات يُعرّف `subTabs` على عنصره في `NAV_ITEMS` **كبيانات** (تمييز الأب + مسارات legacy) — لكنها **لا تُرسَم صفوفاً في الجانبية** (قرار المستخدم: «القائمة موجودة داخلياً وجانبياً ليش؟») — تبويبات الصفحة الداخلية هي المبدّل الوحيد.

### 1.11b بطاقات الصحة في `/hub` ✅ (2026-06-09)
- `loadCarriersHub` يجلب بالتوازي مع `hub_rollup`: RPC `carrier_cod_net_balances` + المراجعات المعتمدة + فواتير RV المفتوحة غير المربوطة بمراجعة
- كل بطاقة ناقل تعرض `HealthStrip`: **COD معلّق** (ذهبي، ينقل لـ`/cod-settlements?carrier=`) + **N فاتورة غير مدقَّقة** (أحمر، ينقل لـ`/ledger?carrier=`) + **آخر تدقيق: <period>** (أو «لم تُدقَّق أي فاتورة بعد» بالأحمر إن كان للناقل عقد بلا أي مراجعة معتمدة)
- الإشارات الثلاث تجيب أسئلة الصباح اليومية دون جولة عبر 3 صفحات

### 1.11c توحيد معرّف أرامكس + فخّ COD-عبر-إشعارات ✅ (2026-06-09)
- **توحيد نهائي**: بيانات `carrier_id='aramex'` اليتيمة (294 صف cod_settlement + 4 قيود دفتر) رُحِّلت لـ`c_1777506662790`. لا وجود لمعرّف `aramex` في أي جدول بعد الآن (alias `canon()` في monthlyReportService بقي كحماية فقط)
- **اكتشاف محاسبي مهم**: أرامكس تحوّل تحصيل COD **كإشعارات دائنة (DG) داخل كشف الحساب** — قيود COD CR الدفترية الأربعة كانت **نفس مبالغ** إشعارات DG الأربعة (2,485 + 13,277 + 2,148 + 8,384.01 = 26,294.01) → ازدواج يخفض الرصيد غلطاً. حُذفت قيود COD الدفترية (صفوف cod_settlement التفصيلية بقيت — هي مصدر شاشة التسويات)
- **الحارس الدائم**: `file_signature.cod_remit_via_credit_note=true` على أرامكس — `saveSettlementUpload` يتخطّى قيد COD الدفتري لهؤلاء الناقلين (صفوف التسوية تُدرَج عادي). أي ناقل يحوّل COD عبر إشعارات كشفه يجب أن يحمل هذا العلم وإلا تكرّر الائتمان

### 1.11d إعادة هيكلة الواجهة — مساحة عمل الناقل + الرفع الذكي + قائمة مبسّطة ✅ (2026-06-09)
- **مساحة عمل الناقل**: `CarrierTabs` (components/) شريط chrome مشترك يربط 5 شاشات (نظرة عامة `/carrier?id=` · المراجعات `/audits?carrier=` · تحصيل COD `/cod-settlements?carrier=` · كشف الحساب `/aramex-statements?carrier=` · الدفتر `/ledger?carrier=`) — تنقّل فقط، لا تضمين صفحات. كل صفحة تقرأ `?carrier=` وتتفلتر (CodSettlements وCarrierStatements عبر effect على `location.search` — PageSlot يبقي الصفحات mounted)
- **الرفع الذكي `/drop`** (SmartDrop.jsx): نقطة رفع واحدة لأي ملف — يشمّ المحتوى: PDF كشف (sniffStatementCarrier) → stash `statementImport` → `/aramex-statements`؛ PDF فاتورة أرامكس (looksLikeAramexInvoice) → stash `webhookImport` → `/upload`؛ xlsx فيه deliveryCharges → مراجعة؛ xlsx بـ AWB/مبالغ فقط → تحصيل (يسأل عن الناقل إن لم يُكتشف). نفس نمط stash+pathname-listener (§1.5). مستمع `statementImport` في CarrierStatements بحارس module-level `CONSUMED_STATEMENT_IMPORTS`
- **القائمة الجانبية**: pinned = الرئيسية + رفع ملف + الوارد + مركز الرفع؛ الأقسام = شركات الشحن (hub/audits/ledger/statements/tasks) · **التقارير** (شهري/KPI/تنبؤ/تصدير — قسم جديد) · الأموال · العملاء · النظام (إدارة الشركات + العقود انتقلتا هنا). عنصر «مراجعة جديدة» حُذف من القائمة (`/upload` ما زال يعمل — يوصَل عبر `/drop`)

### 1.11e سلامة البيانات `/integrity` ✅ (2026-06-12)
- RPC `integrity_check()` (Postgres): 6 فحوص تناقضات صامتة — مراجعة معتمدة بلا أثر دفتري · سحبة بلا ملف · skipped رغم أوزان · اعتماد بلا تحصيل مستخرَج · قيد COD مزدوج مع إشعارات · RV قديمة (+45ي) بلا تدقيق
- الصفحة (`IntegrityCheck.jsx` + `integrityService.js`): بطاقة لكل تناقض + عينات + زر فتح، وأول إصلاح آلي `resetWronglySkipped`. **أي فخّ بيانات جديد يُضاف كفحص هنا**

### 1.11f إعادة هيكلة القائمة الجانبية (IA) ✅ (آخر تحديث 2026-08-01)
- نقطة الحقيقة `NAV_ITEMS`/`NAV_SECTIONS` في `App.jsx`. المثبّت أعلى القائمة = **الرئيسية + لوحة القرارات فقط**، ثم خمسة أقسام مبنية على رحلة العمل: **شركات الشحن · الأموال والتحصيل · العملاء والنمو · التقارير والرقابة · الإعدادات**.
- «الإعدادات» للإدارة الفعلية فقط: الفريق والصلاحيات · إدارة شركات الشحن · العقود والأسعار. الفوترة والإقفال ضمن «الأموال والتحصيل»، ومصادر البيانات/السلامة/السجل ضمن «التقارير والرقابة»، ومركز هاتف ضمن «العملاء والنمو» باسم «الحملات والاتصالات».
- المسميات تميّز الوظيفة لا الكيان: «نظرة الناقلين» · «كشوف الناقلين» · «دفتر حساب الناقلين» · «فرص المنصة» (مولدة من بيانات المنصة) · «مسار المبيعات CRM» (الصفقات المملوكة والمتابعة) · «فواتير ودفعات زوهو» · «سجل التقارير المصدّرة».
- ترتيب الصفوف داخل القسم يُحسم بـ`navOrder` لا بموضع الكائن في الملف؛ أي عنصر جديد يأخذ ترتيبًا صريحًا داخل قسمه.
- **أكورديون قسم-واحد:** فتح قسم يقفل كل البقية. كل دخول/تحديث يبدأ **بجميع الأقسام مقفلة**؛ لا تُحفظ حالة الفتح في `localStorage` ولا يفتح المسار الحالي قسمه تلقائيًا. الطيّ عبر `maxHeight:0` لا unmount.
- **المرحلة ب** (دمج الـhubs): `CarriersWorkspace` صار **3 تبويبات** (كشف الشركات `/hub` · أداء الناقلين `/carrier-kpi` · المطالبات `/claims`) — `carrier-kpi` نُقل من «التقارير» و`claims` من قائمة «شركات الشحن» المسطّحة. عنصر `hub` في NAV له `subTabs` تعرضها كصفوف فرعية. `CARRIER_WORKSPACE_PATHS` يشمل الثلاثة؛ `/claims` لم يعد PageSlot مستقلاً (يُمرَّر `carriers` للـworkspace لأن Claims يحتاجه). المسارات القديمة `/carrier-kpi` و`/claims` تهبط على تبويبها.
- **القاعدة:** أي صفحة رفع/إدخال جديدة → قسم «الرفع والوارد». لا تُثبّت عناصر جديدة (المثبّت = الرئيسية + لوحة القرارات فقط). كل أيقونة قسم لون مميّز. أي عدسة جديدة لـ«حالة الناقلين» → تبويب في `CarriersWorkspace` لا عنصر nav مسطّح.

### 1.19 درجة خطر العميل + لوحة القرارات ✅ (2026-06-21)
- `customerRisk.js`: `computeRisk(c)` يجمع أعلام الشذوذ في درجة 0–100 (دين+أعمار+شدّة الشذوذ+قابلية التحصيل) + مستوى + `shouldStop` (عميل نشط بدفع لاحق ودينه متأخّر/تجاوز الحد = أوقفه قبل التراكم). يُستخدَم في تبويب تنبيهات الديون (شارة خطر + بانر «🛑 يُوقَف الآن» + تصدير القائمة) **و** في لوحة القرارات.
- **لوحة القرارات** `/decisions` (`DecisionsBoard.jsx`، مثبّتة «شاشة الصباح»): تجمع إشارات القرار من كل التطبيق في بطاقات: يُوقَف الآن (من `loadCustomerWatch`+`computeRisk`) · COD لم يُحصَّل (`loadCarrierNetBalances` الموجب) · خزائن COD محتجزة (`loadTreasuryBalances`) · فجوة تسجيل Zoho (`loadVendorReconciliation`) · تنبيهات العملاء. كل بطاقة تنقل لصفحة الإجراء.
- **القاعدة:** أي إشارة قرار جديدة (تحتاج فعلاً اليوم) تُضاف كبطاقة في `/decisions` — لا تُدفَن في صفحتها فقط.

### 1.20 كتالوج الأزرار + معجم التسمية ✅ (2026-06-29، من تدقيق وكلاء)
**دلالة variants الـ`Btn` (في `src/components/UI.jsx`، موثّقة فوق `VARIANTS`):**
- `primary` (أسود) = الفعل الرئيسي **الواحد** للسياق (رفع/تأكيد المعالج/التالي)
- `accent` (أخضر) = تأكيد/حفظ/اعتماد/تسديد/ربط (modal confirm، اعتماد المراجعة)
- `ghost` = مساعد/ثانوي/تنقّل/**إلغاء**/تصدير ثانوي/تحديث
- `danger` (أحمر) = حذف/رفض مدمِّر فقط
- `gold` (برتقالي) = تنبيه/استثناء فقط (لا للأدوات العامة)
- `outline` = الحالة المطفأة لزر تبديل (toggle غير نشط؛ النشط = `primary`)
- **`success`/`navy` مهملان** (success ≡ accent لونياً، navy ≡ primary) — لا تستعملهما في كود جديد
- حجم `full` = عرض كامل (width:100%+توسيط) بدل style overrides · prop `title` للأزرار الأيقونية · المعطّل = لون muted دلالي (لا opacity)

**قاعدة المكان:** PageHeader actions = الفعل الرئيسي (primary) + مساعدات (ghost sm) · شريط الفلاتر = تصدير/أدوات العرض (ghost sm) · صف الجدول = إجراءات سياقية (ghost sm، الإيجابي accent، الحذف danger) · modal footer = إلغاء (ghost) + تأكيد (accent/danger). **أقصى زر بارز واحد في الـheader**.

**معجم التسمية (ممنوع «سحب»/«صدّر» المبهمة):**
- **استيراد** 📥 = ملف وافد للنظام · **تحميل** ⬇️ = ملف للجهاز · **تصدير** 📤 = لنظام خارجي · **رفع** = قناة الاستقبال
- **تحصيل لمحة** = المتوقّع من نظام لمحة الداخلي (out) · **تحصيل شركة الشحن** = المُحوَّل فعلاً (in) · الفرق = متوقّع − مُستلَم
- **مراجعة** = الكيان (السجل) · **تدقيق** = الفعل · **إلغاء** (لا «تراجع») في الـmodals

**توكنات الثيم** (`src/index.css` `:root`): `--space-1..8` (تباعد) · `--fs-xs..3xl` + `--lh-tight/snug/normal/relaxed` (طباعة) — أساس للمهاجرة التدريجية. الوضع الداكن: `--green` إيميرالد مميّز عن `--accent` التركواز. **الخط ثابت PingAR** (لا يُغيَّر).

**القاعدة:** أي زر/تسمية/لون جديد يُقاس على هذا الكتالوج. لا تُدخِل variant مهملاً ولا مصطلحاً مبهماً.

### 1.21 حزمة «الوضوح والسلاسة» — موجات تحليل الوكلاء ✅ (2026-07-02)
من workflow تحليل 6 محاور (IA/ثيم/جوال/أرقام/تقارير/CRM) — أبرز الثوابت الجديدة:
- **مركز التقارير `/reports`** (ReportsCenter.jsx): كتالوج بمعاملات (شهر/ناقل) — شهري + كشف ناقل رسمي (`carrierSoaExport.js`، رصيد جارٍ §2.4 + COD معلّق) + **مطابقة بنكية** (`bankReconReport.js`: bank_transactions × قيود PAY، ±0.5 ر.س ±3 أيام، يستبعد المرفوض §1.15، 3 أوراق). **كل توليد يمرّ عبر `persistAndDownloadExport`** (kinds: monthly/carrier_soa/bank_recon) — قاعدة §1.13 مطبَّقة.
- **درجة صحة الناقل الموحّدة** `carrierScore.js` — لا معادلات محلية (كانت Overview وCarrierKpi متناقضتين).
- **RTL لكل تصدير Excel** عبر `xlsxRtl.js` (31 نقطة) — الاستثناء الوحيد `generateCleanExcel` (§1.15).
- **شريط تنقّل سفلي للجوال** (`.bottom-nav` في App.jsx، ≤768px): الرئيسية/القرارات/رفع/الوارد/القائمة — طبقة جوال منفصلة لا تخالف §1.11f. زر المساعد مرفوع فوقه.
- **CRM قوائم المبيعات** (`/crm?tab=sales`): 3 قوائم من merchants (سجّل وما شحن · توقّف · نشط وخامل +45ي) + `PhoneLink` (tel/wa.me) + بطاقة العميل تعرض جانب المبيعات و`effectiveDebt` (زوهو + محفظة سالبة — `customerRisk.js`).
- **التقرير الشهري**: عمود التغيّر شهر-بشهر + شارة «⚠ زاد ولم يُدقَّق» (مفوتر +10% بلا مراجعة) + طباعة A4 (`@media print` عام — `.no-print` يخفي، `.print-only-title` يظهر).
- **قاعدة المودالات**: `role="dialog"` على Modal — لا تنشئ مودالاً بلا هذا الدور (قواعد الجوال تعتمد عليه).

### 1.22 ربط Zoho Books API + شاشة «الوضع المالي» /pnl ✅ (2026-07-02)
- **الربط**: تطبيق Server-based **منفصل** (Self Client القديم يشغّل النظام الداخلي — **ممنوع لمسه**). OAuth عبر `/zoho-callback` (ZohoCallback.jsx) → edge function **`zoho-sync` v6** تبادل الكود بـrefresh token دائم في `zoho_auth` (RLS بلا سياسات = service role فقط). Scopes قراءة فقط — **ممنوع أي كتابة في زوهو**.
- **أمان الدالة (من فحص وكلاء عدائي — 7 ثغرات أُغلقت)**: verify_jwt يقبل anon العام فلا يحمي → الحماية داخلية: `requireUser` (getUser) لكل action؛ الأرقام تتطلب admin أو `money.pnl`؛ `exchange_web` admin فقط + قفل `already_connected` (الاستبدال يحتاج `force:true`). TTL سيرفري 10 دقائق على `pnl_month` (حصة زوهو 100/دقيقة). CORS مقيّد بدومين التطبيق. **أي action جديد في zoho-sync يمرّ بنفس الحارس**.
- **قائمة الدخل**: `GET /books/v3/reports/profitandloss` (يعمل لكنه **غير موثّق** — computed_net البديل موجود). المحلّل `parsePnl`: «غير التشغيلي» يُطابَق **قبل** «التشغيلي» (وإلا كتبت non-operating فوق income بالمؤسسات الإنجليزية)، مطابقة عمق ≤1، أول-مطابقة-تفوز، لا نزول داخل قسم مُلتقط، الصافي من قسم «صافي» بالاسم أو محسوباً.
- **الشاشة** `/pnl` (FinancialPosition.jsx + pnlService.js): كاش `pnl_snapshots` (صف/شهر، PK كامل) أولاً؛ تحديث حي للشهر الجاري إن قدُم +6 ساعات. بانر رابحون/خاسرون + شلال بلغة غير المالي + أرباع (الجاري لا يُوسَم مكتملاً) + بطاقة في `/decisions`.
- **فحص فجوة التسجيل**: دفترنا (شامل 15%) ÷1.15 مقابل cogs زوهو (قبل الضريبة) — **لا تقارن الشامل بقبل-الضريبة مباشرة** (فجوة وهمية 15%). عتبة نسبية 5% + بطاقة للفجوة السالبة.
- **قاعدتان ثابتتان**: تحصيل COD **ليس دخلاً** (أمانة التجار). «صافي الشهر» بالرئيسية اسمه «صافي حركة النقد مع الناقلين» — **لا رقمان باسم «صافي/ربح» بدلالتين**؛ الربح من `/pnl` حصراً.
- مصدر الدالة المرجعي: `supabase/functions/zoho-sync/index.ts` — أي تعديل يُنشر عبر MCP أيضاً.

### 1.23 حزمة فواتير+زوهو (لوحة الفواتير · المطابقة الحيّة · حملة المتأخرين) ✅ (2026-07-03)
- **لوحة فواتير `/zoho-data`**: التبويب الافتراضي `invoices`. RPC `zoho_invoice_dashboard()` (خفيف — لا يحمّل 4884 صفاً): open_ar/overdue/draft + شهرياً (remaining=SUM(balance)) + أعلى 20 مديناً؛ نقرة المدين تفلتر (`onPick=setQ`). تعريب الحالات عبر `zohoStatusAr`/`ZOHO_STATUS_AR` (pnlService) — شارات `StatusPill` ملوّنة في الجدول والفلتر والتصدير. **لا تعرض حالة زوهو إنجليزية خام في UI جديد**
- **مطابقة أرصدة العملاء الحيّة**: تبويب `zoho_live` (الافتراضي) في `/reconciliation` — RPC `customer_balance_recon_zoho()`: فواتير زوهو المفتوحة (حيّة) × آخر snapshot داخلي (صفوف `is_summary` فقط — الإجمالي per-customer) × دليل المتاجر. المرساة: `customer_merchant_links` → اسم مطبَّع → `'n:'||norm`. الحالات: `matched` (±1) / `needs_investigation` / `internal_only` (أرصدة افتتاحية قديمة — **ليست خطأ ربط**) / `zoho_only`. المحفظة محور مستقل لا تُطرح. نتيجة أول تشغيل: 54 مطابقاً بالهللة (246,376.97) · 25 داخلي-فقط (57,882) · 2 تحقيق (6,156)
- **حملة المتأخرين**: RPC `zoho_overdue_campaign()` (overdue مجمَّعة بالعميل + هاتف من المتاجر + قائمة فواتير) → زر «📲 حملة واتساب» (WhatsAppSendModal/Respondly — تأكيد صريح قبل الإرسال، بوابة `collections.view`) + «📞 ملف الحملة» Excel عبر `persistAndDownloadExport` بـ `kind='zoho_campaign'`
- **بطاقة «فواتير تنتظر نظرتك»** في `/decisions`: `loadInvoicesAwaitingReview` (webhookService) = أحداث `processed_at IS NULL AND audit_id IS NULL AND status≠failed` مع عمر بالأيام — تنقل لـ`/webhook` حيث ⚡ دقّق واعتمد
- **عدّاد الاسترداد** في `/pnl`: شريط تراكمي من `audit_claims` (`summarizeClaims`) — استُرد فعلاً/قيد المطالبة/مكتشفة، ينقل لـ`/hub?tab=claims`
- **تمرير الجوال في المودالات**: أي حاوية `overflowY:auto` داخل Modal تحمل `className="m-flow"` (وإلا حبست إصبع iOS — §1.18). مودال الجوال padding=16

### 1.24 «فلوسي عند العملاء» + ملخّص الصباح + توحيد مرجع الدين ✅ (2026-07-03)
- **`/customer-money`** (CustomerMoney.jsx): شاشة التحصيل الأولى، جوال أولاً — RPC `customer_money_dashboard()` (مستحق/متأخر/أعمار 4 شرائح/تحصيل شهري بحدّين + 56 عميلاً بهواتفهم وآخر دفعة). بطاقة العميل: 📞 tel: + 💬 wa.me + فواتيره بنقرة (`loadZohoOpenInvoices`). حملة واتساب للقائمة المفلترة. في شريط الجوال السفلي («فلوسي» بدل «رفع»)
- **ملخّص الصباح**: edge function `morning-brief` v1 + pg_cron `morning-brief-daily` (7:15 KSA) بهوية X-Cron-Key (نمط zoho-sync). الإعداد في `app_settings['morning_brief']` من زر 🌅 في `/customer-money` (تفعيل/رقم/قالب/معاينة/إرسال تجريبي). 6 متغيرات قالب Respondly. معطَّل افتراضياً — الكرون آمن دائماً
- **قاعدة مرجع دين العملاء = زوهو الحي** (فحص 20 وكيلاً، 14 تناقضاً مؤكداً — التقرير في Codex أدناه وtasks #40/#41):
  - ⛔ [تاريخي — البوابة أُلغيت §1.57] `portal_lookup` (بوابة العميل — **حُذفت**) كانت تقرأ **فواتير زوهو الحيّة** (balance>0.5) لا snapshot — كان يطالب عميلاً سدّد (79.96 حقيقي) بـ6,201.42 مجمّدة. **ممنوع إرجاعه للـ snapshot**
  - الرئيسية: «مستحق لنا (العملاء)» و«تركّز المديونيات» من `zoho_invoice_dashboard` + شارة «زوهو حي» + fallback صامت للـ snapshot؛ النقر ينقل لـ`/customer-money`
  - `/customer-360?tab=receivables` و`loadLatestReceivables()` يقرآن فواتير زوهو المفتوحة من المرآة (`zoho_invoices balance>0.5`) كمصدر أساسي، مع fallback فقط للـ snapshot القديم. لا تعرض رقم مديونية عميل من snapshot كأنه الرقم الرسمي.
  - `loadEffectiveBankBalance()` في bankBalanceService = نقطة الحقيقة **لإجمالي البنوك المسجّلة**: يحسب أحدث مصدر لكل بنك مستقلاً (ختامي كشف ذلك البنك أو إدخاله اليدوي — الأحدث يفوز داخل الحساب)، ثم يجمع الحسابات. **ممنوع أن يستبدل إدخال يدوي لبنك واحد إجمالي بقية البنوك.** Forecast وOverview كلاهما عليها، والواجهة تسمي الرقم «السيولة/البنوك المسجّلة» لا «الوضع الكامل».
- **متبقٍ موثَّق**: تناقضات الناقلين (رصيد /hub شامل المسدَّد · PAY لا يكتبها أحد · صافي معكوس الإشارة · COD الشهري بمصدرين) في task #40 ✅، وازدواجية حملة /receivables في #41 ✅
- **توحيد حملة التحصيل ✅ (محدّث 2026-07-13)**: **الحملة الوحيدة = زوهو الحيّ** في `/customer-money` (WhatsApp + ملف `kind='zoho_campaign'`). ملف `/receivables` أصبح ملف متابعة مبنياً على نفس مرجع الدين (`zoho_invoices`) + دليل المتاجر، ومُرِّر عبر `persistAndDownloadExport` (`kind='internal_watchlist'`). RPC `customer_money_dashboard` كُبِّر بحقول المتجر (billing_type/status/wallet_balance/last_shipment_at — إضافة صرفة، المفاتيح القديمة سليمة) فصار ملف الحملة الحيّ يحمل نفس سياق العميل الغني. **قاعدة: أي حملة/دنّينغ يقرأ الدين من زوهو الحيّ لا من الـsnapshot.**

### 1.25 توحيد أرقام الناقلين — نقاط الحقيقة الواحدة ✅ (2026-07-03، فحص وكلاء)
من فحص إعادة البناء (62 وكيلاً): 5 تناقضات مؤكدة لنفس المفهوم عبر صفحات مختلفة، كلها وُحِّدت ومُتحقَّقة بالـSQL الحي.
- **رصيد الناقل** = `SUM(DR−CR) FILTER (status≠'paid')` — نقطة الحقيقة `carrier_open_balance` (/ledger). `hub_rollup` يُرجِع الآن `open_balance` بنفسها (/hub و/carrier)، وcarrierProfileService يستبعد المسدَّد. **ممنوع حساب balance = totalDr−totalCr الخام** (كان أرامكس يظهر 164,003 وهو مسدَّد بالكامل). total_dr/total_cr للعرض فقط.
- **المدفوعات للناقلين** = جدول `payments` (حسب paid_at) — **لا قيود `doc_type='PAY'`** (لا يكتبها أحد؛ createPaymentRecord يكتب في payments + يقلب status فقط). مطبَّق في: monthlyReportService · bankReconReport (loadPayOps يُطبِّع حقول payments لأسماء القيد) · `monthly_financial_snapshot.carrier_paid`. أي تقرير مدفوعات جديد يقرأ payments لا PAY.
- **مستحق علينا للناقلين (AP)** = `SUM(net) per carrier HAVING net>0.5` (المدينون فقط) — نقطة الحقيقة `ap_aging_by_carrier`. وُحِّد عليها `working_capital_now.total_ap` وcashAgingService. الأرصدة الدائنة (COD محتجز: سمسا/J&T…) **تُعرَض منفصلة** لا تُطرَح (كان −558K مضلِّلاً يخلط COD بالفواتير).
- **COD المُستلَم الشهري** = `cod_settlement direction='in'` (حسب upload_date) — **لا قيود COD الدفترية** (كانت تختلف 655K مقابل 588K). monthlyReportService يقرأ cod_settlement مثل الرئيسية.
- **«صافي الحركة مع الناقلين»** = `COD − billed` (نفس `monthly_financial_snapshot`: cod_received − carrier_spend_gross). التقرير الشهري وُحِّد عليها (كان billed−crTotal معكوس الإشارة). **قاعدة §1.22: لا رقمان باسم «صافي» بدلالتين.**

### 1.26 كتابة زوهو المحدودة — تطبيق الأرصدة الدائنة + منح صلاحية داخلي ✅ (2026-07-03)
- **الكتابة الوحيدة المسموحة في زوهو**: تطبيق رصيد دائن **موجود** على فاتورة **موجودة**. **ممنوع منعاً باتاً**: إنشاء/حذف فواتير، أي POST/PUT/DELETE آخر، أي صلاحية حسّاسة (DELETE/banking/settings.UPDATE). قاعدة المستخدم الصريحة.
- **الـendpoints الصحيحة (مثبتة في مؤسسة المستخدم .com)**: `POST /invoices/{id}/credits` **يُرفَض** «not authorized» في هذه المؤسسة. المستعملان الناجحان (من كود Deluge للمستخدم): **الدفعات الزائدة → `PUT /customerpayments/{id}` مع `{invoices:[...]}`** (اجلب التطبيقات القائمة عبر GET وضمّها حتى لا يدهسها الـPUT) · **الإشعارات الدائنة → `POST /creditnotes/{id}/invoices`** (إضافي آمن). دالة `zoho-apply-credits` v3.
- **كشف الأرصدة** (قراءة): RPC `zoho_customer_unused_credits()` من `zoho_contacts` (unused_credits_receivable>0.5 + دين) — قسم في `/customer-money` + رابط زوهو مباشر لكل عميل. `loadZohoUnusedCredits`/`planZohoApplyCredits`/`applyZohoCredits` في pnlService.
- **منح الصلاحية داخل النظام**: دالة `zoho-authurl` (admin) تبني رابط موافقة بالنطاق الموسّع = قراءة كل الوحدات + كتابة محدودة: **UPDATE على invoices/creditnotes/customerpayments/contacts + `creditnotes.CREATE`** (صفر DELETE/banking/settings.UPDATE). الموافقة تعود لـ`/zoho-callback` (exchange_web force).
- **تصحيح جوهري (2026-07-06 — Tine)**: تطبيق **إشعار دائن** = `POST /creditnotes/{id}/invoices` يعتبره زوهو **CREATE** فيحتاج `creditnotes.CREATE` (بـUPDATE وحدها → «not authorized»). الدفعة الزائدة = `PUT` (UPDATE، تعمل). فالقاعدة القديمة «صفر CREATE» عُدِّلت: `creditnotes.CREATE` مضافة لتطبيق الإشعارات آلياً. **الكود يطبّق فقط (POST …/invoices)، لا يُنشئ مستنداً أبداً (لا `POST /creditnotes`)** — قرار المستخدم صراحةً «المصدر إشعار/دفعة يجب ألّا يفرّق». بعد التعديل يجب **إعادة المنح** (زر «منح الصلاحية») ليشمل التوكن الجديد النطاق. تصحيح لملاحظة سابقة: «not authorized» هنا **كان scope فعلاً** (CREATE ناقص) لا endpoint.
- **تكامل مع Deluge المستخدم**: كوده يعالج الدفعات الزائدة الجديدة (trigger عند الإنشاء). نظامنا يكمّل: **الإشعارات الدائنة + المتراكم** (ما يتخطّاه كوده). لا ازدواج (كل مصدر يُطبَّق مرة).

### 1.26b تطبيق الرصيد من المرآة + القابل للتطبيق فعلاً ✅ (2026-07-04)
من إلحاح المستخدم «احفظ الأرقام واقرأ منها بدل استدعاء زوهو كل مرة» + سلسلة رفض «أكثر من الرصيد» المتكرّر. **الجذر المكتشَف**: الأرصدة كانت **تُطبَّق فعلاً** (الفواتير تُصفَّر)، لكن الشارة تقرأ `contacts.unused_credits_receivable` (رقم مجمّع يتقادم) فتُبقي العميل ظاهراً → كل محاولة لاحقة تصطدم بفاتورة رصيدها صفر. «أكثر من الرصيد» = إعادة تطبيق على فاتورة مسدَّدة، لا فشل حقيقي.
- **حصة زوهو (~100 طلب/دقيقة للمؤسسة، مشتركة مع Deluge)**: `zoho-apply-credits` تطوّر لـ**v11**: `zfetch` إعادة محاولة 429 · كاش توكن (ساعة) + كاش خطة (90ث) · إعادة جلب طازج (getFresh) عند رفض الرصيد · **بناء الخطة من المرآة المحلية** (`buildPlanFromMirror`: `zoho_invoices`/`zoho_creditnotes`/`zoho_payments` عبر اسم العميل من `zoho_contacts`) = **صفر استدعاء زوهو** في الحالة الطبيعية، مع fallback حيّ (`buildPlanLive`) عند فراغ المرآة. `allocate()` منطق مشترك. **قاعدة: الأسماء تتطابق حرفياً بين مرايا زوهو** (contact_name = customer_name) — تحقّقنا.
- **المرآة كُبِّرت** (`zoho-sync` **v12**): جدول جديد `zoho_creditnotes` (لم يكن يُمرآ) + عمود `zoho_payments.unused_amount` (كان `amount` فقط) — أساس بناء الخطة محلياً. المزامنة صارت **كل 30 دقيقة** (cron jobid 2) لإبقاء المرآة طازجة. **أي جلب زوهو متكرّر يجب أن يقرأ من المرآة لا حيّاً؛ التطبيق (الكتابة) فقط يمسّ زوهو + getFresh عند الرفض.**
- **القابل للتطبيق فعلاً**: RPC `zoho_applicable_credits()` = `min(الرصيد المتاح, الفواتير المفتوحة)` لكل عميل من المرآة — يستبعد من له رصيد بلا فواتير مفتوحة (لا شيء يُطبَّق عليه). `loadZohoUnusedCredits` يقرأه بدل `zoho_customer_unused_credits` (contacts). **قاعدة: لا تعرض «رصيد قابل للتطبيق» دون التأكد من وجود فاتورة مفتوحة تقابله.**
- **Phase B — Webhooks ✅ (2026-07-04)**: دالة `zoho-webhook` (verify_jwt=false) تستقبل webhooks زوهو وتحدّث المرآة (`zoho_invoices`/`zoho_creditnotes`/`zoho_payments`) **فوراً بلا استدعاء زوهو**. الأمان: سرّ `zoho_auth.webhook_key` عبر `?key=` (403 لغيره). void/deleted → رصيد صفر. يرجع 200 دائماً (لا إعادة محاولة لا نهائية). المزامنة الدورية (30د) تبقى شبكة أمان. **الإعداد في زوهو خطوة المستخدم** (Workflow Rules لكل وحدة → Webhook للرابط بالسرّ). حينها تُلغى نافذة التقادم عملياً.
- **فخّ الإشعار الدائن العالق + مصالحة الحذف ✅ (2026-07-06 — `zoho-sync` v13)**: عميل ظهر برصيد دائن وهمي 1000 (CN-00029) رغم أن رصيده الحيّ في زوهو صفر. **جذران**: (١) **تطبيق إشعار على فاتورة (`POST …/invoices`) لا يُحرّك `last_modified_time` للإشعار** → مزامنة الدلتا لا تعيد جلبه (تتوقّف عند `last_modified ≤ آخر مزامنة`) فيبقى رصيده عالقاً. الحل: `creditnotes` صار **`noDelta`** (سحب كامل — الجدول عشرات الصفوف). (٢) **الحذف/الإلغاء في زوهو يُخرج الإشعار من القائمة، والمزامنة `upsert` فقط فلا تحذف الصفوف المختفية** → تبقى للأبد. الحل: **`reconcileDeletes`** على `creditnotes` — بعد سحب كامل مكتمل (`more=false`)، يحذف صفوف `synced_at < runStart` (لم تُلمَس هذه الدورة = اختفت من زوهو). **قاعدة: أي كيان مرآة قد تُحذف/تُلغى سجلاته في زوهو دون تحريك last_modified يجب أن يكون `noDelta` + `reconcileDeletes` (فقط لكيان كامل يفي بحدّ الصفحات).** التحقّق: بعد الإصلاح كل الأرصدة القابلة طابقت `zoho_contacts.unused_credits_receivable` بالهللة. **مرجع أوثق للرصيد الدائن = `zoho_contacts` (يحسبه زوهو، يُسحَب كاملاً)** — لو اختلف عن جمع الإشعارات/الدفعات فالمرآة بها صفّ عالق.

### 1.28 التصعيد القانوني `/legal` + أهداف الأعمار ✅ (2026-07-04)
- **مالان مختلفان لا رقم واحد**: العميل **دفع لاحق** دينه = فواتير زوهو المفتوحة (محاسبي). العميل **دفع مسبق** رصيده = محفظة المنصّة (سالب = دين). الرابط بينهما = `customer_merchant_links` (customer_name زوهو ↔ store_id المنصّة). «الرصيد الواحد» = زوهو للاحق + المحفظة للمسبق جنباً لجنب، لا رقم مدموج.
- **RPC `legal_escalation_dashboard()`** (مرآة محلية): (١) إجماليات الأعمار من `zoho_invoices` المفتوحة مقابل أهداف ثابتة **31–60 ≤ 50ألف · 61–90 ≤ 25ألف · 91+ = صفر**. (٢) عملاء تجاوزوا 90 يوم (زوهو) + هاتفهم عبر الربط. (٣) متاجر `billing_type='دفع مسبق'` بمحفظة `<−0.5` من أحدث snapshot للمتاجر.
- **الصفحة `/legal`** (LegalEscalation.jsx، قسم العملاء، `receivables.view`): بطاقات هدف أخضر/أحمر + قائمتا تصعيد (هاتف/واتساب لكل حالة) + «ملف القانونية» Excel (ورقتان) عبر `persistAndDownloadExport` (kind `legal`).
- **القاعدة**: أي إشارة «حوّل للقانونية» جديدة → قائمة في `/legal`. الأهداف ثابتة في الـRPC (تُعدَّل هناك).

### 1.12 COD المستحق غير المحصَّل في Overview ✅ (UX 2026-05-29)
- `overviewService.loadOverview` يجلب `loadCarrierNetBalances()` (RPC `carrier_cod_net_balances`) ويُرجِع `codOutstanding = { total, carriersDue }` (مجموع الصافي الموجب > 0.5 لكل ناقل)
- `Overview.jsx` → `CashHero` يعرض بطاقة "COD لم يُحصَّل بعد" (تظهر فقط إن > 0.5) تنقل لـ `/money?tab=cod`
- مفتاح الشهر في Overview يُحفظ في `sessionStorage['sa-overview-period']` — يدوم خلال الجلسة (يبقى الشهر التاريخي بعد refresh) ويُصفَّر لـ current عند جلسة جديدة. لا يوجد period عام عبر الصفحات (لا مستهلك حقيقي له — Forecast يستخدم horizon بالأيام، باقي الصفحات all-time)

### 1.13 سجلّ السحبات + تخزين ملفات `/internal-exports` ✅ (UX 2026-06-01)
- مشكلة سابقة: ملفات السحب (تحصيلات/فواتير) كانت تنزل للمتصفّح فقط ولا تُخزَّن — لو ضاعت من Downloads ما فيه طريقة لإعادتها (الأوزان كانت مخزَّنة سلفاً عبر `weight_billing_exports` + bucket `weight-billing`).
- الحل (يحاكي نمط الأوزان):
  - bucket `internal-exports` (private) + جدول `internal_export_pulls` (kind/file_name/file_path/row_count/total/pulled_at/pulled_by)
  - `persistAndDownloadExport({ wb, fileName, kind, rowCount, total, userId })` في `internalExportsService.js`: يبني الـ xlsx مرة → يرفعه للـ storage + يسجّل صف + ينزّله للمتصفّح. **فشل التخزين غير قاتل** (التنزيل يكمّل) — bucket ناقص ما يوقف العملية
  - مفتاح الـ storage **ASCII فقط** (`asciiKey`) — الاسم العربي يبقى في `file_name`، المفتاح المنظّف في `file_path` (نفس فخّ §1.7)
  - `pullCodReceipts` (kind='cod') و `pullCustomerInvoicing` (kind='invoicing') يستدعيان الـ helper بدل `XLSX.writeFile`
  - `loadExportHistory()` يدمج `internal_export_pulls` + `weight_billing_exports` في قائمة موحّدة (لكل سجل `bucket`)؛ `downloadExportFile({ bucket, filePath, fileName })` يعيد التحميل من الـ storage
  - `InternalExports.jsx`: قسم "السحبات السابقة" (جدول: تاريخ/نوع/ملف/صفوف + زر تحميل). صفوف `file_path=null` (أوزان قديمة قبل التخزين) زرّها معطَّل
- **القاعدة:** أي تصدير جديد للنظام الخارجي يجب أن يمرّ عبر `persistAndDownloadExport` (تخزين + سجل) لا `XLSX.writeFile` المباشر، ليبقى قابلاً لإعادة التحميل.

### 1.14 معالجة آلية + استبدال snapshots صندوق Zoho ✅ (UX 2026-06-02)
- كل مصادر `UPLOAD_SOURCES` (zoho_customers/zoho_vendors/receivables/internal_settlement/merchants) هي **snapshots** — الأحدث يلغي الأقدم.
- مشكلة سابقة: كل إيميل Zoho يولّد `zoho_intake_events` صف `pending` يحتاج ضغط «عالج» يدوي، وتتراكم نسخ متعددة لنفس النوع (3× أرصدة عملاء…).
- الحل في `UploadsHub.jsx` + `zohoIntakeService.js`:
  - `supersedePendingIntake()` يُجمّع الـ pending حسب `detected_source` ويُبقي الأحدث فقط، ويُحوّل الأقدم لـ `dismissed` (السبب: «مُستبدَل بنسخة أحدث»). يُستدعى في `refresh()` قبل عرض الصندوق → الصندوق يتقلّص لواحد-لكل-نوع
  - **معالجة آلية**: effect في `UploadsHub` يعالج تلقائياً كل حدث `detected_source` (عبر `handleProcessAllIntake`)؛ يتتبّع المعالَج في `autoAttempted` (ref Set) فكل حدث يُعالَج مرة واحدة (الوافد الجديد يُعالَج، والفاشل لا يُعاد في حلقة). الأنواع غير المكتشفة تبقى للاختيار اليدوي
- **القاعدة:** snapshots = الأحدث يلغي الأقدم. لا تعالج نسخة قديمة من نفس النوع. المعالجة الآلية فقط للـ snapshots المكتشَفة (لا للمراجعات/COD التي تحتاج اعتماداً بشرياً)
- **استبدال server-side**: `zoho-intake` edge function **v6** — فور وصول ملف نوعه مكتشَف، يُحوّل الأحداث الأقدم pending من نفس `detected_source` لـ dismissed («مُستبدَل بنسخة أحدث») فوراً. فالصندوق يتقلّص لواحد-لكل-نوع **بدون فتح أي صفحة**. (الـ parsing/ingestion لا تزال client-side عند فتح `/uploads`)
- **معلّق (المعالجة الكاملة server-side)**: نقل الـ parsing+matching لـ Deno يكرّر منطق مطابقة مالي (`resolveStoreIds` 3-tier + RPC `bulk_match_customers`) → خطر drift يسبّب أرصدة خاطئة صامتة. الحل الآمن: مصدر واحد مشترك (RPC أو `_shared` module) لا نسخة مكرّرة. مهمة مقصودة منفصلة، تُختبَر بمقارنة ناتج السيرفر مع ناتج الـ client المعروف

### 1.15 كشف البنك المتراكم + الدمج الذكي ✅ (2026-06-20)
- صفحة `/bank` (BankStatement) كانت **في الذاكرة فقط** — كل رفع يستبدل السابق. الآن العمليات تُحفظ في جدول `bank_transactions` وتتراكم عبر الفترات.
- `saveBankTransactions` في `bankTransactionsService.js`: كل عملية لها `dedup_key` = `ref:<المرجع البنكي>` إن وُجد، وإلا `auto:<تاريخ|وصف|مدين|دائن>`. الجدول عليه **فهرس فريد كامل (لا جزئي)** على `dedup_key` → `upsert onConflict:'dedup_key'` **آمن من فخّ 42P10** (§6). فرفع فترة متداخلة **يدمج** الصفوف المشتركة (يحدّثها لآخر قيمة) ولا يضاعفها، والصفوف الجديدة تُضاف.
- النتيجة تُرجِع `{ saved, added, merged }` للعرض. تبويب «الدفتر البنكي المحفوظ» يعرض المتراكم مع بحث/فلترة فترة/تصدير/حذف.
- **القاعدة:** أي مصدر مالي يُرفَع تكراراً (بتداخل فترات) يجب أن يحمل `dedup_key` ثابتاً + فهرس فريد **كامل** ليُدمَج لا يتضاعف. المرجع البنكي هو المفتاح الطبيعي.
- **فخّ التصادم (2026-07-01)**: التحويل المرفوض يُعاد قيده بنفس الرقم المرجعي (قيد مدين + قيد دائن **بنفس المرجع**). كان `dedup_key = ref:<المرجع>` فيتصادم القيدان → يُدمَجان ويُفقَد أحدهما عند الحفظ. الإصلاح: `dedup_key = ref:<المرجع>:<D|C>` (الاتجاه من `credit>0`). **أي مصدر يمكن أن يحمل قيدين بنفس المرجع (مدين+دائن) يجب أن يضمّ الاتجاه للمفتاح**. (صفوف مايو ما قبل الإصلاح لو أُعيد رفعها تُضاف بمفاتيح جديدة — الفترات القديمة بلا أزواج رفض غير متأثرة).
- **العمليات المرفوضة/المُرجَعة (2026-07-01)**: `annotateRejected(list)` في `bankStatementProcessor.js` يعلّم كلا القيدين (`rejected=true`) حين يطابق قيدٌ دائنٌ نمطَ رفض (`تم رفض التحويل`/مرتجع/عكس قيد/reject/revers…) قيداً مديناً **بنفس المرجع**. يعمل على الصفوف المُحلَّلة والمحفوظة (نفس أسماء الحقول). الواجهة: شارة «↩︎ مرفوض» + تظليل أحمر خفيف + سطر ملخّص «N مرفوضة (صافي صفر)». **الإجماليات المعروضة تبقى شاملة للمرفوض** (تطابق إجماليات البنك المطبوعة في لوحة التحقّق — البنك يعدّها). لكن **التصدير الصافي يحذفها**: `generateCleanExcel` يفلتر `!t.rejected` (كلا القيدين، صافيهما صفر) فلا تصل النظام المحاسبي الخارجي — والتوست يذكر عدد المحذوف. مايو 2026 = 3 تحويلات KonHub دولية رُفضت ورُدّت بالكامل (6 صفوف تُحذف من التصدير).

### 1.16 سمسا فروع في مُنتقي تحصيل COD ✅ (2026-06-20)
- مُنتقي الناقلين في `/cod-settlements` يُبنى من `REMITTANCE_PARSERS` (محلّلات ملفات التحصيل) **لا** من جدول الشركات. فأي ناقل بلا parser تحصيل يختفي من الصفحة حتى لو كان شركة كاملة تُدقَّق.
- `smsa_branches` (RX8668) كان شركة جاهزة بلا parser → اختفى. أُضيف `smsaBranchesRemittanceParser` يعيد استخدام مفاتيح أعمدة سمسا المشتركة (`SMSA_AWB_KEYS`/`SMSA_AMT_KEYS`) بمعرّف `smsa_branches`.
- زر **تنزيل لكل ملف تسوية** في «الملفات المرفوعة» (`loadUploadShipments` + `handleExportUpload`): يصدّر Excel فيه رقم كل شحنة + حالتها في المطابقة (من تجميع `reconByAwb`).

### 1.17 الإقفال الشهري — مُفعَّل ومُنفَّذ بالـ DB ✅ (مُتحقَّق 2026-06-21)
- **جاهز بالكامل**: جدول `period_closes` + RPC `months_with_activity` + خدمة `periodsService.js` (`closePeriod`/`reopenPeriod`/`isClosed`/`refreshClosedSet`) + صفحة `/periods` (بوّابة `system.period_close`). لم يُقفَل أي شهر بعد (0 صفوف) — الميزة تنتظر الاستخدام لا التطوير.
- **الإنفاذ الحقيقي في الـ DB لا الواجهة**: 4 triggers على `carrier_operations`/`cod_settlement`/`audits`/`payments` تستدعي `guard_closed_period(<عمود التاريخ>)`. أي INSERT/UPDATE/DELETE بصفّ تاريخه ضمن شهر مُقفل → `RAISE EXCEPTION 'الفترة % مُقفلة — أعد فتحها قبل التعديل'` (ERRCODE **P0001**). الصفوف **بلا تاريخ معفاة** (لا تُقفَل البيانات القديمة).
- `isClosed()` متزامن من cache على مستوى module؛ مسارات التعديل في الواجهة تستدعي `refreshClosedSet()` لتمنع الـ round-trip قبل ضرب الـ trigger.
- **القاعدة:** أي جدول مالي جديد بعمود تاريخ يجب أن يُربَط بـ trigger `guard_closed_period` ليحترم الإقفال. وأي مسار تعديل جديد يجب أن يتوقّع خطأ P0001 ويعرضه للمستخدم بوضوح.

### 1.18 معالجة الجوال — استراتيجية CSS ✅ (2026-06-21)
- كل إصلاحات الجوال في `src/index.css` داخل `@media (max-width: 768px)` و`(max-width: 480px)`. الصفحات تستخدم inline styles فالقصّ يتم بمطابقة **substring** لقيمة الستايل المسلسلة من المتصفّح (بلا اقتباسات، مع تطبيع مسافات: `minmax(140px,1fr)`→`minmax(140px, 1fr)`، `0`→`0px`).
- **القاعدة:** قبل إضافة محدّد substring جديد، تحقّق من تسلسل المتصفّح الفعلي بـ`getComputedStyle` (لا تخمّن). الشبكات ذات 4+ أعمدة أو عمود px ثابت تنطوي لـ`1fr`؛ صفوف العرض `1fr auto` و`auto 1fr auto` تبقى صفوفاً (لا تُقصَّ).
- أصناف مساعدة: **`.m-cards`** يحوّل جدولاً عريضاً إلى بطاقات (كل صف بطاقة، كل خلية «label: value» من `data-label`؛ خلية `data-label=""` = عنوان البطاقة) — مطبَّق على 9 جداول كثيفة. **`.m-compact`** يُبقي أول عمودين فقط (اسم+مبلغ) للجداول ذات النقر-للتفاصيل. **`.hero-grid`** يتراصّ. **`.mobile-hide`** يُخفي. الجانبية درج `position:fixed` بعرض `min(85vw,320px)` + **`visibility:hidden` مغلقاً** (وإلا تمرير أفقي على iOS — العناصر fixed خارج الشاشة لا يقصّها iOS رغم `overflow:hidden`).
- **القاعدة:** أي جدول عريض جديد على الجوال → استخدم `.m-cards` (لا تخفِ بيانات) أو `.m-compact` (إن كان النقر يفتح تفاصيل). أي درج/عنصر `fixed` خارج الشاشة يجب أن يكون `visibility:hidden` مغلقاً.
- **التمرير السفلي + المودالات (2026-07-31):** عنصر التمرير الحقيقي للصفحات هو `.page-slot` المطلق، لذلك مساحة شريط الجوال الآمنة توضع عليه لا على `.page-content`. المودال القياسي يملك منطقة تمرير واحدة `.modal-body` داخل لوحة محدودة بـ`100dvh`؛ الرأس ثابت داخل اللوحة، ولا يُعاد نمط تمرير الـoverlay مع تمرير داخلي لأنه غير موثوق في Safari. أي مودال جديد يستخدم `Modal` المشترك، وأي محتوى طويل داخله يترك التمرير لـ`.modal-body`.

### 1.18b نظام التصميم الهادئ v5 ✅ (2026-07-28)
- المصدر النهائي للهوية البصرية هو `src/design-v5.css` ويُستورد **بعد** `index.css`. هذه الطبقة تفصل التصميم الجاري عن طبقات CSS التاريخية، فلا تضف override بصرياً جديداً في نهاية `index.css`.
- الاتجاه: مساحة عمل مالية هادئة ومحايدة؛ لا هيدر بطولي متدرّج، لا نقوش مائية، ولا تدرجات لونية للأزرار الأساسية. اللون يُستخدم للدلالة والحالة، لا للزينة.
- القائمة الجانبية **أكورديون قسم واحد**: فتح قسم يقفل البقية، والمسار النشط يفتح قسمه تلقائياً. تبويبات الـhubs تبقى داخل الصفحة ولا تُكرّر كصفوف جانبية.
- خط الواجهة `PingARLT` من الملفات المحلية. لا تعرّف خطوطاً غير موجودة أو تعتمد على طلب خارجي.
- المكوّنات المشتركة (`PageHeader`/`Card`/`Btn`/`Input`/`Select`/`Modal`) هي نقطة تطبيق الشكل. أي صفحة جديدة تستخدمها بدلاً من صناعة هيدر أو زر أو مودال محلي.
- التصميم يدعم الوضعين عبر التوكنات، و`color-mix`، وcontainer queries، و`prefers-reduced-motion`. أي لون صلب جديد يجب اختباره في الفاتح والداكن، وأي عنصر تفاعلي يجب أن يحمل `focus-visible`.
- الرئيسية تعرض **ملخص سيولة واحداً فقط**: الوضع النقدي ← مكوّناته الثلاثة ← الأولويات القابلة للتنفيذ. لا تضف بطاقة أو قسماً لاحقاً يعيد البنك/ذمم العملاء/ذمم الناقلين بالأرقام نفسها.
- أسماء قسم العملاء تصف **وظيفة الشاشة** لا نوع الحاوية: `ملفات العملاء` · `الديون والتحصيل` · `فرص البيع` · `الصفقات والمتابعات` · `خدمة العملاء`. لا تستخدم «مركز» أو «متابعة» وحدها في مسمى جديد.

### 1.18c مساحات العمل وحالات البيانات الصادقة ✅ (2026-07-30)
- `src/components/WorkspaceTabs.jsx` هو المبدّل القياسي لأي hub: عنوان مساحة العمل + تبويبات دلالية `role=tablist/tab/tabpanel` + أسهم/Home/End + تمرير أفقي على الجوال. لا تُنشئ شريط تبويبات محلياً بأنماط inline.
- كل تبويب يحمل `eyebrow/purpose/description/outcome/tone` ويُمرَّر إلى `WorkspaceContext` حتى يفهم الموظف وظيفة الشاشة والنتيجة المتوقعة منها قبل الدخول في التفاصيل.
- أسماء التبويبات تصف الحركة التجارية بدقة: `تحصيل شركات الشحن` ≠ `دفعات الناقلين`، و`الحسابات البنكية` تجمع الحسابات ولا توحي بأنه كشف بنك واحد.
- فشل جلب البيانات المالية **ليس حالة فارغة**: لا تستبدل الخطأ بـ`[]` أو أصفار. اعرض خطأ واضحاً مع إعادة المحاولة؛ وإذا وُجدت بيانات ناجحة سابقة فتبقى ظاهرة مع تنبيه أنها قديمة.
- غلاف صفحات القرار هو `.workspace-page` من `design-v5.css`، مع `.workspace-loading-state` و`.data-load-error` للحالات المشتركة. الجوال يحافظ على 44px تقريباً للأهداف التفاعلية وعلى ظهور الإجراء الرئيسي.

### 1.18d متابعة خدمة العملاء الإدارية ✅ (2026-07-30)
- **حدود التكامل:** هاتف هو قناة التواصل ونظام المحادثة المنفصل؛ `/support` لا يحاول استبداله ولا يفترض أن غياب حدث داخل لمحة يعني أن العميل لم يرد. لا CSAT ولا بوابة عميل ولا تغيير لحالة محادثة هاتف من تذكرة الدعم.
- التذكرة الإدارية تحمل `assigned_to` + `priority` (`normal/high/urgent`) + `next_followup_at` + `last_followup_at`. شاشة `/support` تبدأ بإشارات العمل: متابعة متأخرة · مستحقة خلال 24 ساعة · بلا مسؤول · بلا موعد متابعة.
- الإغلاق/الحل لا يتم بلا `closure_reason` و`resolution_summary`؛ trigger `support_ticket_admin_guard` يفرض ذلك في القاعدة، وRPCs `support_update_status`/`support_assign_ticket`/`support_update_followup` تجمع التعديل مع حدث التدقيق ذرياً.
- **سياق الشحن شرطي:** أنواع `delayed/damaged/cod` وحدها تعرض وتلزم شركة الشحن + AWB؛ بقية الأنواع لا تعرضهما وتحفظ الحقول الثلاثة `null`. القيد `support_tickets_shipping_context_check` يفرض ذلك في القاعدة. خيارات الشركات تأتي من `loadLamhaCarrierOptions()` — نفس الشركات النشطة و`displayName` الظاهر في صفحة المقارنة، لا الاسم الداخلي في `carriers`.
- التحديث الجماعي يمر حصراً عبر `support_bulk_update`: حتى 200 تذكرة، يتحقق من المجموعة كاملة ثم يحدّث الحالة/الأولوية/المسؤول/المتابعة ويسجل حدثاً لكل تذكرة في معاملة واحدة. لا إغلاق جماعي بلا سبب وخلاصة، ولا **حذف جماعي**.
- **لا إغلاق آلي:** كرون `support-autoclose-daily` أُلغي؛ `updated_at` داخل لمحة لا يثبت مرور 3 أيام بلا رد في هاتف. الإغلاق قرار موظف موثّق.
- كل التعليقات والمرفقات في التذكرة **داخلية فقط** ولا تُرسل إلى هاتف أو العميل. لا تضف اختيار «خارجي» قبل وجود تكامل إرسال حقيقي ومؤكد.
- **فصل واتساب عن Leads:** رد العميل على واتساب يبقى مسؤولية فريق هاتف ولا يفتح Lead أو بطاقة عمل في `/sales`. يمكن عرض مؤشرات جودة القناة إدارياً فقط؛ إنشاء فرصة بيع يتطلب إشارة متجر من المنصّة أو وارد Excel/Webhook صريح.
- سياسات RLS لجداول/حاوية الدعم مربوطة بمفاتيح `support.view/create/manage/delete`، وRPCs الإدارية غير متاحة لـ`anon`.

---

## 2. المبادئ الأساسية (Non-Negotiable)

### 2.1 دورة حياة الصفحات
- `App.jsx` يلفّ كل صفحة في `<PageSlot>` الذي **يبدّل visibility فقط** — لا يفصل المكوّن
- `useEffect` بـ `[]` deps يشتغل **مرة واحدة** عند تشغيل التطبيق، لا عند التنقّل
- **القاعدة:** إذا تحتاج كود يشتغل عند الوصول لصفحة، اعتمد على `location.pathname` كـ dep

### 2.2 React StrictMode
- `main.jsx` يلفّ التطبيق في `<React.StrictMode>`
- `useEffect` setup يشتغل **مرتين** في dev (mount → cleanup → mount مرة ثانية)
- `useRef` و `useState initializer` يشتغلان مرتين أيضاً
- **القاعدة:** أي guard يجب أن يكون **على مستوى module** (متغيّر خارج المكوّن) أو في storage (لكن انتبه للحذف المبكّر)

### 2.3 دلالات COD direction
- `'out'` = ما نتوقّع استلامه (مصدره النظام الداخلي للمستخدم فقط)
- `'in'` = ما استلمناه فعلاً (ملف من الناقل، أو tehweel بنكي)
- **لا تنشئ 'out' من audit أبداً.** أي ملف من شركة شحن = 'in'
- **زر «ارفع تحويل» (in) مخفي للناقلين `audit_with_cod`** (iMile/DeliverNow) في `/cod-settlements` — لأن اعتماد المراجعة يُنشئ صفوف 'in' تلقائياً، فالرفع اليدوي يكرّر التحصيل (double-count). يُستبدَل بتلميح "التحصيل يُسجَّل تلقائياً عند اعتماد المراجعة". زر «ارفع متوقّع» (out) يبقى ظاهراً للجميع (مصدره نظام المستخدم الداخلي). الحارس: `fileKindById.get(carrier) === 'audit_with_cod'` في `CodSettlements.jsx`
- **ملاحظة المحاسبة المحايدة (`status='note'`)**: في `cod_reconciliation_action`، الحالة `'note'` تُرفِق ملاحظة لشحنة **دون تغيير حالتها المالية** (تبقى outstanding/over_remit/matched طبيعية). في `loadReconciliation`: `if (action.status !== 'note') override; else keep natural + attach notes`. زر «📝 ملاحظة للمحاسبة» في `Row` (CodSettlements). ملاحظة فارغة = حذف (تستدعي `clearReconciliationAction`). لا تجعل أي حالة قرار (approved/disputed/resolved) note-only — هي تُلغي الحالة الطبيعية عمداً

### 2.4 المحاسبة في `carrier_operations`
- DR = نحن مدينون للشركة (فاتورة شحن)
- CR = الشركة مدينة لنا / نحن دفعنا (تحصيل COD، تحويل بنكي)
- الرصيد = `SUM(DR) - SUM(CR)`. موجب = نحن مدينون، سالب = هي مدينة لنا
- **قيد واحد لكل حدث مالي.** لا قيد لكل AWB. التفاصيل في `audit_shipments` / `cod_settlement`

### 2.4b Broker / sub-carrier pricing (Boleeseh-style)
- `contract.pricingKey = 'subCarrier'` يحوّل سلوك `auditAll`:
  - بدل `pricing['Saudi Arabia']` يستخدم `pricing[row.subCarrier]`
  - لـ COD: يبحث عن `pricing[<subCarrier>_cod]` أولاً قبل الـ fallback
- الـ `subCarrier` يُستخرج من عمود "شركة الشحن" (regex `/^شركة\s*الشحن$/`)
- الـ `codPaymentMethod` يقبل أيضاً عمود "نوع الدفع"
- بوليصة هي المثال الوحيد حالياً (4 ناقلين فرعيين)

### 2.4c أعلام العقد الخاصة (contract flags)
- `posFeeOnCod: true` — رسوم POS = نسبة من **مبلغ التحصيل** (codAmount) لا من عمود POS Amount منفصل. (ويبك: 0.8%). `auditRow` يستخدم codAmount كأساس POS.
  - **الدفع المختلط (split payment) ✅ مُصلَّح (2026-07-03)**: عند دفع مختلط (بطاقة + نقد، مثل J&T `'NLCard Cash'`) النسبة (2%) تُطبَّق على **جزء البطاقة فقط** — النقد مجاني. الملف لا يفصّل جزء البطاقة، فرسم الناقل نفسه هو الحقيقة (2%×جزء البطاقة). الإصلاح في `auditRow`: `isSplitPay` يكتشف وجود رمز نقد **و** رمز بطاقة معاً في `codPaymentMethod` → `expectedPosFee = invoicedPosFee` (passthrough كالوقود/codFee). الأساس + الوزن يبقيان مدقَّقين بصرامة. **مُتحقَّق على فاتورة J&T يونيو (For Tech): 1,690 مطابق، 0 لصالحك** (كانت 8 فروق وهمية −2.20/−4.00/−6.00… كلها 2%×كامل COD مطروحاً منه رسم الناقل الفعلي). البطاقة الكاملة (NLCard بلا نقد) تبقى مدقَّقة 2%×COD (لا تُطابق isSplitPay). الملف سليم رياضياً 100% (التوصيل=16+وزن، الضريبة=15%، DETAILS=SUMMARY).
- `deliveryInclusiveVat: true` — عمود التوصيل **شامل الضريبة 15%**؛ `auditRow` يقسمه ÷1.15 للمقارنة قبل الضريبة ويُظهر فرق الضريبة كـ tax. (ويبك)
- `codFeePassthrough: true` — رسوم COD تُقبل كما هي (expected=invoiced) حين لا تتبع قاعدة. (سمسا فروع، ويبك)
- `fuelPassthrough: true` — (أرامكس) رسوم الوقود **تُقبل كما هي** (expected=invoiced): الوقود market-indexed يتغيّر شهرياً وجدول الناقل المنشور هو المعتمد (قاعدة المستخدم 2026-06-11: «المعتمد في الجدول صحيح مهما كان»). الأساس + RSS يبقيان مدقَّقين بصرامة ضد العقد — هناك يظهر الـoverbilling الحقيقي. لا تعيد تثبيت نسبة وقود لأرامكس وتطالب بفروقاتها
- `priceFromContract: true` — (ديلكس) الملف المرفوع («كشف الطلبات») **بلا أسعار إطلاقاً** (Cost=0) — المحرّك **يحسب** الفاتورة المتوقعة من العقد (التوصيل لكل مُسلَّمة + posFeePct×COD) ويجعلها الطرفين (invoiced=expected، كله ok). يتطلب `mapRows(raw, colMap, {keepUnbilled:true})` وإلا أُسقطت كل الصفوف كغير مفوترة — الـcallers (UploadWizard/CarrierLedger) يستنتجونها من `carrier.contracts.some(c=>c.priceFromContract)`. إسقاط المرتجعات (signingStatus) يبقى نشطاً — مرتجع ديلكس مجاني فلا يدخل العدّ. التقريب 4dp لكل صف (الناقل يحسب النسبة على إجمالي الفترة)
- `inboundPassthrough: true` — (أرامكس) الشحنة ذات منشأ **دولة أجنبية معروفة** (`KNOWN_FOREIGN_COUNTRIES`) ووجهة السعودية = **مرتجع وارد**: تمرّ pass-through بحالة `inbound` (لا تُدقَّق سعرياً — لا جدول وارد بالعقد) وتظهر في تقرير منفصل «وارد لفوترة التجار» (`exportInboundReturns` في export.js — يتضمن `shipperRef` = بوليصة الصادر الأصلية، مفتاح تفويت التاجر بالنظام الداخلي الذي يفوتر الصادر فقط). **الشرط على مجموعة دول معروفة لا `origin≠SA`** — أعمدة المنشأ المحلية فيها مدن (Jeddah) لا تتطبّع لـSaudi Arabia فتُصنَّف غلطاً
- `posFeePct` / `fuelPct` — نسب. `pricing` بشريحة واحدة مسطّحة `[{upTo:null,price:X}]` = سعر ثابت لكل شحنة بغض النظر عن الوزن (calcDelivery يرجعه حتى لوزن 0 — للناقلين الذين لا يفوترون بالوزن مثل ويبك)

### 2.5 file_kind القيم المسموحة
- `'audit_with_cod'` — الفاتورة تشمل COD (DeliverNow)
- `'audit_and_cod_separate'` — ملفان منفصلان (Aramex)
- `'audit_only'` — فاتورة بدون COD
- `'cod_only'` — ملف تحصيل فقط
- `null` / `unset` — غير محدد، النظام لا يستخرج COD تلقائياً

---

## 3. خريطة الملفات

| المسار | المسؤولية |
|---|---|
| `src/lib/coreService.js` | CRUD للـ audits + carriers، بوابة الاعتماد، auto-posting، loadAuditShipments |
| `src/lib/codSettlementService.js` | COD reconciliation، syncAuditCodOut، saveSettlementUpload، loadUploadShipments |
| `src/lib/bankTransactionsService.js` | حفظ/تحميل عمليات كشف البنك المتراكمة (دمج ذكي عبر `dedup_key`) |
| `src/lib/customerReceivablesService.js` | parser + snapshot fallback/upload + Zoho AR rollup (`zoho_invoices`) + merchant overlay |
| `src/lib/merchantsService.js` | merchant directory (snapshot) + Levenshtein fuzzy linker + insights |
| `src/lib/webhookService.js` | CRUD لـ webhook_events، delete (مع verify) |
| `src/lib/carriersHubService.js` | تجميع بيانات `/hub` (paginated) |
| `src/lib/carrierProfileService.js` | بيانات بروفايل شركة واحدة، updateCarrierFileSignature |
| `src/lib/carrierStatementsService.js` | رفع كشوف الشركات الخارجية، carrier_operations CRUD |
| `src/lib/contractHistoryService.js` | تتبع تغييرات العقود |
| `src/pages/UploadWizard.jsx` | معالج المراجعة 3 خطوات، استيراد Webhook |
| `src/pages/AuditResults.jsx` | شاشة المراجعة + بوابة الاعتماد UI |
| `src/pages/WebhookEvents.jsx` | صندوق الوارد، حفظ كمراجعة، حذف |
| `src/pages/CodSettlements.jsx` | تسويات COD، تبويبات، تصدير قسم حالي |
| `src/pages/CustomerReceivables.jsx` | `/receivables` / `/customer-360?tab=receivables` — مديونيات العملاء من زوهو + تنبيهات + ملف متابعة |
| `src/pages/Merchants.jsx` | `/merchants` — دليل المتاجر + لوحة insights + ربط تلقائي |
| `src/pages/CarriersHub.jsx` | `/hub` — كرت لكل شركة |
| `src/pages/CarrierProfile.jsx` | `/carrier?id=X` — بروفايل شركة كامل |
| `src/pages/CarrierLedger.jsx` | `/ledger` — الكشف المحاسبي للشركات |
| `src/pages/MonthlyReport.jsx` + `src/lib/monthlyReportService.js` | `/monthly-report` — تقرير شهري لكل ناقل (مفوتر/تحصيل COD/إشعارات/مدفوعات/صافي + جودة التدقيق). يجمّع `carrier_operations` (حسب `doc_date`) + `audits`. **توحيد alias**: `aramex` → `c_1777506662790` (COD رُفع تحت slug، العمليات تحت الـ id الكامل) عبر `canon()`. تصدير Excel |
| `src/engine/audit.js` | اكتشاف الأعمدة، حساب العقد، buildSummary. **تدقيق المكوّنات**: `auditRow` يحسب مفوتر/متوقّع/فرق لكل مكوّن (delivery/rss/fuel/codFee/posFee) ويفصل RSS عن الوقود المدموج (سطر ~723: `row.rss===0 && calc.rss>0` → ينسب calc.rss من عمود الوقود). الحالة من الإجمالي فقط (لا per-component) |
| `src/engine/aramexInvoiceParser.js` | قارئ فاتورة أرامكس التفصيلية **PDF** (per-shipment): `parseAramexInvoice(buf)` → `{header, rows}`. الـ rows جاهزة للتغذية في نفس مسار rawRows+colMap بـ UploadWizard (المستخدم يستقبل PDF فقط، Excel بالطلب). `deliveryCharges` = gross لكل شحنة (شامل وقود+RSS؛ دولي VAT=0 فnet=gross). يُكتشف بامتداد `.pdf` في `handleFile` |
| `src/engine/codParsers/*.js` | parsers لملفات تحصيل COD لكل شركة |
| `src/lib/carrierScore.js` | **درجة صحة الناقل الموحّدة** (0–100) — نقطة الحقيقة الوحيدة (Overview + CarrierKpi). مكوّنات موزونة، الغائب يُستبعَد ويُعاد توزيع وزنه. عتبات: ≥85/65. لا تعيد معادلة صحة محلية في أي صفحة |
| `src/lib/xlsxRtl.js` | `rtl(wb)` — كل تصدير Excel جديد يلفّ المصنّف به قبل الكتابة (وضع RTL). استثناء وحيد: `generateCleanExcel` (بنية مثبّتة لنظام خارجي §1.15) |
| `src/App.jsx` | Routes + Sidebar + PageSlot |
| `src/components/UI.jsx` | Btn / Card / Modal / Spinner / toast |
| `supabase/functions/webhook-intake/index.ts` | Edge function، حالياً v11 (deployed v12): parsing متسامح — JSON + multipart/form-data + verification ping (جسم فارغ → 200). كان v10 يرفض أي شيء غير JSON صارم بـ `invalid_json` |

---

## 4. مخطط قاعدة البيانات (الجداول الأساسية)

| الجدول | الغرض |
|---|---|
| `carriers` | شركات الشحن (`contracts`, `file_signature` JSONB) |
| `audits` | header للمراجعات (مع `drift_pre_tax`, `mismatch_count`, `review_status`) |
| `audit_shipments` | الشحنات لكل مراجعة (إلى 500K+) |
| `audit_awb_ledger` | للكشف عن تكرار AWB عبر المراجعات |
| `webhook_events` | الإيميلات الواردة |
| `cod_settlement` | تسويات COD per-AWB (in/out) |
| `bank_transactions` | عمليات كشف البنك المتراكمة (دمج ذكي عبر `dedup_key` فريد كامل) |
| `period_closes` | الأشهر المُقفلة (`period` YYYY-MM، status) — triggers `guard_closed_period` تمنع التعديل بشهر مُقفل |
| `cod_reconciliation_action` | اعتمادات/اعتراضات على فروق COD |
| `customer_receivables` | snapshots لمديونيات العملاء (read-only AR view) |
| `customer_settings` | per-customer tag (excluded/priority) — يدوم عبر snapshots |
| `merchants` | snapshots لكشف المتاجر من المنصّة الداخلية |
| `customer_merchant_links` | الربط بين `customer_name` و `store_id` (auto/manual) |
| `carrier_operations` | الكشف المحاسبي (DR/CR لكل شركة) |
| `carrier_statements` | كشوف خارجية مرفوعة من الشركات |
| `payments` + `payment_allocations` | الدفعات وربطها بالعمليات |
| `dispute_notes` | thread للاعتراضات |
| `app_settings` | إعدادات عامة (key/value) |
| `profiles` | المستخدمون والأدوار |

---

## 5. بروتوكول التعديل الآمن

قبل تعديل أي ملف:

1. **اقرأ هذا الملف.**
2. **اقرأ الملف الذي ستعدّله بالكامل** (مش جزء فقط).
3. حدّد أي **invariant** سيتأثّر بتعديلك من القسم 1.
4. لو التعديل يتقاطع مع invariant، اشرح كيف ستحافظ عليه قبل الكتابة.
5. **أصغر تعديل ممكن** — لا تنظّف كود غير ذي علاقة في نفس التعديل.
6. تحقّق من syntax عبر `@babel/parser`:
   ```bash
   node -e "require('@babel/parser').parse(require('fs').readFileSync('PATH','utf8'), { sourceType:'module', plugins:['jsx'] })"
   ```
7. **Smoke-test** التدفّق المتأثّر:
   - عدّلت approveAudit؟ → جرّب اعتماد مراجعة DeliverNow
   - عدّلت Webhook → جرّب "حفظ كمراجعة"
   - عدّلت Hub → جرّب فتح بروفايل شركة
8. **commit واحد لكل تغيير منطقي** — رسالة واضحة تشرح "لماذا" مش "ماذا"
9. **حدّث هذا الملف** إذا التعديل أضاف invariant جديداً.

---

## 6. الفخاخ المعروفة (لا تكرّرها)

| الخطأ | السبب | الحل الصحيح |
|---|---|---|
| استخدام `useRef` كـ guard ضد StrictMode | يُعاد تهيئته بين الـ mounts | استخدم **module-level Set** أو متغيّر خارج المكوّن |
| `useEffect` بـ `[]` deps لـ navigation-triggered logic | الصفحات لا تُفصل (PageSlot) | اعتمد على `location.pathname` كـ dep |
| الاحتفاظ بأسماء عربية في storage keys | Supabase Storage يرفض non-ASCII | sanitize إلى `[A-Za-z0-9._-]` في الـ edge function |
| `setTimeout(..., 0)` لتأخير handleFile | StrictMode يفصل قبل ما الـ timeout يطلع | استدعي مباشرة، الـ FileReader أصلاً async |
| إعادة اشتقاق `reviewStatus` من `audit.isDraft` بعد تعديل `audit.isDraft` في handleApprove | يُلغي `setReviewStatus('approved')` | اعتمد فقط على `[audit.id]` في الـ useEffect |
| استخراج COD كـ 'out' من فاتورة الشركة | يكرّر مع ملف التحويل الفعلي | فقط 'in' من ملف الناقل |
| `from('table').delete()` بدون `.select()` | RLS silent-fail (0 rows affected = no error) | استخدم `.select('id')` وتحقّق من العدد |
| تجاهل `file_signature` في mapping `loadCarriers` | الـ pages تفقد القدرة على قراءة `file_kind` وإلخ — اختفاء أزرار صامت | احرص أن المابر يمرّر كل عمود يحتاجه أي مكوّن استهلاكي |
| استخدام `preview.rows.slice()` بدون فحص `preview.error` | شاشة بيضاء عند فشل الـ parser (rows = undefined) | افحص `!preview.error && Array.isArray(preview.rows)` قبل أي iteration |
| Parser keys لشركة بصيغة ملف واحدة فقط | الملفات بصيغ أخرى تفشل صامتاً (no rows extracted) | احفظ كل column synonyms المعروفة في الـ parser. لكن: `DLX` AWB = ديلكس Delex، `DNL` AWB = ديلفر ناو. لا تخلط الـ keys بينهم |
| الاعتماد على `ws['!ref']` كما هو | بعض الـ exporters (مثل J&T WestBr) يضع نطاقاً قديماً يغطّي 12 صف فقط رغم أن الملف فيه 184 → `sheet_to_json` يقرأ 12 وتُفقَد البقية صامتاً | قبل `sheet_to_json`، اسكان مفاتيح الخلايا واستخرج maxRow/maxCol الفعلي ثم أعد ضبط `ws['!ref']`. مطبَّق في UploadWizard + UploadModal/CodSettlements |
| محاولة تعديل سعر/شريحة العقد مباشرة في DB | يدمّر تاريخ العقود | استخدم `saveCarrierContractsWithHistory` |
| إعادة رفع كشف حساب يُعدّل/يعيد فتح عملية **مسدّدة** | `saveCarrierStatement` كان يقلب أي op غير-pending مبلغها تغيّر لـ `reviewing` ويكتب المبلغ الجديد — حتى لو كانت `paid`. الناقل أحياناً يعيد ترقيم/تصنيف نفس المستند بين نسخ الكشف (AB↔DG) → القيد المسدّد يتغيّر | العمليات `paid`/`partial` **مجمّدة**: عند re-import لا يُعدَّل مبلغها ولا حالتها، فقط `last_statement_id` يُحدَّث. في المعاينة تُصنَّف `frozen` (🔒 مثبّتة) لا `changed`. الحارس: `prior.status === 'paid' \|\| 'partial'` في `saveCarrierStatement` + `deltaOf` |
| تعديل `audits.results` JSONB لإضافة بيانات مهمة | حد TOAST + لن يُحمَّل للـ audits الكبيرة | استخدم `audit_shipments` بدلاً |
| نسيان `idempotency` على auto-posts | إعادة الاعتماد ينشئ قيود مكررة | استخدم unique partial indexes |
| `.upsert(op, { onConflict: 'audit_id' })` على `carrier_operations` | الـ unique index على `audit_id` **جزئي** (`WHERE doc_type='INV'`) — PostgREST لا يمرّر الـ predicate فيرفض Postgres الـ ON CONFLICT بـ `42P10`، والخطأ يُبتلَع في try/catch فلا يُكتب أي قيد INV (تسبّب باختفاء 73,952 ر.س من الدفتر — أُصلح 2026-05-29) | استخدم **delete-then-insert** على `(audit_id, doc_type='INV')` بدل الـ upsert. مطبَّق في `approveAudit` |
| `.upsert(op, { onConflict: 'reference_no' })` على `carrier_operations` في `saveSettlementUpload` (مسار COD) | **نفس فخّ الـ 42P10**: الـ index على `reference_no` جزئي (`WHERE doc_type='COD' AND reference_no IS NOT NULL`) فيُرفض الـ ON CONFLICT، والخطأ يُبتلَع في `console.warn` فلا يُكتب أي قيد COD CR (اختفت 361,280.26 ر.س من 11 رفعة تحصيل — أُصلح + back-filled 2026-05-30) | استخدم **delete-then-insert** على `(reference_no=uploadId, doc_type='COD')` + أعِد `ledgerError` في النتيجة بدل ابتلاع الخطأ (لا ترمِ throw — صفوف `cod_settlement` مُدرَجة سلفاً فالـ retry يتخطّاها كـ cross-file dups ولا يصل لكتلة القيد). مطبَّق في `saveSettlementUpload` |
| تجاهل `file_kind` عند audit approval | إنشاء قيود غير منطقية | افحص دائماً file_kind قبل الـ auto-extract |
| قراءة `stores.xlsx` بدون `raw:true` في XLSX.read | الهواتف 12-رقم تصبح `9.66502E+11` (تفقد آخر رقمين) | `sheet_to_json(ws, { raw:true })` + `toPhoneString` يحوّل `number` → `String(Math.round(v))` |
| auto-link يكتب فوق ربط يدوي | يفقد المستخدم تصنيفه | `autoLinkCustomers` يتخطّى `method='manual'` صراحةً |
| عمود COD باسم مجرّد `"COD"` لا يُكتَشف كـ `codAmount` | أنماط `codAmount` كانت تتطلّب `cod amount`/`cash on` فقط — فعمود iMile المجرّد `"COD"` ما انكشف، وعند الاعتماد (audit_with_cod) سُجّل **0 تحصيل** بدل 41,533 ر.س صامتاً | أُضيف `/^cod$/i` (مُثبَّت بـ anchors) لأنماط `codAmount` في `COL_PATTERNS` — يلتقط `"COD"` المجرّد بدون سرقة `"COD Service Fee"` (الذي يبقى لـ `codFee`). تحقّق دائماً أن `colMap.codAmount` موجود لملفات audit_with_cod قبل الاعتماد |
| استدعاء `loadLatestMerchants()` مباشرة في `loadLatestReceivables` كـ hard dep | الـ receivables تفشل لو ما رُفع snapshot للمتاجر | الاستيراد ديناميكي + `.catch(() => [])` — الـ merchant overlay اختياري |
| `.range(from, to)` للـ pagination **بدون `.order()` ثابت وفريد** | Postgres لا يضمن ترتيباً ثابتاً بين الصفحات، فبمجرد تجاوز الجدول 1000 صف **تتداخل الصفحات** ويتكرّر بعض الصفوف → **مضاعفة المبالغ** في أي aggregate. ظهر في `loadReconciliation` لسمسا (1667 صف): استلمنا = 2× → 399 فرق وهمي. والبيانات سليمة — الخلل في الجلب فقط | أضف `.order('id', { ascending: true })` (أو عمود فريد) قبل كل `.range()` يُكرّر صفحات. `upload_date` وحده لا يكفي (غير فريد) — أضف `id` كـ tiebreaker. أُصلح في **كل** الـ paginated helpers: `loadReconciliation` + `loadSettlementUploads` (codSettlementService) + `loadAllPaginated`/`loadAll` في internalExportsService/merchantsService/customerReceivablesService/carrierProfileService. (`loadAuditShipments` + `syncAuditCodOut` كانا أصلاً مرتّبين بـ id) — **القاعدة: أي `.range()` يُكرّر صفحات لازم `.order('id')` قبله** |

---

## 7. إعدادات الشركات الحالية (محدّث آخر مرة 2026-05-15)

| الشركة | ID | file_kind | عقد ساري | بصمة Webhook |
|---|---|---|---|---|
| DeliverNow | `delivernow` | `audit_with_cod` | ✅ 11 ر.س ثابتة، 15% VAT | ✅ `@delivernow.net` |
| أرامكس | `c_1777506662790` | (غير محدد) | ✅ | ❌ |
| iMile V1 | `imile` | `audit_with_cod` (الـ KSA Fee Bill الإنجليزي = فاتورة + COD معاً، مثل DeliverNow) | ✅ 17 حتى 15kg ثم +1/kg (ceil)، COD fee 1، POS 1% | ❌ |
| J&T Express | `jnt` | `audit_and_cod_separate` | ✅ 16/15kg ثم 1/kg، 2% POS | ⚠️ AWB prefix=JTE، doc-pattern=WestBr، email غير محدد |
| سمسا SMSA | `smsa` | `audit_and_cod_separate` | ✅ 2 عقود: محلي **13 ر.س حتى 15كغ + 2/كغ، وقود 10%** (صُحِّحت العتبة من 10→15كغ في 2026-06-09 بعد استخراجها من 16,606 شحنة — تطابق 100%) + دولي GCC | ❌ · حسابان: RX5251 (هذا) + RX8668 (سمسا فروع) |
| Boleeseh | `boleeseh` | `audit_and_cod_separate` (وسيط broker — فاتورة + تحصيل منفصل) | ✅ تسعير لكل ناقل فرعي (smsa/aramex/aymakan/jt cc/jt cod) | ❌ |
| Webek | `webek` | `audit_with_cod` (تحصيل + فاتورة) | ✅ توصيل ثابت 14 ر.س (Zone A، شامل ضريبة) + POS 0.8% من التحصيل | ✅ `@` (webhook) |
| Aatak | `aatak` | (غير محدد) | ❌ | ❌ |
| Delex | `delex` | `audit_and_cod_separate` | ✅ توصيل 9 ر.س ثابت (الرياض DRY24) + رجيع مجاني + 2% رسوم مدى من COD المحصَّل | ❌ · **فاتورته ملخّصة** (4 بنود) و«كشف الطلبات» تفصيلي بلا أسعار — التدقيق = إعادة حساب البنود من الكشف (عدد المُسلَّم×9 + 2%×مدى) لا شحنة-بشحنة |
| أي مكان Aymakan | `aymakan` | `audit_and_cod_separate` | ✅ توصيل **13 ر.س ثابت كل المدن** + وقود **7.5%** (fuelPct 0.075) + بلا رسم وزن/COD/POS + ضريبة 15% (مُستخرَج من فاتورة 216 شحنة) | ❌ · فاتورة تفصيلية شحنة-بشحنة (أعمدة إنجليزية: Tier Price/Fuel Cost/Bill Number) · **بادئة AWB = RP** · ترويسة الجدول في الصف 18 (ترويسة شركة متعددة الصفوف قبلها — لذا `detectHeaderRow` يمسح 30 صفاً) |
| Varnier / MyGate / سمسا فروع / ثابت | `varnier`/`mygate`/`smsa_branches`/`thabit` | (غير محدد) | ❌ (تستقبل «تحصيل لمحة» المتوقّع من الرفع المجمّع فقط) | ❌ |

> **ملاحظة:** كل تعديل على هذه الجدول يجب أن يتم عبر `/carrier?id=X` (واجهة) أو SQL مع توثيق هنا.

---

## 8. القرارات التصميمية المتّخذة

| القرار | التاريخ | السبب |
|---|---|---|
| تسامح الاعتماد ±0.50 ر.س قبل الضريبة | 2026-05-15 | يستوعب rounding في الـ tiered pricing |
| تسامح الضريبة ±1.00 ر.س (warning فقط) | 2026-05-15 | لا تمنع اعتماد بسبب fractional VAT |
| رفض المراجعة = حذف القيود (لا reversal) | 2026-05-15 | المستخدم admin، الـ audit trail بـ `rejected_at` كافي |
| لا قيد per-AWB في `carrier_operations` | 2026-05-15 | تقليل التعقيد، النظام الداخلي للمستخدم يتتبّع AWB-by-AWB |
| 500K شحنة حد أقصى لكل مراجعة (مبدئياً) | 2026-05-15 | requirement من المستخدم |
| الأقسام الأربعة في القائمة الجانبية | 2026-05-15 | workflow-driven: نظرة عامة / مراجعات / كشوف / إدارة |
| `/hub` كصفحة افتراضية (مستقبلاً) | TBD | أوضح للمستخدم admin غير المالي |

---

## 9. مهام معلّقة معروفة (لا تبدأ بدون قراءة سياقها)

- [x] back-fill قيود ledger للمراجعات السابقة — ✅ 2026-05-29: أُدرجت 8 قيود INV ناقصة (DeliverNow×3، iMile×4، J&T×1 = 73,952.59 ر.س) بعد إصلاح سبب فشل الـ auto-post
- [x] back-fill قيود COD CR الناقصة — ✅ 2026-05-30: أُدرجت 11 قيد COD ناقص (361,280.26 ر.س — aramex×3, smsa×3, jnt×2, delex×2, boleeseh×1) بعد إصلاح نفس فخّ الـ 42P10 في مسار `saveSettlementUpload`
- [ ] تعريف `file_kind` لباقي الشركات (iMile, SMSA, Aramex) — ✅ J&T انتهت
- [ ] back-fill بصمات Webhook (`email_from`) لباقي الشركات بما فيها J&T
- [ ] تطوير parser لـ COD remittance لـ iMile — ✅ J&T انتهى
- [x] إضافة دور "محاسب" / "مدير" مع صلاحيات اعتماد منفصلة — ✅ نظام الأدوار والصلاحيات (§1.10)
- [x] إغلاق فترات شهرية (period closing) — ✅ مُفعَّل ومُنفَّذ بالـ DB (§1.17): جدول `period_closes` + 4 triggers `guard_closed_period` (P0001) + `/periods`. لم يُقفَل شهر بعد، جاهز للاستخدام

---

**آخر تحديث:** 2026-06-21 — (1) §1.18 **معالجة الجوال الشاملة**: استراتيجية CSS كاملة (أصناف `.m-cards`/`.m-compact`/`.hero-grid`/`.mobile-hide`، الجانبية درج 320px مع `visibility:hidden` ضد تمرير iOS، انطواء الشبكات بمطابقة substring متحقَّقة) — 9 جداول كثيفة → بطاقات، كل العالية والمتوسطة من تدقيق وكلاء (39 مشكلة). (2) §1.17 توثيق **الإقفال الشهري** كمُفعَّل ومُنفَّذ بالـ DB (كان مظنوناً معلّقاً).

**أسبق:** 2026-06-20 — (1) §1.15 كشف البنك المتراكم: جدول `bank_transactions` + دمج ذكي عبر `dedup_key` (فهرس فريد كامل، آمن من 42P10) — الرفعات المتداخلة تُدمَج لا تتضاعف. (2) §1.16 سمسا فروع في مُنتقي COD (parser يعيد استخدام صيغة سمسا) + زر تنزيل لكل ملف تسوية مع حالة كل شحنة. (3) إصلاح مُنتقي الناقل في «مطالبة جديدة» (Select يقبل children لا `options=`) + زر «الكشف الكامل» بارز في بطاقة `/hub`.

**أسبق:** 2026-05-30 — (1) إصلاح فخّ الـ 42P10 في مسار COD (`saveSettlementUpload`) + back-fill 11 قيد COD (361,280.26 ر.س). (2) إصلاح اكتشاف عمود COD المجرّد: أُضيف `/^cod$/i` لأنماط `codAmount` — كان iMile KSA Fee Bill يُسجّل 0 تحصيل عند الاعتماد رغم 41,533 ر.س محصَّلة (مُتحقَّق عبر تشغيل المحرّك الفعلي على الملف: 316 شحنة OK، 272 شحنة COD = 41,533.49)

---

## 10. نظام الليد التشغيلي Lead OS ✅ (2026-07-30)

- القناة الحالية هي **Excel**: الرفع يميّز بين `inbound` (مهتمون من حملة، SLA لأول تواصل = 15 دقيقة) و`cold` (قائمة استهداف بلا SLA وارد).
- `crm_leads` هو سجل الفرصة التشغيلي، ويحمل المصدر والحملة ووقت الاستلام وأول محاولة/رد وآخر لمسة والإجراء التالي والنتيجة وسبب الخسارة.
- استيراد Excel لا يحمّل جدول `crm_leads` كاملاً للمتصفح؛ يفحص الهواتف الموجودة فقط عبر `crm_find_existing_leads`.
- تسجيل نتيجة التواصل يمر حصراً عبر `crm_record_lead_outcome`: نتيجة + مرحلة + موعد تالٍ إلزامي، ويحدّث المهمة والصفقة المرتبطة داخل معاملة واحدة.
- الإغلاق يمر عبر `crm_close_lead`: الخسارة تتطلب سبباً، والربح يعني **إغلاق البيع فقط**؛ التفعيل وأول شحنة مرحلتان مستقلتان.
- عند التأهيل/العرض/التفاوض تُنشأ صفقة مرتبطة بـ`lead.id`، ويحرس فهرس جزئي وجود صفقة مفتوحة واحدة فقط لكل Lead.
- `crm_lead_history` يسجل تغييرات المرحلة والمالك والنتيجة والموعد التالي، ولا يعتمد على الواجهة كسجل تدقيق.
- `sales_today` يعرض الوارد غير المسند، المهتمين الجدد، والإجراءات المستحقة بحدود خادمية؛ لا يعيد كامل الجداول داخل JSON.
- **متاجر المنصّة هي المصدر الأول لقائمة المبيعات اليومية:** `sales_today.platform_opportunities` يرتّب من شحن رصيداً ولم يشحن، المسجّل حديثاً بلا أول شحنة، والمتوقف ذي القيمة. استلام الفرصة يكتب في `retargeting_followups` إسناداً واحداً بالهاتف؛ لا ينسخ المتجر إلى `crm_leads`.
- **لا Lead من رد واتساب:** `sales_today` لا يقرأ الردود ولا يعرضها ولا ينشئ منها مهمة. هاتف هو مكان المحادثة وفريقه يتابعها. `crm_leads` يبقى للوارد الصريح من Excel ثم n8n/Webhook.
- المسار القديم `/next-actions` تحويلة توافقية إلى `/retargeting?tab=today`، وليس شاشة مستقلة. إشارات التحصيل تذهب إلى `/customer-money` وإشارات البيع إلى خطة المبيعات.
- **قاعدة ثابتة:** الضغط على اتصال لا يعني «تم التواصل». لا تُسجّل النتيجة إلا بعد معرفة ما حدث، ولا تبقى فرصة مفتوحة بعد تواصل بلا إجراء تالٍ.
- **مؤجل بقصد:** webhook الاستقبال عبر n8n. عند تنفيذه يستخدم نفس سجل `crm_leads` وحقول المصدر/SLA ولا ينشئ نظام فرص موازياً.

### 10.1 CRM متاجر المنصّة ✅ (2026-07-31)

- `/retargeting?tab=pipeline` هو سجل المتابعة التشغيلي لمتاجر المنصّة، بينما «قائمة العمل اليوم» قائمة أولوية فقط، و«تحليل التفعيل» لوحة قياس، و«حملات الاستعادة» أداة تنفيذ جماعي. لا تجعل أيًا منها CRM موازيًا.
- `retargeting_followups.sales_stage` هو **مرحلة البيع اليدوية**، و`v_crm_retargeting.segment` هو **حالة المتجر الموضوعية**. ممنوع دمجهما: «تم الاتفاق» لا يعني أنه نفّذ أول شحنة، و«اشتغل ثم توقف» لا يعني تلقائيًا أننا خسرناه.
- مراحل البيع الثابتة: `new/contacted/qualified/proposal/negotiation/nurture/won/lost/disqualified`. حقل `status` يبقى نتيجة آخر تواصل للتوافق مع الشاشات القديمة.
- `record_platform_sales_activity` هو مسار الحفظ الذري: يحدّث المرحلة/النتيجة/المالك/الموعد ويكتب نشاطًا في `crm_activities(entity_type='platform_merchant')`. التواصل الفعلي مع فرصة مفتوحة يتطلب موعدًا تاليًا، والخسارة تتطلب سببًا.
- «جدول التواصل» مصدره `next_action_at`، ويكشف أيضًا المتابعات المفتوحة بلا موعد بدل إخفائها. `first_contact_at/contact_attempts/lost_at/won_at` مقاييس تشغيلية لا تُشتق من ضغط زر اتصال.
- ملف العميل يجمع ملاحظات المبيعات وأحداث `merchant_lifecycle_events` وسجل هاتف **للقراءة فقط**. رد واتساب أو إسناد محادثة داخل هاتف لا يستدعي دالة الحفظ ولا يفتح Lead.
- `merchant_lifecycle_events` رُجعت تاريخيًا من snapshots الموجودة عند إطلاق CRM، ثم يستمر `capture_merchant_lifecycle_events` في إضافة الأحداث المستقبلية.

---

## 11. الثيم التشغيلي الملوّن — Design System v6 ✅ (2026-07-30)

- `src/design-v5.css` هو **طبقة الثيم النهائية** فوق `index.css` رغم بقاء اسم الملف التاريخي؛ أي تعديل بصري عام يُنفَّذ فيه كي لا تلغيه طبقة لاحقة.
- الجانبية داكنة كحلية في الثيمين، وشعارها الأبيض ثابت. لا تعُد إلى جانبية بيضاء على مساحة عمل فاتحة.
- مساحة العمل تحمل خلفية باردة متدرجة، والبطاقات بيضاء أو ملوّنة بدرجة دلالية مع حدود وظلال واضحة؛ ممنوع نمط «أبيض فوق أبيض» الذي يجعل الأقسام كتلة واحدة.
- رأس الصفحة شريط هوية كحلي/أزرق، أما رأس ملخص العمليات في الرئيسية فشريط مستقل بنفس اللغة البصرية لتمييز القرار عن بقية البيانات.
- ألوان البطاقات دلالية: أخضر للتحصيل والسيولة الإيجابية، أحمر للالتزام والخطر، أزرق للبنوك والمعلومة الأساسية، برتقالي للضريبة والاستثناء، وبنفسجي للأولويات.
- الثيم متحقق عند 1280px وعلى جوال 390px. لا تُخفّض التباين أو تزيل الخلفيات الدلالية عند إضافة صفحة جديدة.

---

## 12. دورة حياة متاجر المنصّة + إشارات الجاهزية ✅ (2026-07-30)

- `v_crm_retargeting` يحافظ على عقد أعمدته القديم ويضيف فقط: `zatca_done` · `compliance_pending` · `readiness_score` · `opportunity_score` · `team_route` · `next_step`.
- **التوجيه ليس كله مبيعات:** رصيد سالب → تحصيل · ملف/توثيق ناقص → تهيئة · مكتمل+موثق+مربوط بلا شحنة → تفعيل مبيعات · متوقف → استعادة · نشط مرتفع القيمة → إدارة حساب.
- `merchant_sales_signals()` إثراء إضافي مستقل عن `sales_today()`؛ الواجهة تستدعيهما بالتوازي وتواصل بقائمة اليوم القديمة إذا لم يتوفر الإثراء أثناء النشر المتدرّج.
- «جاهزون بالكامل لأول شحنة» قائمة منفصلة للمتاجر الأقدم من 30 يومًا كي لا تزاحم الجديد ولا تبقى مدفونة. استلامها يكتب في `retargeting_followups` بنفس مفتاح الهاتف؛ لا نسخ إلى `crm_leads`.
- `merchant_lifecycle_events` يسجل فروقات موضوعية بين Snapshot مكتمل وسابقه: تسجيل · اكتمال ملف/توثيق · ربط/تغيير ربط · شحن رصيد · أول شحنة · عودة شحن · إيقاف/إعادة تفعيل.
- الاستدعاء يتم **بعد اكتمال كل دفعات رفع المتاجر** عبر `capture_merchant_lifecycle_events`، وهو idempotent على `(snapshot_id, store_id, event_type)`. فشل الالتقاط لا يُفشل رفع Snapshot ناجحًا.
- أول Snapshot في قاعدة جديدة هو baseline ولا يولّد آلاف أحداث «مسجل جديد».
- فهرس `merchants_snapshot_store_uidx` يفرض متجرًا واحدًا لكل Snapshot، وفهرس `(uploaded_at desc, snapshot_id)` يخدم اختيار أحدث Snapshot.
- **الثابت:** ردود واتساب عبر هاتف لا تدخل أيًا من هذه القوائم ولا تنشئ Lead؛ مصدر الإشارة هو انتقال متجر المنصّة فقط.
- **محرّك التوجيه التجاري (2026-07-31):** `v_platform_commercial_routing` يفصل `live/live2` كربط متجر مباشر عالي النية، و`webhook/zapier` كتكامل آلي، والفارغ كتسجيل يدوي. الإشارات اليومية: ربط مباشر خلال 5 أيام بلا أول شحنة · **تجاوز آخر شحنة 5 أيام (يبدأ من اليوم السادس؛ 1–5 أيام فترة تشغيل طبيعية وليست فرصة استعادة)** · رصيد موجب بلا شحن حديث · ربط مباشر غير نشط.
- **ترتيب الاستعادة:** تصنيف `recent_stop` يُرتّب تصاعديًا حسب `days_since_last` (اليوم السادس أولًا، ثم السابع...) قبل درجة الإشارة أو عمر المتابعة؛ الهدف التواصل عند بداية الانقطاع، لا تقديم من مضى على توقفه أشهر. هذا الترتيب يُطبّق داخل `platform_commercial_pipeline` قبل `LIMIT/OFFSET` حتى يبقى صحيحًا عبر كل الصفحات.
- **قائمة التشغيل داخل CRM:** `platform_commercial_pipeline_v2` تضيف `work_state/work_rank` وعدّادات وفلاتر حالة التواصل (`لم نتواصل` · `لم يرد` · `متابعة مستحقة` · `تواصل بلا موعد` · `موعد قادم` · `بلا مسؤول`) وتطبّقها قبل `LIMIT/OFFSET`. الجدول يعرض الإجراء، عدد المحاولات، نتيجة وآخر تواصل، الموعد، المسؤول، عمر التوقف والحجم دون اشتراط فتح ملف العميل؛ الملف يبقى للتاريخ الكامل والكتابة.
- **منع تعارض المبيعات والتحصيل:** أي مديونية زوهو مفتوحة (`zoho_invoices.balance>0.5` عبر `customer_merchant_links`) أو **أي محفظة سالبة على أي متجر** للهاتف تجعل `financial_hold=true`. الحساب يُستبعد من `platform_commercial_pipeline` و`sales_today_routed`، وtrigger `retargeting_followups_financial_hold_guard` يمنع كتابة متابعة مبيعات له حتى تزول المعلّقات. لا تستخدم صافي المحافظ لإخفاء متجر سالب بمتجر موجب.
- `/retargeting?tab=pipeline` يبدأ بـ«لايف جديد عالي النية»، ويعرض سبب الأولوية ودرجتها. بطاقة «محوّلون للتحصيل» تنقل إلى `/customer-money` ولا تفتح قائمة مبيعات موازية.

---

## 13. مطابقة زاتكا × زوهو قبل الإيداع ✅ (2026-07-31)

- `loadVatReturn` يحتفظ بتصنيف وخانات زوهو كما هي، ثم يمررها إلى `vatReconciliation.js` لفحص حسابي مستقل.
- الفحص يعيد احتساب **الخانة 1 (مبيعات 15%)** و**الخانة 7 (مشتريات 15%)** من `(المبلغ + التعديل) × 15%`؛ لا يعمم 15% على الصفرية/المعفاة/الاستيراد ولا يعيد تصنيف الفواتير.
- المستند أصبح اسمه **«مسودة الإقرار»** لا «إقرار رسمي»: يعرض ضريبة الإيداع المتوقعة، ويظهر رقم زوهو والفرق صراحةً في ملاحظة المطابقة ونسخة Excel.
- حقول `totals.filingOutputTax` و`filingInputTax` و`filingNetDue` هي أرقام الإدخال المتوقعة في زاتكا؛ أرقام `outputTax/inputTax/netDue` الأصلية تبقى أرقام زوهو بلا تعديل.
- **الثابت:** لا يُخفى فرق زوهو/زاتكا ولا يُستبدل رقم زوهو صامتًا. أي خانة غير 1 و7 تبقى كما وردت من زوهو حتى يراجعها المحاسب.

---

## 14. قائمة المبيعات اليومية ≠ مخزون الفرص ✅ (2026-07-31)

- رقم `platform_opportunity_count` هو **مخزون فرص الفريق** وليس عدد الاتصالات المطلوبة من الموظف اليوم.
- `/retargeting?tab=today` يعرض دفعة منصة محدودة (12 فرصة كحد أقصى في الشاشة) بعد المتابعات والمواعيد الملزمة؛ المخزون الكامل يبقى في «مسار عملاء المنصّة».
- «جاهزون لأول شحنة» إشارة ترتيب داخل الفرصة، وليست قائمة يومية ثانية؛ منعًا لتكرار العميل في عمودين.
- زر «أضف لليوم» يستدعي `claim_platform_sales_opportunity`:
  - إسناد ذري يمنع استلام موظفين لنفس الهاتف.
  - يكتب `next_action_at` فورًا حتى لا تنشأ متابعة مفتوحة بلا موعد.
  - يرفض أي حساب عليه `financial_hold` عبر مصدر التوجيه والحارس القائم.
- ردود واتساب عبر هاتف لا تنشئ Lead ولا تدخل قائمة المبيعات اليومية.
- **الثابت:** لا تعرض أرقام Backlog الكبيرة بصياغة توحي أنها مهام يومية، ولا تنشئ متابعة مبيعات مفتوحة بلا مسؤول وموعد تالٍ.

---

## 15. قائمة التحصيل اليومية + ملكية المهام + تحقق الوعود ✅ (2026-07-31)

- `/collections` يعرض افتراضيًا **25 حسابًا فقط كعمل اليوم**، مرتبة: وعد متأخر/اليوم → تأجيل انتهى → دفع مسبق وعليه دين → تجاوز سقف → +90 → 61–90 → 31–60. الباقي يظهر صراحةً كـ«مخزون» ولا يُصاغ كعمل مطلوب اليوم.
- `refresh_collection_tasks()` هو مسار المزامنة الذري مع `customer_ar` (دين زوهو الحي شامل الأرصدة الافتتاحية). يغلق ما سُدّد، يستبدل السبب بأعلى أولوية، ينشئ الناقص بلا تكرار، ويوزع المهام الجديدة على الموظفين الحاملين `collections.update_stage`.
- زر المزامنة **صريح** وليس effect تلقائيًا عند فتح الصفحة؛ لا تجعل مجرد التصفح ينشئ/يغلق/يعيد إسناد عشرات المهام.
- ملكية `collection_tasks` منفذة بالـRLS: الموظف يرى ويعدّل مهامه فقط. المدير يرى الفريق كله، وصلاحية `collections.view_all` استثناء حساس يمنح الرؤية الشاملة لموظف غير المدير.
- تسجيل الوعد يمر عبر `collection_record_promise`: يحفظ الرصيد الأساسي لحظة الوعد. `reconcile_collection_promises_internal` لا ينسب السداد إلا عند اجتماع **دفعة زوهو بعد الوعد + انخفاض فعلي في الدين**؛ وبذلك لا يُحسب إشعار دائن وحده كتحصيل موظف.
- تحقق كامل الوعد مع بقاء دين يعيد المهمة لعمل اليوم لبقية الرصيد؛ تصفير الدين يغلقها، والوعد المتأخر غير المتحقق يعود للمتابعة. المقياس المالي = `promise_paid_amount` لا عدد المكالمات.
- حارس تعارض المبيعات يسمح فقط بإغلاق متابعة الحساب المحجوز ماليًا إلى `disqualified` بلا موعد تالٍ؛ لا يسمح بفتح/استمرار متابعة مبيعات عليه.

---

## 16. حدود تكامل هاتف + أمان الأحداث والتاقات ✅ (2026-07-31)

- **هاتف هو صندوق العمل التشغيلي للمحادثات والمكالمات.** لمحة طبقة بيانات وحملات وIVR وتحليل؛ لا تعرض طابور محادثات بديل ولا تعتبر رد واتساب أو مكالمة واردة Lead أو مهمة CRM.
- `hatif-webhook` يسجّل التسليم/القراءة/الرد لأغراض القياس ويسند المحادثة داخل هاتف حسب القالب، لكنه لا يكتب `retargeting_followups` أو `crm_tasks`. و`hatif-call-webhook` يحفظ المكالمة والتحليل فقط. الاستثناء هو اختيار IVR أو إحالة عمل صريحة ومهيأة لذلك.
- ربط حالة الرسالة يبدأ بـ`messageId`/`whatsapp_campaign_sends.message_id` ثم يعود إلى conversation/contact للبيانات التاريخية فقط؛ لا تربط رسالتين متقاربتين اعتمادًا على «أحدث محادثة» إن توفر معرّف الرسالة.
- `hatif-send` **يفشل مغلقًا** إذا تعذّر جلب `no_whatsapp_phones`؛ تعذّر الحارس لا يعني السماح بالإرسال.
- API هاتف تستبدل قائمة تاقات المحادثة كاملة. لذلك `hatif-tag-sync` و`hatif-retag-runner` يقرآن المحادثة أولًا، يزيلان/يحدّثان فقط التاقات الأحد عشر التي تملكها لمحة، ويحفظان كل تاق موظف أو إحالة لا تملكه لمحة. `hatif-tags` يعرض الاختيار الحالي قبل التحرير اليدوي.
- **أمان الـwebhook (مغلق):** الدالتان الرسميتان تتحققان من `X-Voxa-Signature` كـHMAC-SHA256 للنص الخام عبر `HATIF_WEBHOOK_SECRET`، و`HATIF_REQUIRE_SIGNATURE=true` مفعّل في الإنتاج. رابطا واتساب وما بعد المكالمة في هاتف بلا `?key=`؛ الطلب الموقّع يمرّ، وغير الموقّع يُرفض `403`. يبقى `?key=` فقط على `hatif-workspace-webhook` و`ivr-webhook` لأن عقد التوقيع الرسمي المنشور لا يشملهما.
- `hatif-workspace-webhook` و`ivr-webhook` ليسا ضمن عقد التوقيع الرسمي المنشور؛ لا تفرض عليهما سر القناة قبل تأكيد دعم هاتف لهما كتابةً.
- **صندوق أحداث متين:** كل طلب رسمي موقّع يثبت أولًا في `hatif_webhook_inbox` بمفتاح SHA-256 من `(المصدر + النص الخام)`. التكرار لا يعيد المعالجة، والمحاولة الفاشلة أو التي قُتلت قبل الإكمال قابلة للاستعادة. أي فشل قاعدة مهم يرجع `500` بدل `200` كي تعمل إعادة محاولة هاتف؛ فقط الحدث غير المرتبط بحملة يُختم `unmatched` بلا إنشاء Lead أو طابور محادثات.
- **منع تكرار الإرسال قبل API:** `hatif_send_claims` يُحجز ذريًا قبل استدعاء هاتف في الإرسال الفوري والمجدول و`drip`. نجاح معروف يمنع التكرار، وفشل HTTP مؤكد وحده قابل لإعادة المحاولة، أما انقطاع الشبكة فيُسجل `unknown` ولا يُعاد تلقائيًا لأن هاتف قد يكون استلم الطلب.
- هوية الإرسال المنطقية = `(اسم الحملة + القالب + الهاتف + idempotency_ref)` ولا تعتمد على كونه فوريًا أو مجدولًا. `idempotency_ref` هو مفتاح المتجر في وضع «رسالة لكل متجر» حتى لا يحجب متجران صحيحان يشتركان في هاتف واحد.
- **مركز هاتف `/whatsapp-settings` (2026-07-31):** مساحة عمل واحدة بتبويبات URL ثابتة: نظرة عامة · الحملات والرسائل · التحصيل المرتبط · المكالمات وIVR · نشاط فريق هاتف · جودة التواصل · الربط والأتمتة. الإعدادات لا تُحمّل ولا تظهر إلا مع `whatsapp.configure`، وبقية التبويبات تُرشّح بالصلاحية. «التحصيل المرتبط» ارتباط زمني خلال 14 يومًا وليس عائدًا منسوبًا للحملة، و«نشاط الفريق» ليس SLA ما لم تصل أزمنة الإسناد/الرد الموثوقة من هاتف.
- الصفحة تشرح حدود التكامل داخل قرار الاستخدام نفسه: الردود والإسناد في هاتف، ولمحة للقياس والحملات وIVR. لا تُعِد تسمية المركز إلى «صندوق وارد» ولا تضف قوائم محادثات أو Leads من الردود.

---

## 17. مركز قيادة 500 عميل نشط + Webhook المتاجر ✅ (2026-07-31)

- `/retargeting?tab=activation` يقيس الهدف على **العميل الفريد بالهاتف** لا على عدد صفوف المتاجر. تعريف النشط ثابت: آخر شحنة خلال 5 أيام، والهدف الافتراضي 500 عميل.
- `customer_activation_command_center` هو مصدر لوحة القيادة: الحالي والفجوة والحركة الأسبوعية والدخول المطلوب أسبوعيًا وفجوات تنفيذ الفريق والنتيجة الموضوعية من أحداث أول شحنة/عودة شحن. لا تكتب معادلة بديلة في الواجهة.
- أرقام «لم نتواصل/بلا مسؤول/تواصل بلا موعد» هي دين تنفيذ على مخزون الفرص وليست عدد مكالمات اليوم. قائمة اليوم تبقى دفعة صغيرة ومملوكة بموعد تالٍ وفق §14.
- `platform-merchants-webhook` يقبل **Snapshot كامل فقط** (`mode='full'`) ويستبدل رفع Excel كمصدر تلقائي، ثم يشغّل نفس محرك `capture_merchant_lifecycle_events` داخل معاملة الإدخال. لا تنشئ مسار أحداث جزئية موازياً قبل تصميم صريح له.
- توقيع القناة HMAC-SHA256 على `timestamp + '.' + exactRawBody` عبر سر بيئي لا يُحفظ في المستودع؛ نافذة التوقيع 5 دقائق و`event_id` مفتاح idempotency. إعادة نفس الطلب آمنة، وإعادة المعرّف بجسم مختلف تعارض `409`.
- الإدخال ذري: إيصال `platform_snapshot_receipts` + كل صفوف `merchants` + أحداث دورة الحياة في معاملة واحدة. حارس الحجم يرفض Snapshot ناقصًا خارج 60%–150% من حجم اللقطة الحالية، والجدول مقفول لـservice role فقط.
- رد واتساب أو مكالمة هاتف لا يفتح Lead ولا يغيّر نتيجة المبيعات. نجاح المبيعات يُثبت موضوعيًا عندما تظهر أول شحنة أو عودة شحن في Snapshot المنصة، ويمكن فقط نسبته لتواصل مسجل سابق.

---

## 18. اعتمادية تكامل Zoho Books ✅ (2026-07-31)

- `zoho_webhook_inbox` هو صندوق الوارد الدائم لأحداث زوهو: بصمة SHA-256 للنص الخام تمنع التكرار، والفاشل أو `processing` المتوقف لأكثر من دقيقتين قابل للاستعادة. نجاح المعالجة فقط يُختم `processed`، والخطأ المهم يرجع `500` لتمكين إعادة المحاولة.
- Webhook الفواتير/الدفعات/الإشعارات يستخدم `last_modified_time` القادم من زوهو إن توفر، ولا يسمح لحدث أقدم بدهس صف أحدث. كل خطأ `upsert` يُفحص صراحةً؛ لا تعتمد على `try/catch` وحده مع Supabase client.
- `zoho_sync_runs` يسجل كل مزامنة: المصدر، الحالة، نتائج كل كيان، عدد استدعاءات API والمدة. الفشل الجزئي لا يُعلن نجاحًا كاملًا، ولا يتقدم `last_sync` للكيان الذي فشل.
- سقف الـ25 صفحة القديم أُلغي. حد الأمان الحالي 500 صفحة (100,000 سجل/كيان)، وإذا لم تكتمل الصفحات يفشل الكيان ولا تُحفظ نقطة مزامنة كاذبة. `full:true` يعيد البناء كاملًا مع نفس الحارس.
- مرايا الفواتير/الدفعات/الإشعارات تحفظ `customer_id`، ومرايا فواتير/دفعات الموردين تحفظ `vendor_id`. الاسم للعرض؛ المعرّف هو أساس الربط المستقبلي.
- أي كتابة «تطبيق رصيد» تمر أولًا عبر `zoho_write_operations` بمفتاح idempotency من الواجهة. النجاح/النتيجة الجزئية/الفشل غير المؤكد لا يُعاد آليًا بنفس المفتاح؛ حالة `unknown` تتطلب مراجعة زوهو قبل محاولة جديدة.
- تنفيذ تطبيق الرصيد **للمدير فقط** في الخادم والواجهة. `plan` يبقى قراءة للمصرح لهم. لا توسّع صلاحية الكتابة قبل قرار صريح واختبار RLS/الحارس الخادمي.
- الجداول الثلاثة الجديدة (`zoho_webhook_inbox`/`zoho_sync_runs`/`zoho_write_operations`) مقفلة عن `anon` و`authenticated` وممنوحة لـ`service_role` فقط. لا تعرض الحمولة الخام أو سجل الكتابة مباشرةً في الواجهة؛ ابنِ RPC ملخصًا محدودًا عند إضافة شاشة صحة.
- تضييق SELECT على مرايا زوهو القديمة **مؤجل بقصد** حتى تُنقل الشاشات المعتمدة عليها إلى RPCs محددة؛ لا تسحب السياسة العامة دفعة واحدة فتتعطل المبيعات والتحصيل والبحث.
- **OAuth زوهو (2026-08-01):** رابط الموافقة يحمل `state` موقّعًا قصير العمر ومربوطًا بالمدير الذي بدأ الربط، و`exchange_web` يرفض الكود إن غاب أو فشل التحقق. صفحة callback لا تستخدم `force:true` تلقائيًا؛ تعرض تأكيد استبدال صريح لأن كود زوهو صالح لدقيقتين فقط. إذا أعاد Zoho أكثر من مؤسسة نشطة فلا يُختار أول صف من الـAPI: يُحفظ الـrefresh token مؤقتًا (10 دقائق) في `zoho_oauth_pending_grants` المحمية للـservice role فقط، ويختار المدير المؤسسة صراحةً، ثم يُستهلك الطلب ذريًا ويُحذف الصف. الإلغاء يحذفه فورًا وكرون كل 15 دقيقة ينظف الطلبات المنتهية/المهجورة. المؤسسة الوحيدة تُربط مباشرة، وعند إعادة الموافقة تُفضّل `org_id` الحالية في واجهة الاختيار إن بقيت مصرحًا بها.

---

## 19. التاريخ الميلادي وصحة سجل مكالمات هاتف ✅ (2026-08-01)

- كل عرض عبر `Date.toLocaleString` / `toLocaleDateString` / `toLocaleTimeString` يُحقن له `timeZone='Asia/Riyadh'` و`calendar='gregory'` من `src/main.jsx`. اللغة العربية لا تعني تقويم أم القرى؛ تواريخ المنتج ميلادية دائمًا. أي تنسيق خادمي مستقل يجب أن يحدد `ar-SA-u-ca-gregory` والمنطقة صراحةً.
- أرقام موظف هاتف في `hatif_call_agent_stats`: المكالمات = كل الصفوف، المردودة = `pickup_time` غير فارغ، الوارد = `call_type=1`، الصادر = `call_type=2`، مدة التحدث = مجموع `talk_seconds`، وآخر مكالمة = أحدث `creation_time`. التجميع بمعرّف `user_id` الثابت، والاسم من قائمة مستخدمي هاتف.
- `آخر مكالمة` يصف نشاط الاتصال، أما `آخر مزامنة` فيصف صحة الربط ويؤخذ من أحدث `synced_at` في `hatif_call_log`. لا تستخدم أحدهما بدل الآخر في الحكم على توقف التكامل.
- `hatif-pull-calls` يعمل كل 5 دقائق، idempotent بمعرّف المكالمة، ويفشل صراحةً عند فشل توكن/API/كتابة قاعدة البيانات. تبويب نشاط الفريق يعيد القراءة تلقائيًا كل دقيقة ويعرض حالة المزامنة؛ هذا تحديث شبه لحظي بحد أقصى تشغيلي يقارب 5 دقائق، وليس بثًا لحظيًا.

---

## 20. إدارة المسوّقين ونقطة التعادل (`/marketers`) ✅ (2026-08-01)

- **المقصود بالتعادل:** لا يدخل ربح لمحة في الحساب. تكلفة المسوّق الشهرية = الراتب + عمولته المتدرجة، وتكلفة الطلب الفعلية = إجمالي تكلفة المسوّق ÷ الطلبات المؤهلة. نقطة التعادل هي أول عدد طلبات يجعل التكلفة الفعلية ≤ التكلفة المستهدفة للطلب التي يحددها المدير لكل مسوّق.
- **العقود المدعومة:** راتب فقط · عمولة فقط · راتب + عمولة · شرائح عمولة متصلة تبدأ من الطلب 1 وتنتهي بشريحة مفتوحة. إذا كانت عمولة الشريحة المفتوحة ≥ التكلفة المستهدفة مع راتب غير مغطى فالتعادل «غير ممكن» ويُعرض صراحة.
- **التوليد التلقائي للشرائح:** عند وجود راتب وسقف تكلفة، يبني `buildAutomaticCommissionTiers` أربع شرائح بنسب 50% ثم 65% ثم 80% ثم 90% من السقف. عرض الشريحة الأولى = `ceil(الراتب ÷ (السقف − عمولة الأولى))`، والبقية بنفس العرض، والأخيرة مفتوحة. حدود `from_order/to_order` شاملة؛ لذلك بداية التالية = نهاية السابقة +1، ولا تُكتب يدويًا. التخصيص اليدوي مسموح، لكن العمولات لا تنخفض وآخر عمولة يجب أن تبقى أقل من السقف؛ الحارس موجود في الواجهة و`marketing_assert_plan_tiers` معًا.
- **التاريخ لا يُعاد كتابته:** كل تعديل تعويض ينشئ خطة جديدة بـ`effective_month`. لا تُعدّل خطة قديمة ولا نتائج شهر مقفل.
- **المسودة مقابل الحكم:** عدد الطلبات يُسجل أولاً كمسودة ويعطي توقعاً فورياً، لكنه لا يغيّر لون المسوّق. `marketing_close_month` لا يقبل الشهر الجاري، ويثبت snapshots ثم يغيّر الحالة رسمياً.
- **حركة الحالة:** إخفاق: أخضر → أصفر → أحمر → متوقف. نجاح: أحمر → أصفر → أخضر، والأخضر يبقى أخضر. الأشهر يجب أن تُقفل زمنياً حتى لا تتشوّه الحركة.
- **الصلاحيات:** `marketers.view` · `marketers.manage` · `marketers.record_month` · `marketers.close_month`. العمليات الذرية تعمل عبر RPCs محمية بـ`crm_has_permission`، والجداول كلها RLS ولا وصول لـ`anon`.
- **نقطة الحقيقة:** `src/lib/marketersService.js` للحساب الحيّ، و`marketing_variable_commission` + `marketing_break_even_orders` + `marketing_close_month` للحكم الموثّق في القاعدة.

---

## 21. نبض تشغيل الفرق في لوحة القرارات ✅ (2026-08-01)

- `company_operating_pulse()` طبقة **قراءة وقيادة فقط** فوق مصادر العمل الحالية؛ لا تنشئ جدول مهام رابعاً ولا تنسخ العملاء بين الأنظمة. المبيعات تبقى في `retargeting_followups`/`crm_leads`، والتحصيل في `collection_tasks`، والدعم في `support_tickets`.
- `/decisions` يعرض «نبض تشغيل الفرق» قبل إشارات القرار المالية: **عمل اليوم · المخزون · المتأخر/الوعود** لكل فريق، مع دين التنفيذ (بلا مسؤول/بلا موعد) والتنقل إلى مساحة العمل الأصلية.
- عمل المبيعات اليوم = دفعة فرص المنصة القصوى 12 + المتابعات والإجراءات المستحقة خلال 12 ساعة. عمل التحصيل اليوم = أول 25 حساباً من طابوره المرتب. عمل الدعم اليوم = الاتحاد المميز للعاجل أو غير المسند أو بلا موعد أو المستحق خلال 24 ساعة. **لا تعرض إجمالي المخزون بصياغة توحي أنه مطلوب اليوم.**
- الصلاحيات تُحسم داخل RPC: `overview.view` للوحة، ثم لا يعاد قسم فريق إلا مع صلاحية ذلك الفريق. نطاق المبيعات/التحصيل يكون `team` لمن يملك الرؤية الشاملة، وإلا `mine`. الدعم يعكس لوحة الدعم الإدارية المشتركة.
- `financial_hold_conflicts` فحص حوكمة لا فرصة بيع: يحصي فقط متابعات المبيعات المفتوحة التي أصبحت على حساب محجوز مالياً. الحارس يمنع إنشاء تعارض جديد؛ النبض يكشف البقايا التاريخية ولا يغلقها أو يغيّر ملكيتها صامتاً.
- تحميل النبض مستقل ومتدرّج: يظهر فور اكتمال RPC ولا ينتظر أبطأ استعلام مالي في لوحة القرارات، وتُحفظ آخر نتيجة 90 ثانية داخل `sessionStorage` بمفتاح يحمل معرّف المستخدم كي تكون العودة السريعة فورية بلا تسريب نطاق مدير لموظف. زر التحديث يتجاوز الكاش. فشل RPC لا يُسقط اللوحة: يظهر خطأ مستقل مع إعادة محاولة، وتبقى بقية الإشارات ظاهرة. نقطة الواجهة: `src/lib/companyOpsService.js` + `OperatingPulse` داخل `DecisionsBoard.jsx`.

---

## 22. أداء القراءة ووضوح مسارات العمل ✅ (2026-08-01)

- **ممنوع تجميع لوحة CRM بتنزيل كل الفرص للمتصفح.** `crm_board_stats()` يعيد المجاميع المطلوبة مباشرة من Postgres ويحافظ على نطاق RLS؛ `/crm?tab=board` يعرض تحميلًا وخطأً مستقلين مع إعادة محاولة.
- **مساحة CRM الرسمية:** الصفقات والمواعيد وأداء المبيعات فقط. قائمة الاتصال اليومية وفرص المتاجر الجديدة/المتوقفة تبقى في `/retargeting`، وردود واتساب تبقى داخل هاتف. المسار القديم `?tab=queue` يُحوّل إلى الصفقات ولا يُنشئ قائمة عمل موازية.
- **رد هاتف لا يساوي مهمة CRM.** أُلغي توليد/عرض مهام `↩️ ردّ وارد من … — تابِعه الآن`، وأُغلقت المتبقية القديمة فقط. لا يشمل ذلك طلب معاودة اتصال IVR الصريح ولا أي مهمة أنشأها الموظف.
- `forecast_receivables_rollup()` يجمع أعمار الذمم في الخادم، و`carrier_open_balances_all()` يعيد أرصدة كل الناقلين باستدعاء واحد. شاشة التوقع لا تعيد تنزيل آلاف فواتير زوهو ولا تنفّذ استدعاءً منفصلًا لكل ناقل، وتملك مهلة 15 ثانية برسالة تحدد المصدر المتأخر.
- `global_entity_search()` هو بحث الخادم عن المتاجر وفواتير زوهو؛ بحث AWB يبقى من مصدره المتخصص. لا تعرض لوحة البحث «لا نتائج» قبل اكتمال الاستعلام البعيد، ولا تعرض حالة زوهو الإنجليزية الخام.
- **البنوك:** العدد التشغيلي المتوقع ثلاثة حسابات. إن لم يتوفر حساب في المصدر، تعرض الرئيسية «حساب غير مسجل» وعدد `المسجل/المتوقع`؛ ممنوع اختلاق رصيد أو مضاعفة رصيد بنك واحد. السيولة تعرض رقمين مختلفين بوضوح: قبل حجز الضريبة، والمتاح بعد حجز ضريبة الربع، مع جسر يبيّن التركيب.
- **رصيد بلا فاتورة:** إذا كان رصيد العميل موجودًا وعدد فواتير زوهو المفتوحة صفرًا، تعرض الواجهات «رصيد افتتاحي بلا فاتورة مفتوحة» بدل «0 فاتورة» أو عمر فاتورة وهمي.
- **تواريخ المنتج:** `src/main.jsx` يفرض الرياض + الميلادي + الأرقام اللاتينية على دوال `toLocale*`. عند غياب locale يستخدم `en-GB` لتجنب ترتيب الشهر/اليوم الأمريكي. التنسيقات العربية الصريحة تستخدم `ar-SA-u-ca-gregory-nu-latn`.
- **سجل التقارير:** لا يعرض تاريخًا مفتوحًا بالكامل؛ آخر 100 سجل فقط مع فلتر نوع وتقسيم 10 سجلات للصفحة، والأحدث أولًا. إجراءات صف دفتر الناقل تُظهر الإيجابي الرئيسي وتجمع الاستثناءات داخل «المزيد».

---

## 23. التزامات معاودة الاتصال عبر هاتف ✅ (2026-08-01)

- هاتف يبقى مكان إجراء المكالمات والمحادثات؛ `hatif_call_commitments` سجل قياس إداري فقط، ولا ينشئ Lead أو محادثة أو قائمة عمل مستقلة.
- `hatif-pull-calls` يطبّع رقم العميل في `hatif_call_log.contact_phone`، ويستخرج الموعد فقط عند وجود طلب اتصال مستقبلي واضح في `ai_summary`. الموعد الغامض يُوسم `needs_confirmation` ولا يتحول إلى حكم آلي.
- نافذة الاتصال تُحسب بتوقيت الرياض والميلادي انطلاقًا من وقت المكالمة المصدر. المثال «غداً بين 10 و12» يُحفظ كنافذة لا كنقطة زمنية واحدة.
- `evaluate_hatif_call_commitments()` يطابق الوعد بمكالمة **صادرة فعلية** من هاتف: في الموعد وتم الرد · حاول في الموعد ولم يرد العميل · متأخر · لم يتصل. ضغط زر اتصال داخل لمحة لا يدخل القياس.
- المكالمة المبكرة لا تُحتسب وفاءً لنافذة طلبها العميل، والمكالمة المتأخرة يمكن أن ترفع الحالة من «فائت» إلى «تم متأخرًا» خلال يوم واحد. اسم المنفذ الفعلي يؤخذ من `hatif_call_log.user_name` ويظهر اختلافه عن الموظف المتوقع.
- `/crm?tab=tasks` يجمع المواعيد الداخلية مع لوحة التزامات المقاسة دون خلط المصدرين، وسجل العميل الموحد يعرض المكالمات الصوتية اليدوية واسم الموظف من سجل هاتف الكامل.
- التشغيل المجدول يتوقف عند أول صفحة مكالمات معروفة. لإعادة استخراج التزامات قديمة يشغّل المدير `hatif-pull-calls?backfillCommitments=true&maxPages=N`؛ الحد الأقصى 100 صفحة، ويقيّم آخر 90 يومًا، ولا يتغير سلوك الكرون المعتاد.

---

## 24. الرقابة المالية الموسعة من Zoho Books ✅ (2026-08-01)

- التوسعة البنكية **قراءة ومطابقة فقط**: OAuth يطلب `ZohoBooks.banking.READ` و`ZohoBooks.debitnotes.READ` (اسم نطاق Vendor Credits الرسمي)، ولا يطلب `banking.CREATE/UPDATE` ولا يستورد كشفًا إلى زوهو. أي كتابة بنكية مستقبلية قرار ومرحلة مستقلة.
- `zoho_bank_accounts` و`zoho_chart_accounts` و`zoho_vendor_credits` مرايا يكتبها `service_role` فقط. القراءة للمستخدم المصرح له بـ`zoho.view` أو`money.pnl` عبر RLS وGRANT صريح؛ لا وصول لـ`anon`.
- الربط بين حساب زوهو والبنك الداخلي أو خزينة الناقل **صريح يدويًا** في `zoho_financial_account_links`؛ ممنوع المطابقة الضبابية لأسماء الحسابات. التعديل يمر عبر `zoho_set_financial_account_link` وصلاحية `zoho.configure` الحساسة.
- `zoho_financial_control_dashboard()` مصدر ملخص الرقابة: حسابات البنوك والفرق بين الدفتري والتغذية، صافي الموردين، الأرصدة الدائنة، الخزائن المربوطة، حالة الصلاحيات واستهلاك API. لا تجمع الشاشة هذه المجاميع بتنزيل كامل الجداول.
- **رصيد المورد ليس فاتورة مفتوحة:** شاشة الرقابة تعرض أربعة أرقام منفصلة: رصيد فواتير الموردين المفتوحة من `zoho_bills` · إجمالي الذمم على الموردين من `zoho_contacts.outstanding_payable` · السلف/الدفعات الزائدة والأرصدة لصالحنا من `unused_credits_payable` · الصافي بعد المقاصة. إشعارات `zoho_vendor_credits` المفتوحة جزء مستقل ولا يجوز وصفها بأنها كامل الرصيد لصالحنا. `vendor_positions` في RPC يعرض القرار لكل مورد (مطلوب دفعه/رصيد لنا/مصفّى).
- مقارنة البنك ثلاثية لكل حساب مربوط: أحدث كشف/إدخال في لمحة × الرصيد الدفتري في زوهو × رصيد التغذية البنكية. فهرس فريد يمنع ربط حسابي زوهو باسم البنك الداخلي نفسه كي لا يتضاعف الإجمالي.
- جدول الحسابات يعرض للمستخدم «رصيد زوهو الدفتري» و«الرصيد الختامي للبنك» و«الفرق عن زوهو» كقيم منفصلة. الرصيد الختامي مصدره أحدث `bank_statement_summaries.closing_balance` أو `bank_balance_log.balance` ويظهر معه المصدر والتاريخ؛ لا تعرض صفر التغذية البنكية على أنه رصيد ختامي.
- مساحة `/zoho-data` تبدأ بخمسة تبويبات عمل ثابتة أعلى المحتوى: نظرة عامة · العملاء والفواتير · الموردون والمصروفات · البنوك والمطابقة · القيود والحسابات. النظرة العامة تعرض أعلى 5 موردين وأعلى 5 حسابات تحتاج انتباهًا فقط؛ الجداول الكاملة لا تظهر إلا داخل قسمها، فلا تُدفن التبويبات بعد صفحة طويلة على الجوال.
- دليل الحسابات يُعرّب في العرض والتصدير: الحالة (`active` ← «نشط»)، أنواع الحسابات، وأسماء حسابات زوهو القياسية. يبقى الاسم الإنجليزي الأصلي أسفل الترجمة للمراجعة المحاسبية، ولا يُغيَّر الاسم داخل زوهو. حسابات الضرائب/حقوق الملكية وغيرها تعرض «لا يحتاج ربطًا بنكيًا»؛ إجراء الربط/التصنيف محصور في حسابات البنك والنقد.
- كل صف من endpoint البنوك ليس بنكًا فعليًا: قد يكون خزينة تشغيلية أو بوابة دفع أو صندوقًا. بادئة «خزينة» تصنيف عرض حتمي فقط ولا تنشئ ربطًا؛ الربط المالي يبقى يدويًا. `bank_balance=0` لا يُعرض كرصيد فعلي إن لم توجد إشارة feed (`feed_status/last_refreshed_at`)؛ يعرض «غير متصل» بدل صفر مضلل. العدد البنكي في KPI = المرتبط صراحةً من أصل الثلاثة المتوقعة (§22).
- `zoho_integration_capabilities` يفرق بين عطل التكامل وبين `needs_reauthorization`. نقص صلاحية بنكية جديدة لا يُسقط مزامنة الفواتير القائمة ولا يُعلن النظام معطلًا.
- كل استدعاء في مسار المزامنة يمر بـ`_shared/zohoClient.ts`: مهلة، وعدّ لكل محاولة، وإعادة محاولة محدودة لـ429/5xx مع احترام `Retry-After`. `zoho_api_daily_usage` يسجل الاستهلاك اليومي؛ `ZOHO_DAILY_API_BUDGET` اختياري للعرض ولا يُخمن تلقائيًا من خطة العميل.
- تنزيل المستندات يمر عبر `zoho-sync?action=download_document` المحمي ولا يكشف OAuth للمتصفح: فاتورة العميل تستخدم PDF الرسمي (`invoices/{id}?accept=pdf`)، وفاتورة المورد تستخدم المرفق الأصلي الرسمي (`bills/{id}/attachment`) وقد يكون PDF أو صورة أو Excel. لا تستخدم endpoint غير موثق لتوليد PDF لفاتورة المورد، ولا تحفظ الملف في Storage تلقائيًا.
- الكيانات الثقيلة ذات السحب الكامل لا تُعاد كل 30 دقيقة: جهات الاتصال كل ساعتين، والمصاريف/الإشعارات الكاملة كل 6 ساعات، مع تجاوز صريح عبر `full/force`. المزامنة الدورية المعتادة تشمل الطبقة المالية عندما يحين وقتها.

---

## 25. الإرسال الليلي إلى زاتكا عبر تكامل Zoho ✅ (2026-08-02)

- `zatca-auto-push` لا يتكامل مع زاتكا مباشرة؛ يرسل الفاتورة الموجودة إلى Fatoora عبر endpoint زوهو الرسمي `POST /invoices/{id}/einvoice/push`. التوقيع وXML والربط التنظيمي تبقى مسؤولية Zoho Books.
- يعمل `zatca-auto-push-2345-riyadh` يومياً عند `20:45 UTC` = `23:45 Asia/Riyadh`. يستدعي Edge Function بمفتاح `X-Cron-Key` من `zoho_auth`؛ لا يُخبز OAuth أو service-role في مهمة cron.
- المرشح المحلي هو `zoho_invoices.einvoice_status='yet_to_be_pushed'` وتاريخه حتى اليوم السعودي، بحد أقصى 200 للدورة. قبل كل كتابة تُقرأ الفاتورة حياً من Zoho، وكل فاتورة تُرسل منفردة حتى لا يخفي نجاح المجموعة فشل سجل واحد.
- `ZohoBooks.invoices.CREATE` مطلوب من Zoho لعملية push رغم أن النظام لا ينشئ فاتورة. أي إعادة تفويض بعد هذا التاريخ يجب أن تشمل النطاق، ويبقى `zoho-authurl` للمدير فقط.
- منع التكرار دائم عبر `zoho_write_operations.idempotency_key='zatca_push:<invoice_id>'`. النجاح لا يعاد إرساله، والفشل يعيد استخدام السجل نفسه مع آخر خطأ ووقت المحاولة.
- مستندات «الرصيد الافتتاحي» مستثناة حتمياً من الإرسال التلقائي وتظهر كـ`opening_balance_requires_manual_review`؛ ليست بيعاً جديداً ولا يجوز خلق حدث ضريبي منها. لا تحذف هذا الحارس أو توسّع الإرسال إلى حالات أخرى بلا قرار محاسبي صريح.
- التشغيل اليدوي الآمن هو `{action:'preview'}` ولا يكتب في زوهو. الاستجابة التشغيلية تفصل `pushed/skipped/failed/excluded`، والفشل الجزئي يرجع HTTP 207.

---

## 26. دورة الفاتورة والكتابة البنكية المضبوطة في Zoho ✅ (2026-08-02)

- بوابة الكتابة الوحيدة للواجهة هي `zoho-operations`، محمية بمدير أو `zoho.configure`. لا تنشئ فواتير ولا تحذف مستندات، وكل كتابة تمر عبر `zoho_write_operations` بمفتاح idempotency ونتيجة مستقلة.
- صفحة `/zoho-data` تعرض `einvoice_status` بالعربية وتدعم تحديداً جماعياً لعمليتين منفصلتين: **تحويل المسودات المحددة إلى مرسلة** (`POST .../status/sent`) و**إرسال المحدد إلى زاتكا عبر زوهو** (`POST .../einvoice/push`). لا يُخلط تغيير الحالة بإرسال البريد للعميل.
- إرسال زاتكا اليدوي يقبل فقط `yet_to_be_pushed`، ويستبعد الرصيد الافتتاحي مثل الكرون. الفشل يظهر لكل فاتورة ويمكن تحديدها وإعادة المحاولة؛ النجاح السابق لا يعاد.
- استيراد البنك يتطلب ربطاً صريحاً `link_kind='bank'` و`internal_bank_name`. المعاينة تجلب العمليات غير المستوردة فقط وتعرض العدد والإيداعات والسحوبات والمكرر؛ التنفيذ لا يقبل بلا `transaction_ids` صريحة من المعاينة.
- استيراد الكشف يستخدم `POST /bankstatements` ونطاق `ZohoBooks.banking.CREATE`. لا يصنف العملية ولا ينشئ مصروفاً/دفعة عميل تلقائياً؛ المطابقة المحاسبية مرحلة مراجعة لاحقة. منع التكرار على مستوى العمليات من سجلات الاستيراد الناجحة.
- معاينة استيراد البنك تبدأ من endpoint زوهو الرسمي `GET /bankaccounts/{account_id}/statement/lastimported`: تعرض آخر رقم مرجعي وتاريخه، ثم تقترح العمليات الأحدث فقط. إذا لم يوجد المرجع في الدفتر المحلي، تبدأ من تاريخه وتستبعد كل معرفات ومراجع آخر كشف في زوهو؛ سجل `zoho_write_operations` يبقى حارس تكرار ثانياً. ممنوع الرجوع صامتاً إلى كامل تاريخ البنك عند وجود مرساة زوهو.
- `ZohoBooks.invoices.CREATE` مطلوب من Zoho لـe-invoice push، و`banking.CREATE` مطلوب لاستيراد الكشف؛ بعد إضافة النطاقين يجب إعادة التفويض مرة واحدة قبل أول تنفيذ.
- صحة Webhooks تعرض الأحداث `failed/processing` للمدير. «إعادة مزامنة هذا الحدث» لا تعيد حقن payload قديم؛ تشغّل مزامنة Zoho كاملة موثقة ثم تقفل الحدث عند النجاح.
- مركز التقارير يدعم بالإضافة للسابق: الميزانية العمومية، التدفق النقدي، ميزان المراجعة ودفتر الأستاذ عبر قائمة endpoints بيضاء في `zoho-reports`. إن رفضت خطة/دولة Zoho تقريراً يُعرض خطؤه ولا تُقدّر الأرقام محلياً.
- مركز التحصيل اسمه الرسمي «مديونيات العملاء»، وتبويباته تُسمّى بنتيجة العمل: نظرة عامة وأعمار الدين · إجراء اليوم ووعود السداد · الحالات القانونية · الأرصدة والمطابقة.
- حسابات Zoho التي يبدأ اسمها بـ«خزينة» تبقى خزائن تشغيلية حتى لو أعادها endpoint البنوك بالنوع التقني `bank`: لا تعرض لها رصيد كشف بنك أو فرقًا بنكيًا، ولا تسمح بربطها ببنك داخلي أو استيراد كشف؛ تُصنّف فقط كخزينة ناقل أو نقد/صندوق.
- على الجوال، `.page-slot` هو عنصر التمرير الوحيد ويجب أن ينتهي بفاصل فعلي بعد المحتوى فوق `.bottom-nav`؛ الاعتماد على `padding-bottom` وحده غير كافٍ في Safari مع flex/absolute.
- المودال المشترك يُرسم عبر portal مباشر إلى `document.body` حتى لا يصبح `position:fixed` تابعًا لـ`.page-slot` القابل للتمرير؛ الرأس وزر الإغلاق يبقيان خارج `.modal-body` المتحرك.
- مرساة استيراد البنك: آخر كشف مستورد أولاً، ثم أحدث عملية في `/banktransactions` للحساب. إن لم توجد أي مرساة في زوهو فالنتيجة الآمنة صفر مع `anchor_required=true`؛ ممنوع اعتبار كامل التاريخ المحلي «عمليات جديدة».
- إذا وُجد صف في `zoho_bank_import_anchors` فهو قرار إداري يدوي ويتقدّم حتمًا على آخر كشف وأحدث عملية في زوهو. لا تستبدل مرجعًا يدويًا تلقائيًا؛ تبدأ المعاينة بعد مطابقته في `bank_transactions` وتعرض مصدره `manual_reference` بوضوح.
- الرئيسية تعرض «أرصدة البنوك في زوهو» من `zoho_financial_control_dashboard` المحلي فقط: الحسابات ذات `display_kind='bank'` والربط الصريح، مع الرصيد الدفتري والختامي والفرق والتاريخ والإجمالي. الخزائن لا تدخل، وفتح الرئيسية لا يستدعي Zoho API حيًا.
- إجراء الحساب البنكي في `/zoho-data` اسمه «فحص العمليات الجديدة» لا «استيراد العمليات». مرساة البداية وعدد المستبعدات تفاصيل تقنية مخفية؛ عند صفر يظهر «كل العمليات مرحّلة إلى زوهو»، ولا يظهر زر الاستيراد داخل المودال إلا إذا كانت المعاينة تحتوي عمليات جديدة فعلًا.

## 27. عملاء الحملات من Google Sheets إلى CRM وهاتف ✅ (2026-08-02)
- بوابة الاستقبال الوحيدة هي Edge Function `google-lead-intake` (ويب هوك خارجي، `verify_jwt=false` مع HMAC SHA-256 على `timestamp.rawBody` ومهلة 5 دقائق). `event_id` إلزامي، و`campaign_lead_inbox(source,event_id)` يمنع تكرار الاستجابة.
- التدفق يكتب في `crm_leads` كـ`lead_kind='inbound'` و`source_channel='google_sheets'`، ويعيد استخدام شاشة المبيعات وSLA أول استجابة 15 دقيقة. لا ينشئ جدول عملاء أو CRM موازياً.
- التوزيع دائري فقط على ملفات `profiles.accepts_campaign_leads=true` ذات `lead_notification_phone` وصلاحية مبيعات. يُنشأ `crm_tasks` للتواصل الأول؛ غياب موظف مؤهل لا يسقط الـLead بل يتركه بلا إسناد مع سبب واضح.
- إشعار الموظف يستخدم قالب هاتف المحدد في `HATIF_NEW_LEAD_STAFF_TEMPLATE` ومتغيراته بالترتيب: اسم العميل، الجوال، المدينة، النشاط، الحملة، رابط ملف الـLead. فشل الإشعار لا يرجع إنشاء العميل ولا يكرر الاستقبال.
- **حارس Zoho القطعي:** هذا التدفق لا يستدعي Zoho ولا ينشئ/يعدّل جهة اتصال أو عميلًا فيه. إنشاء عملاء Zoho يبقى مسؤولية نظام المنصة الحالي؛ ShipAudit يربط قراءةً فقط بعد ظهور العميل من المصدر المسؤول.
- سكربت الربط المرجعي موجود في `docs/google-campaign-leads-apps-script.js` ويقرأ الأسرار من Google Script Properties، لا من خلايا الورقة.

## 28. مركز وكلاء العمل — الأساس الآمن ✅ (2026-08-02)
- المسار `/work-agents` هو نقطة الحقيقة المرئية لتعريف الوكلاء وحالتهم وجدولهم ومستوى الأمان ومصادرهم وسجل التشغيل.
- الجداول `work_agents` و`work_agent_runs` تحفظ التعريفات وسجل التنفيذ. سجل التشغيل للقراءة من الواجهة؛ الكتابة التشغيلية المستقبلية تكون من الخدمة السحابية فقط.
- كل الوكلاء تبدأ `status='draft'`. ظهور موعد مقترح في البطاقة لا يعني وجود Cron نشط؛ لا يتحول الوكيل إلى `active` قبل بناء منفذه واختبار المعاينة ومنع التكرار وتحديد المسؤول.
- مستويات الأمان: `monitor` قراءة وتنبيه، `limited` إجراء آمن محدود، `approval` ينتظر اعتماد المسؤول، `sensitive` يحتاج صلاحية واعتمادًا صريحًا.
- الصلاحيات: `agents.view` للعرض، `agents.manage` للإعداد، `agents.run` للتشغيل اليدوي، و`agents.approve_sensitive` للاعتماد الحساس. المدير يتجاوز JSONB كالمعتاد.
- قاعدة ثابتة: الذكاء الاصطناعي يقترح ويصنف؛ التنفيذ المالي والحذف والكتابة الخارجية تمر بقواعد حتمية وصلاحيات وidempotency وسجل تدقيق.

### 28.1 الوكيل الأول: سداد الفواتير المتأخرة ✅ (2026-08-02)
- الوكيل `overdue_sadad` يعمل افتراضيًا كل سبت الساعة 18:00 بتوقيت `Asia/Riyadh` (`0 15 * * 6` بتوقيت UTC)، ويمكن تشغيله/إيقافه وإعادة جدولته من `/work-agents`.
- يقرأ `zoho_invoices` فقط، ويختار الفواتير ذات الرصيد المتبقي التي تجاوز تاريخ استحقاقها 30 يومًا، ثم يصل للهاتف عبر `customer_merchant_links` وآخر snapshot من `merchants`. لا يكتب في Zoho ولا ينشئ عميلًا فيه.
- قالب هاتف هو `sadad`، ومتغيراته: اسم المتجر، إجمالي المستحق، وعدد الفواتير. الإرسال يمر عبر `campaign_queue` و`campaign-runner` ليرث الحظر وتطبيع الهاتف وحد السرعة وسجل الإرسال.
- مفتاح منع التكرار هو `work-agent:overdue-sadad:<cycle>:<phone>`؛ إعادة المحاولة في دورة واحدة لا تعيد الرسالة، والدورة الأسبوعية التالية تسمح بتذكير جديد إن بقي الدين.
- التشغيل اليدوي يفرض معاينة ثم تأكيدًا صريحًا. صلاحية الإعداد `agents.manage` والتشغيل `agents.run`، وكل دورة تسجّل في `work_agent_runs`.
- نقطة الحقيقة التنفيذية: `supabase/functions/work-agent-overdue-sadad/index.ts`، والشروط والجدولة في `work_agents.config` وRPC `configure_overdue_sadad_agent`.

### 28.2 الوكيل الثاني: زاتكا الليلي ✅ (2026-08-02)
- الوكيل `zatca_nightly` هو الواجهة التشغيلية لمهمة `zatca-auto-push` الحالية؛ يعمل يوميًا 23:45 بتوقيت السعودية ويمكن تشغيله/إيقافه وإعادة جدولته من `/work-agents`.
- المصدر `zoho_invoices.einvoice_status='yet_to_be_pushed'`. قبل كل كتابة يجلب الفاتورة حيًا من Zoho، ثم يرسلها إلى ZATCA عبر endpoint زوهو `einvoice/push`؛ لا يوجد تكامل مباشر موازٍ مع زاتكا.
- الأرصدة الافتتاحية مستبعدة دائمًا. `zoho_write_operations` ومفتاح `zatca_push:<zoho_id>` يمنعان التكرار ويحفظان نتيجة كل فاتورة.
- التشغيل اليدوي يفرض معاينة ثم تأكيدًا صريحًا وصلاحية `agents.approve_sensitive`. كل دورة مجدولة أو يدوية تسجّل في `work_agent_runs`.
- نقطة الحقيقة: `supabase/functions/zatca-auto-push/index.ts`، وإعداداته في `work_agents.config` وRPC `configure_zatca_work_agent`.

### 28.3 الوكيل الثالث: تقرير الإدارة اليومي ✅ (2026-08-02)
- `management_daily_report` يعمل يوميًا الساعة 10:00 ص بتوقيت السعودية، ويُنشئ تقريرًا داخل النظام فقط؛ لا يرسل واتساب أو بريدًا دون اعتماد قناة خارجية مستقل.
- التقرير يجمع: عدد وقيمة فواتير العملاء المتأخرة، فواتير زاتكا المعلقة، العملاء الجدد اليوم، العملاء بلا مسؤول، المهام المتأخرة، وأخطاء الوكلاء خلال 24 ساعة، مع وقت آخر مزامنة زوهو.
- التشغيل المجدول واليدوي يحفظ snapshot كاملًا في `work_agent_runs.details` وملخصًا عربيًا في `summary`. المعاينة لا تكتب سجلًا.
- نقطة الحقيقة: `work-agent-management-report` وRPC الخدمة `management_daily_snapshot`، والتحكم عبر `configure_management_report_agent`.

### 28.4 الوكيل الرابع: صحة التكاملات ✅ (2026-08-02)
- `integration_health` يعمل كل 6 ساعات عند الدقيقة 5 (`5 */6 * * *`)، ويراقب ستة فحوص: حداثة زوهو، هاتف، بيانات المنصة، Webhooks، آخر تشغيل مزامنة زوهو، والمهام الأساسية في Cron.
- هو وكيل مراقبة فقط: لا يعيد تشغيل التكاملات ولا يعدّل البيانات تلقائيًا. النتيجة تحفظ في `work_agent_runs.details.snapshot`، والحالة `partial` عند وجود مصادر تحتاج مراجعة.
- من `/work-agents` يمكن تشغيله/إيقافه، تغيير التكرار (15/30/60 دقيقة أو 6 ساعات)، وضبط حدود قدم كل مصدر، مع معاينة حيّة دون كتابة. المعاينة تسمي الفحوص الستة وتعرض سبب كل حالة بدل عداد مبهم.
- حداثة زوهو تُقاس من آخر تشغيل مزامنة ناجح، لا من آخر فاتورة تغيّرت؛ نجاح مزامنة بلا تغييرات يبقى نجاحًا. Webhooks ذات `status='processed'` لا تُعد عالقة حتى لو كان سجلها التاريخي بلا `processed_at`؛ العالق هو `pending/processing` فقط. فشل زوهو القديم خلال 24 ساعة يبقى معلومة، وتُحسم الصحة من حالة آخر تشغيل.
- نقطة الحقيقة: `work-agent-integration-health` وRPC الخدمة `integration_health_snapshot` والتحكم عبر `configure_integration_health_agent`.
- الكتالوج التشغيلي الحالي مقصور على الوكلاء الأربعة المفعّلين أعلاه؛ حُذفت مسودات `new_leads` و`bank_reconciliation` و`monthly_close` في 2026-08-02، ولا تُعرض بطاقات غير مؤسسة في `/work-agents`.

## 29. المدين والدائن ومبلغ التحصيل — فصل محاسبي ثابت ✅ (2026-08-02)
- `customer_ar.total_due` يبقى **الرصيد المدين الخام من Zoho**، و`unused_credits` يبقى **الرصيد الدائن غير المستخدم**. لا يجوز دمجهما في حقل واحد أو اعتبار الرصيد المغطى «مسددًا» قبل أن تنخفض المديونية فعليًا في Zoho.
- `customer_ar.collectible_due` هو إسقاط تشغيلي للمتابعة فقط: `max(total_due - unused_credits, 0)`. لا يكتب في Zoho ولا ينشئ دفعة أو قيدًا.
- توزيع الرصيد الدائن لأغراض العرض والحملات يتم بالأقدم أولًا في `customer_collectible_lines`: الرصيد الافتتاحي بتاريخ **2026-01-10** ثم الفواتير حسب الاستحقاق/التاريخ/المعرّف. هذه معاينة وليست تسوية محاسبية.
- شاشة التحصيل تعرض ثلاثة أرقام منفصلة: الرصيد المدين في زوهو · الدائن المقابل · المطلوب تحصيله. العملاء المغطّون بالكامل يُستبعدون من واتساب/الاتصال/مهام التحصيل ويظهرون في «تسويات زوهو المطلوبة».
- الرصيد الدائن الزائد لعميل بلا مديونية يبقى «رصيدًا لصالح العميل»، ولا يُخصم من مديونية عميل آخر ولا من إجمالي التحصيل إلا بقدر ما يقابل مديونية نفس جهة الاتصال.
- `customer_money_dashboard` و`zoho_overdue_campaign` و`work_agent_overdue_candidates` و`credit_stop_list` و`v_collection_candidates` تستخدم `collectible_due`/`customer_collectible_lines` للحملات والمهام. أي مسار تحصيل جديد يجب أن يستخدم الطبقة نفسها، لا `zoho_invoices.balance` أو `total_due` منفردًا.

## 30. مصدر مطابقة أرصدة Zoho الحي ✅ (2026-08-02)
- جميع أرصدة Zoho في `/reconciliation`، بما فيها قسم «غير مرتبط» ومرشح ربط الصف الداخلي، تقرأ `customer_balance_recon_zoho()` ومرآة Zoho API الحية.
- لقطات `store_balance_snapshots(source='zoho')` القديمة تاريخية للتدقيق فقط؛ ممنوع استخدامها كرصيد تشغيلي أو لعرض عميل سدد رصيده الحالي.
- اللقطة الداخلية فقط تبقى مصدر الرصيد الداخلي المرفوع؛ لا يتغير منطق الكاش أو المدين/الدائن بهذا الإصلاح.
